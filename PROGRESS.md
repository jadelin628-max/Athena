# 考研知识点记忆 App — 进度记录（跨对话续接用）

> 用途：token 上限导致新开对话时，先读本文件即可无缝续上任务，不依赖上一段对话记忆。
> 工作目录（**2026-08 已改名**）：`D:\deepseek harness work\考研\Athena`（原 `D:\deepseek harness work\考研\记忆app`，目录改名后若工具全部报 ENOENT/not found，是因为旧 cwd 不存在——pwsh 显式传 `-workdir` 到已存在目录如 `D:\` 即可恢复）。
> 注意：应用名「Athena」与文件夹名一致，但 git/备份路径若引用旧名需同步更新。

## 运行方式
- 静态站点：`index.html` + `style.css` + `app.js` + `sw.js`（PWA 离线）。
- 数据文件：`data/math3.js`（数三）、`data/econ.js`（微观）、`data/stats.js`（统计）、`data/politics.js`（政治）。
- 本地启动：`serve.ps1`（或直接双击 index.html 亦可，SW 仅在 http(s) 下生效）。
- 改完 `app.js`/`style.css`/`data/*.js` 后，**在线刷新一次**即可看到更新（SW 对静态资源是「网络优先」）。

# 📐 开发标准（内容与文本规范，v1.4.3 起固化，后续一律遵守）

## 1. 算法来源
- **核心调度**（难度 D / 稳定度 S / 可提取性 R、遗忘曲线、间隔、稳定度·难度更新、短时记忆、FSRS-6）**全部来自 FSRS 官方**（open-spaced-repetition 的 ts-fsrs / fsrs-rs v6.x，21 权重 + 官方公式）。**非官方/自创必须标注 ⚠️ 并先请示**。
- **自创（已标注 ⚠️）**：掌握度 = 存储强度对数的压缩。
- **未实现**：SSP-MMC 最优调度（值迭代/随机短路径）；权重为"平均用户"默认（未按用户数据训练）。

## 2. 内容范围（北大光华金融硕士 431）
- **431 = 微观经济学 + 数理统计（含计量经济）；无宏观/金融内容**。
- **数三 = 高等数学 + 线性代数**（概率论与数理统计归「统计」科——2026 已删数三的概率模块，避免与统计重复）。
- 覆盖教材/考纲：统计=茆诗松/何书元/陈家鼎 + 古扎拉蒂·伍德里奇计量；微观=平新乔十八讲/范里安；数三=考研数学三大纲。

## 3. 例题规范
- 例题**仅真实真题**（题源在 `真题/` 的提取文本或 vision 转写）；**无真实真题则不配例题（严禁编造）**。
- **一道真题横跨多知识点可复用**于多张卡。

## 4. 文本 / 公式渲染规范
- **卡/知识点标题**含数学公式时，**必须用原始模板 `R\`...\``**（普通字符串会把 `\mathrm`/`\beta`/`\b` 等被 JS 转义破坏，如 xt10 曾不渲染）。
- **数学** `$..$`/`$$..$$` 交给 KaTeX；**正文 / 加粗 / 下划线用应用字体**：`\textbf{…}`→`<b>`、`\underline{…}`→`<u>`、`\textit{…}`→`<i>`、`\textrm`/`\textsf`/`\mathrm`/`\text`→默认字体；**不得出现字面 `\textbf`/`\n`**。
- **长答案按分点分段**：`renderTex` 检测 `①②③…`/全角`（n）`，拆成 `.ans-point` 段（跳过数学下标/命令内标记如 `X_{(1)}`、`\textbf{①…`）。

## 5. 时间预算（软上限）
- 每日时间预算为**软上限**：超出仅提示，**不限制继续学习**；新卡引入不被剩余预算封顶。

## 6. 数据安全（改文件铁律）
- 改 UTF-8 数据文件**禁用 PowerShell `Get-Content`/`Set-Content`**（默认按 GBK 解码会致中文乱码——曾因此丢失 `stats.js`）。
- 用 **write 工具**或 **Python `open(...,encoding='utf-8')`**（不加 BOM）；**先备份**、只追加/定点编辑、每步 `node --check`+去重/引用完整性。
- **子代理不并行改同一文件**（防冲突；曾有子代理自行派生子代理导致失败/丢失）。

## 7. 版本号与更新日志管理
- **只有在用户明确发出「版本号更新」命令后**，才升 `VERSION`、写新版本 CHANGELOG 条目、升 `sw.js` 缓存版本；届时**总结自上次版本更新以来的所有改动**写入日志。
- **未获命令前不改版本号**（即使做了改动，也仅记录在 PROGRESS/说明里，等用户下令再统一写入版本日志）。
- **升版本必须同步的 5 处**（漏一处都会不一致）：① `app.js` 的 `const VERSION`；② `app.js` 的 `CHANGELOG` 最新一条 `.v`；③ `sw.js` 的 `const VERSION`（缓存版本 `ms3-vN`，独立体系）；④ `index.html` 的 `?v=` 缓存戳（manifest + 图标，共 3 处须一致）；⑤ `PROGRESS.md` 最新的「## 版本 vX.Y.Z」小节。
- **升版本后跑 `node tools/check_version.mjs`** 验证一致性（它自动比对上述 5 处）。

## 8. 正文「分点」格式
- 长答案用 `① ② ③…` 或全角 `（1）（2）…` 分点；`renderTex` 会据此拆成 `.ans-point` 段。
- **加粗统一用 `\textbf{…}` 或用 markdown `**…**`**（二者都会渲染成 `<b>`、应用字体）；**不要混用造成不平衡**。

## 9. 改完必跑校验（tools/ 三道闸）
- 改任何 `data/*.js` → `node tools/check_data.mjs` + `node tools/check_render.mjs`（数据完整性 + 渲染不变量）。
- 改 `app.js` 渲染逻辑 → `node tools/check_render.mjs`。
- 升版本 → `node tools/check_version.mjs`。
- 详见 `tools/README.md`；三者全绿才算改完。

## 版本 v1.4.4（渲染器重构 + 掌握度/目标改稳定度 S 量纲 + 工程校验工具化，已完成）
- **渲染器重构为字符级扫描器**：`renderInto` 从「先按 `$` split、再 `renderProse` 转文本命令」改为**字符级扫描**——`\cmd{...}` 用**括号配对**读取（支持嵌套花括号，如 `\textbf{方差用 $\chi^{2}$}` 不再被 `{2}` 截断）、`**…**`/`$…$`/`$$…$$` 作为整体 token 递归渲染。修复「命令内嵌数学被 `$` 拦腰切断 → `\textbf` 字面残留」一类 bug；`renderProse` 同时支持 markdown `**…**`→`<b>`（应用字体）。
- **毕业目标 & 掌握度量纲从「半衰期 h」改为「稳定度 S（天）」**（⚠️ 自设计展示层，FSRS 调度内核未动）：
  - 起因：倒计时 120 天时目标半衰期显示 10843 天（≈29.7 年）——半衰期 h=K·S、K≈90.35 放大导致目标数值失真、掌握度虚高（S=2.3 天即显示 57%）。
  - 修正：毕业目标 `targetH()`→`targetS()` = **剩余天数**（下限 14 天），语义「停止复习后仍 ≥90% 记得」；`isGraduated` 改判 `stab ≥ targetS()`。**毕业时机完全不变**（原判据 h≥K·remain ⟺ S≥remain，K 约掉）。
  - 掌握度 `ln(1+h)/ln(1+H)` → `ln(1+S)/ln(1+S_N)`：S=1 天→14%、S=10 天→50%、S=30 天→72%、S=120 天→100%，不再虚高。
  - **学习阶段掌握度不再「固定挡位 10+step×5」**：改用 FSRS 短时稳定度 `c.stab`，随评分真实变化（再次≈4%/困难≈17%/良好≈25%/简单≈46%，S_N=120 天计）。
  - 设置项「毕业目标半衰期」→「毕业目标稳定度」；`DB.settings.targetH`（旧键）迁移删除、改用 `targetS`（默认 90 天）；半衰期 h 仅作卡面参考展示。
- **删除正文泄漏的内部卡片编号**：全量扫描，删除面向用户正文里的内部 ID（`（cu26）`/`（un05）`/`（rv12）`/`（wf13）`/`（it08）`/`（gm16）`/`（gm17）`/`（we11）`/`（we12）`/`（fl15/gm14，…）` 等 11 处，全在 `econ.js`；`\ge0`/`\to0`/`\ne1` 等合法 LaTeX 未动）。
- **数据渲染 bug 修复**：`math3.js` zb19（inline `\begin{vmatrix}…\\…\end{vmatrix}` 的 `\\` 非法→改行间公式、`$|A&C\\O&B|$` 错误 `\\` 残留→`$|A|\,|B|$`）；`econ.js` mk10 例题 q 补 `$` 包裹；`stats.js` jj01/jj11 例题 a 的 JSON `\\n` 字面→真 `\n`。
- **工程校验工具化（tools/ 三道闸，零 npm 依赖）**：`check_data.mjs`（数据完整性+内容不变量）、`check_render.mjs`（headless Chrome 跑真实 renderTex+KaTeX，断言 0 katex-error/0 字面 `**`/0 残留命令）、`check_version.mjs`（版本标记一致性）；`tools/README.md`；数据备份归档至 `backup/`。
- **开发标准新增 #9「改完必跑校验」**；#7 增补「升版本必同步 5 处 + 跑 check_version」。
- **校验**：`node --check` app.js + 4 数据文件 = 0；`check_data`=0 泄漏/0 未闭合；`check_render`=0（891 卡/3883 字段）；`check_version`=一致。
- **版本**：`VERSION='1.4.4'`；CHANGELOG 新增 v1.4.4；sw.js ms3-v26；index.html ?v=6。

## 版本 v1.4.3（长答案分点分段 + 开发标准固化，已完成）
- **长答案分点分段**：`renderTex` 顶部加 `splitPoints`，检测 `①②③…` 与全角 `（1）（2）…` 分点，把长答案拆成 `.ans-point` 段（带左侧竖线 + 间距）；跳过数学下标/命令内的标记（`X_{(1)}`、`\textbf{①…` 等）以免破坏结构。实测 `jj06`=12 段、`reg06`=4 段、`xt10`（推导无分点）=0 段（单流，不多拆）、无 `katex-error`。
- **开发标准固化**：在 PROGRESS 顶部写入「📐 开发标准」（见上），汇总此前你提出/默许的规范：算法来源（核心=FSRS 官方、自创需⚠️标注并请示）、内容范围（431=微观+统计·无宏观金融；数三=高数+线代）、例题仅真实真题且可跨知识点复用、标题用原始模板、正文加粗/下划线用应用字体、长答案按分点分段、时间预算软上限、数据文件禁 PowerShell GBK 读写。
- **版本**：`VERSION='1.4.3'`；CHANGELOG 新增 v1.4.3；sw.js ms3-v25。

## 版本 v1.4.2（正文文本模式命令改用 HTML 渲染，字体彻底统一，已完成）
- **按你确认**：把正文里的 `\textbf`/`\underline`/`\textit` 等**不再交给 KaTeX**（KaTeX 字体），而是**转成 HTML**：`\textbf{…}`→`<b>`、`\underline{…}`→`<u>`、`\textit{…}`→`<i>`、`\textrm`/`\textsf`/`\mathrm`/`\text`→（默认字体）仅渲染内部文本；命令内部若含 `$..$` 数学则**递归交给 KaTeX**（这样 `\textbf{均值用 $t$}` 里的 `$t$` 仍是公式）。
- **实现**：`renderTex` 拆为 `renderInto(el, str)`（拆 `$..$`/`$..$$` 交给 KaTeX）+ `renderProse(el, p)`（正文保留为文本节点；把文本模式命令转成对应 HTML 标签，内部内容再 `renderInto`）。**数学用 KaTeX，正文/加粗/下划线用应用字体**。
- **校验**：`node --check` 6 文件全 0；CDP（jj06 可见层 **11 个 `<b>`**、可见文本无字面 `\textbf`、无 `katex-error`、math `$..$` 正常；reg06 `hasB/hasU:true`）。
- **版本**：`VERSION='1.4.2'`；CHANGELOG 新增 v1.4.2；sw.js ms3-v24。

## 版本 v1.4.1（修复正文渲染字体不一致 + \\textbf/\\n 残留，已完成）
- **问题**：上一版用 `\text{}` 包裹整段带命令的正文，带来两个副作用：① **字体不一致**——被包裹的正文用 KaTeX 字体、未包裹的用应用字体；② **跨行正文（真实换行）被 KaTeX 报错**，显示字面 `\\n`/`\\textbf`。
- **修复（`renderTex` prose 分支）**：改为「普通正文（含换行）保留为**文本节点**（应用字体），仅把 `\textbf{}`/`\underline{}`/`\textit{}`/`\textrm{}`/`\textsf{}`/`\mathbf{}`/`\underline` 等**文本模式命令片段**单独交给 KaTeX 渲染」。→ 字体统一、无 `\\n`/`\\textbf` 字面残留。
- **排查**：扫描数据确认**没有字面 `\n`**（命中的是 `\not` 命令、或 EXAMPLE JSON 里的 `\\n`=换行，均合法）——所以此前 `\n` 是 `\text{}` 包裹跨行正文导致 KaTeX 报错，而非数据问题。
- **校验**：`node --check` 6 文件全 0；CDP 对 jj06/reg06/test03/xt13 等多张含 `\textbf` 的卡：`hasError:false`（无 katex-error / 无字面 `\n`）、`hasK:true`（命令已用 KaTeX 渲染）、中文保留为文本节点。
- **版本**：`VERSION='1.4.1'`；CHANGELOG 新增 v1.4.1 条目；sw.js ms3-v23。

## 版本 v1.4.0（学习步进对齐 FSRS-6 + 时间预算软上限 + 若干修复，已完成）
- **学习步进严格对齐 FSRS-6**：区分 **Learning**（新卡，`learning_steps=['1m','10m']`）与 **Relearning**（复习遗忘，`relearning_steps=['10m']`）两个独立状态。复习卡"遗忘"→ `state='relearning'`，10 分钟一步重学（复用 `STEP_MS`/`LAST_STEP` 为学习用，`RELEARN_MS=[10min]`/`RELEARN_LAST_STEP=0`）；不再复用"1m→10m"两步。所有状态判断（sanitize/统计/队列/文案/`doRate` push/`buildSession`/`surfaceDue`/`nextReviewText`/`mastery`）均纳入 `'relearning'`。
- **时间预算改为"软上限"**：超出预算仅提示（统计条「⏱ 今日 X / 预算 Y 分（已超出，可继续）」、完成页提醒），**不再限制继续学习**；新卡引入（首次收卡与"再来 N 张"）**不再被剩余时间预算封顶**（删去 `waveBudgetCap()` 门控）。
- **修复列表**：
  1. **含数学公式的卡标题**（普通字符串里的 `\mathrm{Var}(\hat\beta)` 等被 JS 转义破坏：`\m`→`m`、`\h`→`h`、`\b`→退格）——用脚本把"标题含反斜杠的普通字符串"批量转成原始模板 `R`...``（stats.js 转 3 处）；`xt10`「多元回归模型与 Var(β̂)」现正常渲染。
  2. **学习界面卡片右上不再显示题目**（`renderLearnCard` 去掉 `f.title` 的 `.muted` 标题，防透题；保留分类徽章）。
  3. **公式容器**（你问的更好方案→采用组合）：**行内公式允许换行**（`.kx-inline{white-space:normal}`，自然排版、不再每个行内公式出滚动条）；**仅超宽展示公式** `.kx-block` 用横向滚动容器；容器 `overflow-x:auto` 兜底，杜绝移动端整页缩放。
- **校验**：`node --check` 6 文件全 0；CDP（页脚 v1.4.0、学习卡右上无 `.muted` 标题、`xt10` 标题正常渲染 `mord mathrm` 无 `katex-error`、375px 视口宽行内公式 `wrapped=true` 且 `bodyW=375` 不缩放）；C1/C2/C3 三项 + 版本均通过。A（relearning 分支）、B（软预算）逻辑经语法校验 + 状态机代码确认。
- **版本**：`VERSION='1.4.0'`；CHANGELOG 新增 v1.4.0 条目；sw.js ms3-v22。

## 版本 v1.3.9（记忆算法升级 FSRS-6 + 移动端/渲染修复，已完成）
- **记忆算法 FSRS-4.5 → FSRS-6（2024-2025 最新，用户指定升级）**：
  - 采用 **官方 21 参数默认权重**（ts-fsrs/fsrs-rs v6.x `DEFAULT_PARAMETERS`：`[0.212,1.2931,2.3065,8.2956,6.4133,0.8334,3.0194,0.001,1.8722,0.1666,0.796,1.4835,0.0614,0.2629,1.6483,0.6014,1.8729,0.5425,0.0912,0.0658,0.1542]`，w20=decay=0.1542）。
  - 遗忘曲线：`R(t,S)=(1+factor·t/S)^{decay}`，`factor=e^{ln0.9/decay}-1`，`decay=-w20=-0.1542`（已验证 t=S 时 R=0.9）。
  - 间隔：`I = clamp(round(S·r^{1/decay}-1/factor), 1, ...)`，DR=0.9 时 `I=S`。
  - 初始稳定度 `S0(G)=max(w[G-1],0.1)`：Again 0.212 / Hard 1.293 / Good 2.306 / **Easy 8.296**（原 4.5 为 0.40/1.18/3.17/15.69）。
  - 难度：`delta=-w6·(G-3)` → 线性阻尼 `(10-D)/9` → 均值回归到 `D0(Easy)=w4`（锚点改为 Easy，原为 3）。
  - 成功稳定度 `S'=S·(1+e^{w8}(11-D)S^{-w9}(e^{w10(1-R)}-1)·hardPenalty·easyBound)`，w15=Hard 惩罚、w16=Easy 加成。
  - 遗忘稳定度：`sForget=w11·D^{-w12}·((S+1)^{w13}-1)e^{w14(1-R)}`，并与**短时记忆封顶 `S/e^{w17·w18}`** 取小 → 遗忘后不高于遗忘前。
  - **短时记忆稳定度（引入，此次核心）**：`S'=clamp(S·max(s^{-w19}·e^{w17·(G-3+w18)}, 1 当 G≥2))`——**学习期 Again 会降低稳定度，Good/Easy 不降**。
  - **学习阶段重构**：新卡首评建立初始 D/S；其后同日学习步进用**短时记忆**更新；毕业（Good 过最后步 / Easy）用**当前稳定度**定间隔。
  - ✅ **修复用户问题**：反复忘后总算"简单"毕业，不再给 15.69 天（16 天间隔）。实测：新卡点"简单"→ **8 天**；`Again→(同日)简单`→ 短时记忆 → **约 1 天**。
- **毕业/半衰期/目标 h_N 同步 FSRS-6**：半衰期 `h=K·S`，`K=(0.5^{1/decay}-1)/factor≈90.4`（FSRS-6 曲线较平缓）；`targetH()` 挂钩时 =`K·剩余天数`（仍等价 **S ≥ 剩余天数** ⟺ 考试日可提取性≥90%）；`isGraduated/graduatedCount/halflifeHistogram` 复用该换算。
- **（同批）前两条修复**：`\textbf/\underline` 正文渲染（见 1.3.8 续写）、移动端长等式溢出（`.kx-inline` 内部滚动 + 容器 overflow-x:auto，实测 375px 视口 body 不缩放、宽公式内部滚动）。
- **校验**：`node --check` 6 文件全 0；独立 Node 数值校验（R(S,S)=0.9、Easy 初始间隔 8、Again→Easy 短时记忆≈1 天、K=90.35）；CDP（页脚 v1.3.9、新卡"简单"毕业 8 天/稳定度 8.2956、统计页 6 KPI、无运行时异常）；四科计数 238/286/180/187 不变。
- **版本**：`VERSION='1.3.9'`；CHANGELOG 新增 v1.3.9 条目；sw.js ms3-v21。
- **说明**：FSRS-6 为官方开源实现（open-spaced-repetition ts-fsrs/fsrs-rs v6.x），权重/公式均来自官方，非自创；"短时记忆"为 FSRS-5/6 官方机制。学习步进仍用 app 既有 `[1m,10m]`（对齐 FSRS 默认 learning_steps）。

## 版本 v1.3.8（修复 \textbf/\underline 渲染 + 统计安全重做 + 数三例题回校，已完成）
- **修复：`$` 之外的文本模式 LaTeX 命令不渲染**：`renderTex` 只对 `$...$`/`$$...$$`（数学模式）交给 KaTeX，`$` 之外的中文/正文被当作纯文本节点，导致 `\textbf{}`/`\underline{}` 等文本模式命令原样显示。修复：对"含 `\\[a-zA-Z]+` 命令且花括号平衡"的正文段，用 `\text{...}` 包起来交给 KaTeX（`\textbf`/`\underline`/`\mathrm` 等在 `\text{}` 内可正常渲染，已实测），否则保持纯文本。中文正文不受影响；`$` 内数学不受影响。`VERSION='1.3.8'`；sw.js ms3-v20。
- **统计模块"完整内容拓展"（安全重做，无事故）**：163→**180 卡**（套路卡 ys01–05 + 广度/计量 jj01–12；细化 pb02/pb03/rv01/nc09/reg06/est05 的 back；仅 3 条真实真题 EXAMPLE：jj01 南开432-2021 / jj06 中科大432-2016 / jj11 上交432-2020）。独立核验：`node --check` 0、180 卡无重复、cat 合法、META/EXAMPLE/REL/DEPTH 键与 REL.to 零缺失。**全程严禁 PowerShell GBK 读写**（吸取统计事故教训），并有 `stats.js.bak.pre2` + `stats.js.bak.guard` 双备份。
- **数三真题语料（干净版）**：52 页全部渲染 + read_image 转写（6 条转录子代理 A/B/C/D/E1/E2/F），合并稿 `数三真题_2010-2021_vision_merged.txt`；页码→年份对照见下（逆序）。
- **数三例题回校（已完成）**：第一版回校子代理自行派生 9 个逐年验证子代理后失败（`math3.js` 未被改动）。改用**单代理、禁止派生**（`213ede3a`）重跑成功并独立核验：**修正 1 处**（lim17 2020：`e^{1/(x-1)}`→`e^{1/(x+1)}`，答案 3→2 选B，解析更正）、**新增 1 条**（lim07 2012 数三补 EXAMPLE）、约数十条核对正确未改、约十余条因无法在语料精确定位而**保守未改**（"宁可不改"）；`node --check` 0、238 卡无重复、107 个 EXAMPLE 键零缺失、仅改 EXAMPLE q/a + 新增 1 条（未动 id/cat/title/front/back/META/REL/DEPTH）。**结论**：此前尽管源文本是低质 OCR，子代理重构的例题大多正确（靠领域知识+片段线索），仅 lim17 一例被证实错误并已修正——例题质量总体可靠。
- **范围外未处理（留待后续）**：`src` 为 2009 前（ser10/lim12/tay02/mvt01）与 2022–2026（含数组元素，如 int16/ode05/tp03/zt06 等）的例题，因不在 2010–2021 干净语料覆盖内，未回校。
- **备份清单（勿删，供恢复）**：`data/stats.js.bak.pre2`、`data/stats.js.bak.guard`（统计主/次备）；`data/math3.js.bak.pre`（数三主备）；`data/stats.js.corrupt.bak`（乱码损坏副本，可删）。

## ⚠️ 数据事故与恢复（stats.js，2026-08）
- **事故**：做"统计套路卡/细化/计量广度"的后台子代理用 PowerShell 5.1 的 `Get-Content`（默认按系统代码页 **GBK** 解码）读取 **UTF-8** 的 `data/stats.js`，导致全部中文变乱码，随后删除该文件 → `stats.js` 丢失（163 卡）。
- **恢复**：该子代理称任务开始时已把 `stats.js` 全部 1219 行逐行读进会话。授权它**只忠实重建原始内容**（含 xt01–xt15，新增卡全部丢弃；禁用 `Get-Content/Set-Content`；UTF-8 无 BOM；必须过 `node --check`+完整性）。
- **独立核验（本 agent）**：`node --check` exit 0；163 卡、无重复 id；`pb01`(样本空间与随机事件)/`rv01`/`xt01`/`xt15` 均正确；四科完整性脚本 **OK**（统计163/微观286/数三238/政治187）。
- **教训（勿回退）**：凡是改 UTF-8 数据文件的脚本/命令，**一律用 write 工具或 `Python open(...,encoding='utf-8')`，绝不使用 PowerShell 的 `Get-Content`/`Set-Content`（默认 GBK），也不加 BOM**。子代理改数据文件前应先复制一份原始内容备份（如 `*.bak`），并只用追加/定点编辑。
- 现状：`data/stats.js` 已恢复至 163 卡；`stats.js.corrupt.bak` 为乱码损坏副本，可安全删除（本 agent 未删，留作证据）。
- **未完成**：统计学科第二轮"全面覆盖（套路卡/细化/计量广度）"内容随事故**丢失**，需要在安全前提（不用 GBK 解码、先备份、只保留真真题）下重新补做。

## 版本 v1.3.7（统计页重构 + 内容拓展，已完成）
- **交互改动（app.js + style.css，本模型实现并验证 9/9）**：
  - **统计页重构 + 记忆算法可视化**：总览 KPI（总卡片/已毕业/待复习/平均掌握(存储)/平均可提取R/累计遗忘）→ 学习日历热力图 → **记忆算法关键指标趋势**（平均掌握度·存储、平均可提取性 R、待复习、已毕业、累计遗忘、学习中新卡，每日快照 `DB.log.metrics`，`sparkTrend` 画线）+ 记忆状态分布 + 记忆强度（半衰期 h）分桶分布 + 各分类掌握度。
  - `snapshotMastery` 扩展为每日记录 `DB.log.metrics[t]`（avg/avgR/due/grad/lapses/learn/review）；新增 `avgCurrentR/graduatedCount/totalLapses/stateCounts/halflifeHistogram`，`sparkTrend` 折线助手。
  - **浏览页学习卡片**：`browseItem` 的「📝 我的笔记」模块上移至「🧠 记忆」模块之上（与学习页一致，v1.3.0 的规则补齐到浏览页）。
- **内容拓展（3 个子代理并行，仅追加、不碰旧卡；例题仅真实真题）**：
  - **统计 ×15（xt01–xt15，148→163）**：次序统计量分布/并/独立、充分与完备统计量、UMVUE 与 Lehmann-Scheffé、特征/矩母函数、泊松过程、大数定律多种形式、多元回归 Var(β̂)、多重共线性、异方差与 White 检验、Wilcoxon 秩和、K-S 与 χ² 拟合、列联表独立性——对齐 茆诗松/何书元/陈家鼎 + 古扎拉蒂/伍德里奇计量 + 光华431。每张均带真实真题（中科大812 / 复旦432 / 南开432 / 北大数院431 / 北大光华431 等）。
  - **微观 ×14（we01–we14，255→269）**：二级价格歧视、价格领导/主导厂商、多工厂垄断、经济租金/准租金、长期行业供给、保留工资与劳动供给、一般均衡的核、价格补贴vs一次性补贴福利、时间偏好贴现、斯宾塞教育信号、赢家诅咒、从量税vs从价税、地租与级差地租、资本配置线——对齐 平新乔十八讲/范里安 + 光华431。we01（2013 光华 二级价格歧视）、we08（2019 光华 供暖补贴）带真实真题；其余 12 张在真题文本中无对应题，**按规则未配例题（未编造）**。
  - **数三 ×16（gk01–gk16，209→225）**：新增 `prob:'概率论与数理统计'` 分类并加入 ORDER（数三大纲含概率统计约 1/3，此前数三数据集无概率卡，是最大空洞）；覆盖常见分布期望方差总表/二项/泊松/均匀/指数无记忆性/正态标准化/期望性质/方差中心矩/协方差相关系数/大数定律/中心极限定理/矩估计/极大似然/估计评选/置信区间/假设检验。gk03/05/06/07/08/09/12/13/14 带数三真题；其余按规则无真题未配例题。（子代理核实数三**无**三重积分/曲线曲面积分，故未加入，避免错内容。）
  - **跨知识点复用**：一条真题横跨多个知识点时，可在多个卡片下重复出现（各子代理已按此处理，如中科大次序统计量题复用于 xt01/xt03）。
- **校验**：`node --check` 6 文件全 0；数据完整性脚本**OK**（统计163/微观269/数三225/政治187，均无重复id、cat 合法、META/EXAMPLE/REL/DEPTH/PITFALL/MNEM 键与 REL.to 全落地、ORDER 合法）；headless Chrome + CDP **9/9**（页脚 v1.3.7、三科总卡数 225/269/163、新卡 gk03/we08/xt05 在浏览页存在且带真实真题来源、统计页渲染含 6 KPI、零运行时异常）。
- **版本**：`VERSION='1.3.7'`；CHANGELOG 新增 v1.3.7 条目；sw.js ms3-v19。
- **说明**：本轮为「内容广度补强 + 交互可视化」，记忆算法内核（FSRS-4.5）未改动。

## 版本 v1.3.6（掌握度=存储强度到目标 + 目标倒计时通用化，已完成）
- **① 掌握度算法改为「存储强度到目标的比」（用户拍板：前者，毕业=100%）**：
  - `mastery()` 复习阶段改为：`h=cardHalflife(c)`（半衰期=存储强度）、`H=targetH()`（毕业目标），`s=clamp(ln(1+h)/ln(1+H),0,1)`，`掌握度=round(100·s)`。**已毕业（h≥H）⟺ s=1 ⟺ 100%**。
  - 对数压缩：使「记忆强度增长先快后慢」更符合直觉、避免备考早期目标 h_N 大（如 1535 天）导致全员 0%。
  - 顶标签改为「毕业」（`isGraduated` 为真即可）；浏览筛选/统计/导图/原理图例同步加入「毕业」。**FSRS 调度内核未动**（间隔仍 `I=S`），掌握度只是重新解读 ⇒ 标注 ⚠️ 自设计（依据论文「存储强度」概念 + 对数映射）。
  - 实测：距 120 天时目标 1535，Easy 首卡 h=200.7 → 掌握度 72%「已掌握」（未达标）；目标日改到今天 → 目标 179，该卡 → 100%「毕业」。**证实「毕业=满」且早期不虚高**。
- **② 趋势图与掌握度解耦**：新增 `currentR(id)`；`doRate` 历史快照改存「复习时可提取性 R」；`svgMasteryTrend` 标题改为「当前可提取性 R 趋势（时间）」，实际 vs 预测两条线同为 R、量纲一致。（掌握度徽章=存储强度；可提取性 R 另在卡面/趋势图显示——二者分离，对应论文「存储/提取强度」。）
- **③ 考研倒计时 → 目标倒计时（目标名/日期可编辑，考研后可复用）**：
  - 新增 `DB.settings.goalTitle`（默认「考研」）；`goalTitle()` 助手；`EXAM_AUTO_MONTH/DAY` 逻辑保留（留空默认最近一个 12/20）。
  - `updateCountdown()`/`showCountdownPopup()` 用 `goalTitle()`：徽章「📅 距{目标} N 天 / 🎯 今天是{目标}」、弹窗「距离{目标}还有…/今天是{目标}日！」、提示语「目标日期：…」。
  - 设置页：「考研日期」行拆为「目标名称」文本输入（maxLength 10，可编辑）+「目标日期」date 输入；描述说明改名/改期后 App 继续可用（复习排期不受影响，仅倒计时/毕业目标/掌握度随之更新）。
  - `memoryBox` 目标文案「稳至{目标}(≥90%) · 距{目标} N 天」；`targetLinked()`/`targetH()` 相关文案与内部注释全部改为「目标倒计时」。
  - **回答用户「改日期是否影响数据」**：会——顶部倒计时/每日弹窗/毕业目标 h_N/掌握度（都按剩余天数算，改日期整体重算）；**不会**——复习排期（FSRS `I=S` 与日期无关）。
- **版本**：`VERSION='1.3.6'`；CHANGELOG 新增 v1.3.6 条目；sw.js ms3-v18。
- **验收**：`node --check` app/sw 全 0；headless Chrome + CDP **9/9**（页脚 v1.3.6、徽章「距考研 120 天」、「目标名称」默认考研、改成「四六级」后徽章「距四六级 120 天」且持久化、未达标卡「已掌握 72%」/达标后「毕业 100%」、记忆框「稳至四六级≥90% · 距四六级 120 天」、趋势图标题「当前可提取性 R」、零报错）。

## 版本 v1.3.5（毕业目标与考研倒计时挂钩，已完成）
- **用户拍板**：毕业目标 `h_N` 与考研倒计时挂钩（保证等级沿用默认 90%），改动标注为「⚠️ 借 SSP-MMC「目标半衰期」概念 + 适配考研目标（非论文原始算法）」。
- **核心算式**：`targetH() = 3·剩余天数/(p0^-2 − 1)`，`p0 = TARGET_CONFIDENCE = 0.9`。因 FSRS 满足 `R(S,S)=0.9`（幂律 `R(t,S)=(1+F·t/S)^-0.5` 在 t=S 时=0.9），故「考试日可提取性 ≥0.9」⟺ **稳定度 S ≥ 剩余天数**，等价于半衰期目标 `h_N ≈ 12.79×剩余天数`。
- **效果**：备考早期剩余天数大→目标高、几乎只有很熟的卡「已毕业」；临近考试剩余天数小→目标降低、更多卡达标（时间短、记忆撑到考试的门槛自然低）。剩余天数设下限 `TARGET_MIN_DAYS=14`，避免考前/考后任何卡都被标「已毕业」。
- **开关**：设置页「毕业目标半衰期」新增复选框「与考研倒计时挂钩」（默认开，`DB.settings.targetLinkExam`）；关闭则回退到手动固定值（默认 90 天）。记忆框在挂钩时显示「目标：稳至考试(≥90%) · 距考研 N 天」，关闭时显示「目标 h_N=… 天」。
- **版本**：`VERSION='1.3.5'`；CHANGELOG 新增 v1.3.5 条目；sw.js ms3-v17。
- **验收**：`node --check` app/sw 全 0；headless Chrome + CDP **8/8**（页脚 v1.3.5、设置勾选默认开、描述「距考研 120 天 → 目标 h_N≈1535 天」、记忆框「目标：稳至考试(≥90%) · 距考研 120 天」且无「目标 h_N=90 天」、挂钩时 Easy 卡（h=200.7<1535）不显示「✔已毕业」、关闭挂钩后显示「目标 h_N=90 天 · ✔已毕业」、零报错）。
- **回答用户「h 怎么算」**：`fsrsHalflife(S)=3·S/F`（F=19/81），由 **FSRS 幂律** `R(t,S)=(1+F·t/S)^-0.5` 令 R=0.5 解出（h≈12.79·S）——**不是**论文 HLR 的指数定义（`p=2^{-Δt/h}`）。因排期引擎是 FSRS 幂律，用幂律下的 50% 时间作 h 才与真实遗忘曲线自洽；未改成指数 h。
- **回答用户「间隔怎么算」**：复习间隔 `I = (S/F)·(FDR^-2 − 1)`（`fsrsInterval`），FDR=0.9、F=19/81 ⇒ `FDR^-2−1 = F`，故 **I = S** 恰为稳定度 S —— 即「在回忆概率正好掉到 0.9 时复习」。S 每轮由 `fsrsDifficulty`（难度含均值回归）+ `fsrsSuccessStability`/`fsrsLapseStability`（随真实可提取性 R、等级 G 非线性更新；间隔久仍记住→涨更多、难度高→涨很少、遗忘→降）决定。学习阶段（新/学习卡）不走 FSRS，用时间步进（1min→10min→毕业）。

## 版本 v1.3.4（SSP-MMC 启发：时间预算 + 目标半衰期 + 双维度，已完成）
- **依据**：IEEE TKDE 2023《Optimizing Spaced Repetition Schedule by Capturing the Dynamics of Memory》（墨墨 SSP-MMC-Plus，KDD'22 扩展版，`真题/../` 无、见 zhuanlan 656714407 摘要）——核心是「以最小记忆成本（MMC）达到目标半衰期 h_N」，且用「时间/成本约束」而非「张数」安排复习。
- **① 每日复习目标改为「时间预算」**（`DB.settings.minutesPerDay`，默认 20 分钟 / 设置 5~120）：
  - `budgetSec()/todayCostSec()/todayCostMin()/remainingBudgetSec()`：以时间为单位的每日预算，追踪今日实际用时。
  - `RATING_COST_S=[9,6,3,1]`（忘记/困难/良好/简单，对齐论文「认识≈3s / 忘记≈9s」）；`doRate` 里 `addCost(r)` 把每次评分的成本计入 `DB.log.cost[今日]`。
  - `waveBudgetCap()` = 剩余时间预算 ÷ 每张新卡估计成本（`EST_NEW_CARD_SEC=10`）；`buildSession` 首波与 `newwave` 引入的新卡数按剩余预算封顶（读作「再来 N 张」受预算约束）。
  - `statsBar` 显示「⏱ 今日 X / 预算 Y 分」；完成页在预算用尽时提示「今日时间预算已用尽…明日继续／设置里调高」。
  - 复习（到期卡）保持优先、不被阻断；时间预算主要约束「每天引入多少新卡」与「把复习实际用时计入预算」（复习是事实上的大头）。
- **② 引入「目标半衰期 h_N」毕业判据**（`DB.settings.targetH`，默认 90 天 / 设置 7~730）：
  - `fsrsHalflife(S)=3·S/F`（由幂律 `R(t,S)=(1+F·t/S)^-0.5` 解 R=0.5 得到；F=19/81 ⇒ h≈12.79·S）；`cardHalflife(c)`。
  - `isGraduated(c)`：复习卡半衰期 ≥ 目标值 → 视为「毕业/稳固」；`stats()` 的 `mature` 由原「ivl≥21」改为 `isGraduated`。
  - 记忆框居中显示「半衰期 h=… 天 · 目标 h_N=… 天」，并标注「✔已毕业」；不再显示固定「目标保留 90%」（FDR=0.9 仅保留为 FSRS 排期的期望保留率，不影响毕业概念）。
- **③ 卡面双维度（存储强度 S + 提取强度 R）**：`memoryStrengthText(c)` 输出「存储强度 S=…天 · 半衰期 h=…天 · ✔已毕业」；学习视图 meta 中复习卡附加显示（掌握度徽章仍是当前可提取性 R %，二者分离，对应论文「存储强度/提取强度」）。
- **验收**：`node --check` 5 文件全 0；headless Chrome + CDP 8/8（页脚 v1.3.4、统计条「⏱ 今日 0 / 预算 20 分」、设置含「每日复习时间预算」默认 20 与「毕业目标半衰期」默认 90、评分 Easy 毕业卡记忆框显示「稳定性 15.7 天 · 半衰期 h=200.7 天 · 目标 h_N=90 天 · ✔已毕业」且不再「目标保留 90%」、零报错）。
- **未做/后续**：按论文把「到期复习也按每日时间上限硬性顺延」（risk：导致复习积压）、SSP-MMC 的值迭代调度（需训练好的记忆模型，本地 PWA 无 2.2 亿条数据）；当前做法是「时间预算约束新卡摄入 + 复习计入预算」的务实版。

## 版本 v1.2.2（时政 2026 化 + 更新日志页，已完成）
### 已完成并落盘（data/politics.js）
- 时政分类按用户要求改为**仅收录 2026 年时政大事**（2027 考研时政范围，当前约 2026-08-20）：
  - 分类名改为 `shizheng: '2026 时政大事'`；**删除**全部「当代世界经济与政治」框架卡与 2024/2025 旧事件卡（原 sz01–sz15 中的框架类、二十届三中全会2024、2024中央经济工作会议、2025两会）。
  - 新增 7 张卡（id 沿用 sz01–sz07，内容全部基于 web 检索到的真实事件）：
    - sz01 中央经济工作会议（2025-12 举行，部署 2026 年经济工作，稳中求进以进促稳）
    - sz02 2026 年中央一号文件（2026-02，三农/乡村全面振兴/农业强国）
    - sz03 2026 年全国两会（2026-03，GDP 目标 **4.5%—5%**、通过十五五纲要）
    - sz04 「十五五」规划纲要（2026—2030，2026-03 通过、2026-01-01 起实施）
    - sz05 中德联合新闻声明（2026-02，大国外交）
    - sz06 庆祝建党 105 周年大会（2026-07-01，习近平重要讲话）
    - sz07 2026 年夏季达沃斯论坛（2026-06，大连）
  - META / REL / DEPTH / PITFALL / MNEM 已全部同步更新（sz01/sz03/sz04 为 5 星，sz05/sz07 为 3 星）；文件头注释已改。
- 参考来源（2026 检索）：[2026政府工作报告 4.5%—5%](https://finance.sina.cn/2026-03-06/detail-inhpyyhu2708189.d.html)、[建党105周年大会讲话](https://news.dahe.cn/2026/07-04/2106049.html)、[2026中央一号文件](https://www.gov.cn/zhengce/202602/content_7056997.htm)、[夏季达沃斯大连](https://www.ln.chinanews.com.cn/news/2026/0529/355602.html)。
### 已完成并落盘（app.js + style.css）
- `app.js`：
  - `VERSION` 升为 `'1.2.2'`（页脚自动显示「Athena · 版本 v1.2.2」）。
  - 新增 `const CHANGELOG = [...]`（更新日志数组，位于 VERSION 之后，含 1.0.0–1.2.2 六版条目）。
  - `renderSettings()` 增加「📜 更新日志」区块：折叠开关按钮（`data-action="togglog"`，默认折叠）+ 列表（每版一行：版本号徽章 + 日期 + 条目列表），当前版本（v1.2.2）高亮（樱粉底 + 雾蓝徽章）。
  - `handleAction` 增加 `case 'togglog':`（`classList.toggle('hidden')` 切换 `.changelog-body`，按钮文字随「展开/收起」联动）。
- `style.css`：新增 `.changelog-*` 样式（徽章樱粉渐变、当前版雾蓝→晴蓝渐变，条目 13px，沿用背多分色板）。
- `sw.js`：缓存版本 ms3-v6 → ms3-v7（发布惯例，随应用壳变更）。
- 导出文件名等无改动必要。
### 验证（已通过）
- `node --check` 全 0（app.js + 4 个数据文件）；数据完整性脚本全绿：政治总卡数 **187** 张（原 195 − 删 12 框架/旧事件 + 增 7），sz01–sz07 全部落在 `shizheng` 分类且无重复 id，四科 META/REL/DEPTH/PITFALL/MNEM/ORDER 引用全落地。
- 顺手补齐 `data/math3.js` 历史遗留的 DEPTH 缺口：`ext01`–`ext28` + `ms01`–`ms03` 共 31 张卡补上深度值（math3 DEPTH 覆盖 175→206 全卡；DEPTH 目前仅存数据、app.js 尚未消费）。
- headless Chrome + CDP 回归：版本页脚 v1.2.2、设置页「📜 更新日志」默认折叠/可展开收起/6 条记录/当前版高亮 v1.2.2、浏览页「2026 时政大事」分类仅 7 卡且卡名全对、零报错。
- CHANGELOG 草案已粘贴进 app.js（见 `app.js` 顶部）。

## 版本 v1.3.3（例题清理：仅保留真实真题，已完成）
- **追加修复**：① 新卡波次改为**完全随机**取样（`firstWave`/`newwave`/`newToStudy` 用 `shuffle` Fisher–Yates，非 DATA 顺序、也非 interleave 的「每章一个」循环），实测刷新三次波次组成各异、有同章节重复，符合完全随机；② 学习卡右上角知识点名称改用 `texEl` 渲染，含公式标题（如 `$\sqrt[n]{n}$`）正确走 KaTeX。
- **删除所有非真题例题**：`EXAMPLE` 中删除全部「无来源自编例题」「幕布版自测」「题库（名校考研真题·厉以宁题库等）」条目；知识卡片例题区**仅保留真实考研真题**。
- 结果：数三保留 125 条真题（99 卡）、微观保留 140 条（122 卡）、统计保留 159 条（148 卡）；删除非真题 161 / 210 / 96 条；数三 110 卡、微观 133 卡因原本只有自编例题而**不再显示例题段**（这些是概念/公式卡）。
- 实现：用「R`…`/引号感知」的括号扫描定位 `const EXAMPLE = {…}` 整块，仅保留 `isReal(src)` 命中的真题条目，重新生成该块（值用 JSON 序列化，保留原始 LaTeX 字符串，功能与原 R`…` 等价）；先写临时副本校验再覆盖。
- 保留规则 `isReal(src)`：含 真题/光华/复旦/南开/中科大/中山/上财/北师大/上交/清华/人大/考研 且 非「幕布版自测」、非「题库」、非「名校考研真题」标签。
- 版本号：`VERSION='1.3.3'`；CHANGELOG 新增 v1.3.3 条目；sw.js ms3-v14。
- 验证：`node --check` 全 0；完整性（0 重复/0 坏引用、无「幕布版自测/题库」残留、条目 q/a 完整）；CDP（lim05/pd09/pb01 显示真题来源、lim01/pd01 无例题段、零报错）。

## 版本 v1.3.2（去自定校准 + 掌握度=可提取性 R，已完成）
- **删除自定的「期望保留率」校准**（`calibState/calibTarget/calibFactor/recordCalibration`），改用 FSRS 论文标准默认保留率 `FDR=0.9`——核心调度完全对齐 FSRS-4.5 论文定义。
- **掌握度改为「当前可提取性 R（预测回忆概率）」**：`mastery()` 复习阶段 = `R(t,S)=(1+F·t/S)^−0.5` 转百分比（此刻回想起来的概率）；学习/新卡为低值占位。趋势图「实际」记录复习时的 R、「预测」用 100×R(t,S) 衰减，二者同为可提取性、量纲一致可直接对比。
- **无自创算法**：本次改动全部对齐 FSRS-4.5 论文/训练定义（可提取性 R、期望保留率 0.9、官方 17 权重、幂律、均值回归、稳定性非线性更新），未引入任何未经论文验证的新机制。
- 版本号：`VERSION='1.3.2'`；CHANGELOG 新增 v1.3.2 条目；sw.js ms3-v13。
- 验证：`node --check` 0；CDP（页脚 1.3.2、毕业卡「目标保留 90%」、掌握度=R（复习后 100%）、`DB.log.calib` 不再出现、零报错）。
- **说明（工作计划自创标记）**：本版本无自创。后续若需新增功能（如「个人参数优化器」「学习步进调整」等），非论文/训练来源的改动会先向你请示并在 PROGRESS 中标注「⚠️自创」。

## 版本 v1.3.1（完整 FSRS-4.5 落地，已完成）
- **采用完整 FSRS-4.5（替换 1.3.0 的简化版）**：
  - 卡片按「**难度 D(1~10) + 稳定性 S(天)**」两参建模；幂律遗忘曲线 `R(t,S)=(1+F·t/S)^-0.5`，`F=19/81`，验证 `t=S` 时 `R=0.9`。
  - 复习间隔由**期望保留率**反推：`I = (S/F)·(R_req^(-2) − 1)`（R_req = 真实数据校准目标，默认 0.9）。
  - 采用 FSRS **官方 17 参数**默认权重 `FW`（fsrs-benchmark 训练得到）。
  - **初始**：`S0(G)=w[G-1]`（Again .40/Hard 1.18/Good 3.17/Easy 15.69）；`D0(G)=w4 − e^(w5·(G−1)) + 1`（Again 7.19/Good 5.28/Easy 3.22）。
  - **难度更新**（含均值回归锚点 D0(3)）：`D' = w7·D0(3) + (1−w7)·(D − w6·(G−3))`。
  - **稳定性更新**：成功 `S×(1 + e^(w8)·(11−D)·S^(−w9)·(e^(w10·(1−R))−1)·h·b)`（h=Hard 惩罚 w15、b=Easy 加成 w16）；遗忘 `S' = min(S, w11·D^(−w12)·((S+1)^w13 −1)·e^(w14·(1−R)))`（稳定性不上升）。
  - 关键：稳定性增长**与真实可提取性 R 挂钩**（间隔久仍记住→涨更多）、难度**均值回归**、遗忘**非线性降稳定性**——这三点是之前简化版缺失、也是 FSRS 科学性的核心。
- **掌握度与 FSRS 绑定**：`mastery()` 复习阶段改由 `稳定性 S` 平滑导出 `100·S/(S+3)`（学习阶段按步进、新卡 0），不再用任意 `s` 分数。
- **学习阶段保留时间步进**（1min/10min），毕业时用 FSRS 初始化 D/S；重学毕业沿用被遗忘更新过的 D/S。
- UI：毕业卡记忆框显示「难度 D / 稳定性 S 天 / 目标保留 R%」，预测曲线用 FSRS 幂律。
- 版本号：`VERSION='1.3.1'`；CHANGELOG 新增 v1.3.1 条目；sw.js ms3-v12。
- 验证：`node --check` 全 0；Node 校验 FSRS 数值（Good 首间隔 3.17 天 / Easy 15.69 天，t=S 时 R=0.9）；CDP（良好→第2步学习、简单→毕业「间隔16天·难度3.2·稳定性15.7·目标保留90%」+预测(FSRS)、四档、零报错）。

## 版本 v1.3.0（整合 1.2.7 后全部改动 + 初步 FSRS，已完成）
- **整合 1.2.7 之后的全部迭代**（四档评分、时间步进、掌握度与算法绑定、真实数据校准、记忆可视化双曲线、清空统计、评分 UI 等）——见以下各条目：
  - **四档评分**：恢复 忘记/困难/良好/简单（Again/Hard/Good/Easy），`flex` 单行等宽对齐卡片。
  - **学习阶段时间步进**：`STEP_MS=[1min,10min]`，答 Good 到最后一档毕业、Easy 直接毕业；学习卡按计时器到点重现（`surfaceDue`）。
  - **掌握度与算法绑定**：`mastery()` = `0.6×近期回答质量 + 0.4×当前间隔稳定性`，平滑、与排期一致。
  - **真实数据校准**：到期复习时把评分映射为真实保留度，累加进 `DB.log.calib`，目标保留率 = `0.9+平均偏差`（钳 0.7~0.97），并影响间隔；清空进度同步清空统计。
  - **记忆可视化**：记忆框揭示后显示，含「下次复习时间 + 掌握度趋势（实际 + 预测 两条线，时间横轴，SVG 可浏览）」。
- **初步 FSRS**：
  - 卡片新增 `diff`(难度 1~10) / `stab`(记忆稳定性，天) 两参。
  - 幂律遗忘曲线 `R(t,S)=1/(1+t/S)`；复习间隔由「期望保留率(校准目标)」解出：`间隔 = S×(1/DR−1)`。
  - `fsrsD`（忘记+0.5/困难+0.2/良好−0.1/简单−0.35，钳 1~10）；`fsrsS`（成长倍率 0.2/1.0/1.6/2.4 × 难度因子）。
  - 学习毕业初始化 D/S（良好→stab 9、简单→stab 18），复习按 D/S 更新并出间隔。
  - UI：记忆框显示「难度 D · 稳定性 S 天 · 目标保留 R%」；预测曲线改用 FSRS 幂律（`预测(FSRS)`）。
  - 说明：这是「简化版 FSRS」（未做 19 参数极大似然训练），但已具备 D/S 分离 + 幂律遗忘 + 期望保留率→间隔 的核心机制。
- **笔记上移**：卡片页 `notes-box` 置于 `memory-box` 之上。
- 版本号：`VERSION='1.3.0'`；CHANGELOG 新增 v1.3.0 条目；sw.js ms3-v11。
- 验证：`node --check` 全 0；CDP（页脚 1.3.0、笔记在记忆之上、毕业卡记忆框显示「难度 4.7·稳定性 18·目标保留 90%」+ 预测(FSRS)，四档评分、零报错）。

## 版本 v1.2.7（记忆算法重构 + 每科软上限新卡 + 例题真题化，进行中）
- **Bug 修复 + 记忆算法重做（用户拍板：先改变再修补）**：
  - 旧算法矛盾：① 忘记不降 `ef`（易度），只重置 `reps=0`，导致被越排越远、反复忘；② 掌握度 `s` 与排期 `ivl/ef` 两套脱节；③ 学习/复习混为一层；④ `rest` 把全部已学卡一遍遍塞回，和 SM-2 排期天然冲突 → 就是「再来一轮重复」「可无限学」的温床。
  - 新模型（两阶段）：`new → learning → review`。
    - **忘记**（任意状态）：回退到 `learning`，`ef -= 0.2`（最低 1.3），会话内**插回队列 5 张后**重现，直到答对（非忘记）才「毕业」进入 `review`。
    - **新卡/学习卡**：非忘记即毕业（`grad=1`，`ivl=1/2 天`）→ 进入 PM-2 复习。
    - **复习卡**：SM-2 间隔（`ef` 随回答质量升降，`ivl` 封顶 180 天）；忘记 → 回退 `learning`。
  - 每日队列 = **到期复习 + 续学（learning）卡 + 本批新卡**；删除了 `rest`/`paid`/每日配额 + 跨学科全局记账（那套在 1.2.6 档试了全局化，又导致「一科用满另科无卡」，故整体废弃）。
  - **三档评分（用户建议，合并良好/困难 → 模糊）**：`忘记(0)` / `模糊(1)` / `简单(2)`。学习阶段只有答「简单」才毕业；「忘记」隔 4 张重现、「模糊」隔 8 张重现（都未毕业，计入「未学完新卡」）；复习卡答「模糊」→ 间隔减半、易度略降，答「简单」→ 拉长间隔。
  - **四档评分 + 时间步进（对齐主流 Anki）**：恢复 忘记/困难/良好/简单（Again/Hard/Good/Easy）；学习阶段改**时间步进** `[1分钟, 10分钟]`（`STEP_MS`/`LAST_STEP`），答 Good 到最后一档后毕业(1天)，Easy 直接毕业(4天)；复习卡 Again→回学习(1分钟步)、Hard 间隔×1.2、Good 间隔×ef、Easy ×ef×1.3；`step` 字段 + 记忆框显示「学习中 第N步 · 约X分钟后重现」。
  - **记忆算法可视化（v3）**：记忆框**揭示答案后才显示**；不再展示 易度/已答对/遗忘；改为「下次复习时间 + 掌握度趋势图」，趋势图中含**实际曲线（历史）**与**预测曲线**（按校准目标保留率衰减到下次复习）两条线，时间横轴、SVG 可横向滚动浏览、点 hover 显示值。独立「遗忘曲线」模块已删除。
  - **真实数据校准**：到期复习时，把用户评分映射为「真实保留度」（忘记 0.2 / 模糊 0.6 / 简单 0.9），与基线 0.9 的偏差累计进 `DB.log.calib`；样本≥5 后，目标保留率 = 0.9 + 平均偏差（钳制 0.7~0.97），并作为 `calibFactor` 影响复习「简单」间隔的缩放（遗忘偏多 → 目标低 → 间隔短），预测曲线也用该校准目标绘制，越用越准。
  - **掌握度与算法绑定**：`mastery()` 由「近期回答质量(0.6)+当前间隔稳定性(0.4)」平滑导出，学习阶段压低，与排期一致、跳动小。
  - **评分 UI 对齐**：忘记/模糊/简单 三档改为 `display:flex` 单行等宽，与学习卡片对齐（不再是 2+1 格子）。
  - **统计趋势图日期坐标**：总体平均掌握度折线的横坐标现在标注具体日期（首/中/尾）。
  - **步进自适应**：保留卡片数步进（忘记隔 4 张、模糊隔 8 张），但若学习卡重插后超过 90s 仍未轮到，则自动拉近到队首附近提前重现。
  - **清空学习进度同时清空统计**：`resetall` 现在会把 `DB.log` 全部清空（今天已学习/待复习/未学完新卡/统计条归零），便于测试。
  - **每科软上限新卡（用户方案）**：每批最多引入 **10 张**（`NEW_WAVE=10`，按学科记 `DB.log.newIntro.ids`）；学习完本批后完成页出现「再来 10 张新卡」按钮，点击再引入下一批（软约束，无每日硬限制、跨学科互不影响）。
  - **UI**：删除学习卡右上角「剩余 x 张」（顶栏改为 分类徽章 + 卡片标题）；统计条增加「待复习 N 张」「未学完新卡 M 张」（`未学完新卡` = 处于 `learning` 且未毕业 `grad==0` 的卡，即曾经点过「忘记」的新卡）；设置页移除「每日新卡数量」行。
- **内容拓展（长期）：例题真题化**：识图管线验证可用（PyMuPDF 渲染原 PDF → `modlens_read_image` 转录 LaTeX；注：子代理无法稳定调用 modlens，主模型可直接读图）。3 个子代理把自编例题替换为真实真题（数三 15 张/16 道、统计 15 张、微观 20 张，含 1.2.5 新卡优先真题化：wr02/wr03/xr01/xr02 等）；1.2.5 少数卡（wr01 方向导数、xr01 替代弹性、xr03 拍卖）因语料无对应真实真题而宁缺毋滥跳过。严禁编造，找不到可靠题跳过。
- 验证：`node --check` 全 0；CDP 回归（波次新卡 10 张、忘记→learning+未学完新卡 1、毕业→review、完成页「再来 10 张」、点击后新卡出现、跨学科各自成波、浏览/自测正常、零报错）。
- 版本号：`VERSION='1.2.7'`（跳过了 1.2.6）；CHANGELOG 新增 v1.2.7 条目；sw.js ms3-v10。

## 版本 v1.2.5（再来一轮不重复 + 倒计时跳学习 + 今日去重统计 + 内容拓展首批，已完成）
- **Bug 修复：完成每日目标后「再来一轮/再一组」重复展示上一组卡片**：
  - 根因：`buildSession` 之前用 `rest` 把「所有非到期的已学卡」再塞回队列——刚评分、被 SM-2 排到未来日期的卡立即重演。
  - 修复：删除 `rest`，`deck = due(到期复习) + paid(今日额度内已引入未评分新卡) + quota 新卡`；已学未到期卡按排期到期后才出现，不再重复。点击「再来一轮」后若无待复习/新卡则进入空队列完成页（提示「今日新卡额度已用完…」）。
- **新增：点击左上角考研倒计时徽章 → 跳转学习界面**（`#countdownBadge` 加 `data-action="nav" data-arg="learn"` + role/tabindex + 指针手型/悬停效果）。
- **修正：「今天已学习 X 张」按去重卡片统计**：`todayReviewed()` 改读 `DB.log.detail[今日]` 的合法卡 id 去重数（而非 `log.daily` 每次评分累加）；热力图仍用 `log.daily` 看复习强度。实测：20 次评分 → 显示「今天已学习 10 张」。
- **内容拓展（长期目标 #4，本轮第一批，3 个子代理并行完成）**：
  - 数三 `math3.js`：+3 卡 `wr01` 方向导数与梯度 / `wr02` 可降阶高阶微分方程 / `wr03` 反常积分敛散性判别套路（206→209）；细化 5 张 `<tay07/tay08/lim14/la18/ineq01>`（补适用条件+易混辨析）；新增 8 条 PITFALL（29→37）。
  - 微观 `econ.js`：+3 卡 `xr01` 替代弹性 / `xr02` 委托代理与激励相容 / `xr03` 拍卖投标策略套路（252→255）；细化 5 张 `<pd18/it08/mk24/un12/fl15>`；新增 8 条 PITFALL。
  - 统计 `stats.js`：+3 卡 `xr01` 指数分布无记忆性 / `xr02` 似然比检验 / `xr03` 区间估计与假设检验公式选型套路（145→148）；细化 4 张 `<pb01/rv03/sm08/test04>`；新增 7 条 PITFALL（19→26）。
  - 说明：这批新卡例题多为自编（`真题\提取文本\` OCR 噪声高、无法可靠锚定对应真题，未附 `src`，无编造）；后续批次（长期）继续完善例题真题、继续细化与扩充。
- 版本号：`app.js` 顶部 `VERSION='1.2.5'`；CHANGELOG 新增 v1.2.5 条目；sw.js 缓存版本 ms3-v9。
- 验证：`node --check` 全 0；headless Chrome + CDP（全 r=0 20 次评分 → 去重 10 张、完成后点再来一轮 → 空队列无重复、倒计时点击跳学习、零报错）。

## 版本 v1.2.4（移动端顶栏抽屉化 + 再来一轮修复，已完成）
- **移动端顶栏瘦身（方案 C · ☰ 抽屉菜单，用户选定）**：
  - `index.html`：header nav 增加 `#menuToggle`（☰）；新增左侧滑出抽屉 `#drawer`（7 个导航条目：📚学习/🔍浏览/🗺️导图/✏️自测/📈统计/🧠原理/⚙️设置，含 `data-action="nav"`）+ 半透明遮罩 `#drawerBackdrop`（`data-action="menuclose"`）。
  - `style.css`：抽屉样式（固定定位、translateX(-105%) 收起、`.open` 滑入、z-index 250/遮罩 240）；`@media (max-width:520px)` 顶栏单行：隐藏 7 个 `.nav-btn`、显示 ☰、学科下拉/倒计时徽标/掌握度紧凑化、`header nav` 不换行；`@media (min-width:521px)` 桌面隐藏 ☰ 与抽屉。
  - `app.js`：`openDrawer/closeDrawer()` + `menuToggle` 点击切换 + `handleAction` 新增 `case 'menuclose'` + `nav` 时自动关抽屉 + Esc 同时关抽屉；`updateNavBadge` 改为 `querySelectorAll`（顶栏与抽屉的「学习」项都显示待复习角标）；抽屉打开时触屏滑动不触发翻页。
  - 按用户选择：**倒计时徽标与学科掌握度（图标+%）都保留**在顶栏（Athena 字标此前已在 1.2.3 移除）。
- **Bug 修复：完成今日任务后「再来一轮」无反应**：
  - 根因：`buildSession` 在每日新卡额度用尽且学科内无已学卡时重建出**空 deck**，完成页的「再来一轮」每次点击都重建空 deck → 界面无任何变化（会话中断/进度丢失等场景触发）。
  - 修复：`markNewIntroduced` 改为记录今日已引入新卡的 **id 数组**（`DB.log.newToday[t].ids`）；`buildSession` 把「今日额度内已引入但尚未评分（仍为 new）」的卡作为 `paid` 重新纳入队列（不再额外占额度，总额度不超上限）；完成页 `deck.length===0` 时**不再显示**无效的「再来一轮」按钮，并给出诚实提示（「今日新卡额度已用完，还有 N 张新知识点明日开放」/「全部知识点已纳入学习计划」）。
  - 旧数据兼容：无 ids 的存量 `newToday` 记录按无 paid 处理（不崩、无死按钮）。
- 版本号：`app.js` 顶部 `VERSION='1.2.4'`；CHANGELOG 新增 v1.2.4 条目（2 条）；sw.js 缓存版本 ms3-v9。
- 验证：`node --check` 全 0；headless Chrome + CDP（页脚 v1.2.4；注入「额度用尽+有ids」→ 重新提供 10 张卡、完成后再来一轮正常；注入「额度用尽+无ids」→ 完成页无死按钮、提示正确；375px 移动端：顶栏 7 导航全隐藏、☰ 显示、徽标与掌握度保留、抽屉开/关/导航/遮罩关闭全正常；零报错）。

## 版本 v1.2.3（倒计时徽标移位 + 真题例题补全，已完成）
- **倒计时弹窗文案**：删除「把考点拆成卡片，抽一张、背一张、对一张。」提示句（该句仅剩 index.html meta description 里的品牌 tagline，弹窗不再显示）；≤30 天冲刺提示语保留。
- **倒计时徽标移至界面左上角**：`index.html` 头部 `.brand` 内用 `#countdownBadge` 替代原 `A`thena 字标（`.brand-wordmark` 删除，对应 CSS 规则清理，`.brand` 改 `align-items: center`）；`#brandText`（学科图标 + 平均掌握度）仍紧随其后。`updateCountdown()` 无需改动。
- **更新日志去「背多分」**：CHANGELOG v1.2.1 条目中「美术全面换用『背多分』品牌视觉规范」改为「美术全面换用新品牌视觉规范」（此项按用户要求**不写入** v1.2.3 更新日志）。
- **真题例题补全（3 个子代理并行，本轮共 155 张卡新增真题 src）**：
  - 数三 `math3.js`：65→**93**（+28 张新卡，另有 inf08/ext07/ode02 追加例题；共写入 41 道，均为 `'YYYY 年数三真题'`）。跳过约 115 张：基础公式卡（求导/积分/三角/展开式等真题不单独命题）、OCR 题干乱码不可靠（lim13-15/la02/ext02 等）、文本无对应可确认题目的卡。
  - 微观 `econ.js`：55→**105**（+50 张，全部 `'YYYY 北大光华 431 真题'`，2013-2026；同一真题不同小题合理复用于相关卡）。跳过 147 张：光华真题池有限（2013-2026 微观约 63 题，多数已被既有 EXAMPLE 引用），无对应知识点题。
  - 统计 `stats.js`：61→**138**（+77 张目标卡 + est05/est06 追加，共 79 道，来源含光华 431 / 复旦 432 / 中科大 432 / 南开 432 / 中山 432 / 上财 432 / 北师大 432 / 上海交大 432 / 考研数学三 等）。跳过 7 张：文本中无独立命题（sm09 分位数、est11 独立性、reg09 预测区间等）。
  - 原则：只加 `真题\提取文本\` 与数三 1987-2025 PDF 中真实存在的题，严禁编造；每张卡 q/a 只聚焦该卡知识点。
- 版本号：`app.js` 顶部 `VERSION='1.2.3'`；CHANGELOG 新增 v1.2.3 条目（2 条：徽标移位+弹窗精简、真题例题补全含数量）。
- 验证：`node --check` 全 0；数据完整性全绿（四科无重复 id、META/REL/DEPTH/PITFALL/MNEM/ORDER/EXAMPLE 引用全落地、政治 187 卡不变）；headless Chrome + CDP（页脚 v1.2.3、徽标贴左上角、弹窗无 tagline、更新日志 7 条且无「背多分」、当前版高亮 v1.2.3、零报错）。

## 版本 v1.2.1（Athena 品牌化 + 倒计时 + 每日新卡上限 + 助记 + 时政，已完成）
- **品牌 Athena**：manifest 更名「Athena · 考研知识点记忆」（short_name=Athena），index 标题/描述更新，顶栏加 `A`thena 字标（A 樱粉），页脚「Athena · 版本 v1.2.1」。
- **美术全面换用「背多分」制图规范**（`pics/背多分-品牌视觉规范-v1.html`）：
  - 色板：桃粉 #F4C0D1 / 樱粉 #D4537E / 雾蓝 #B5D4F4 / 晴蓝 #378ADD / 墨灰 #2C2C2A（style.css :root 与 dark 主题全换；主按钮/重点=樱粉、正确/图表=晴蓝、错误=#E24B4A）。
  - 四学科头部渐变改为色板内取色（数三=晴蓝雾蓝、微观=桃粉樱粉、统计=雾蓝桃粉、政治=樱粉墨灰）。
  - 图标：按规范 200×200 网格重制（粉底圆角方块 + 一蓝一白错位抽认卡 + 右下樱粉对勾圆），渲染 `icons/icon-512.png / icon-192.png / apple-touch-icon.png(180) / favicon.png(48) / icon-512-maskable.png`（maskable 全出血+图形缩至安全区）。渲染法：SVG+HTML → headless Chrome `--screenshot`（须 `--user-data-dir`，每尺寸独立 profile，`--default-background-color=00000000` 透明底）。
  - 掌握度色阶/热力图/趋势线/导图节点（根=樱粉、选中=樱粉、环线=晴蓝、连线=#DCD9D0）全部换用色板；星级徽章改樱粉。
- **考研倒计时**：
  - 上边栏常驻徽章「📅 距考研 N 天」（≤30 天变红「urgent」）；每日首次打开弹窗（同一天不重复，localStorage `athena_countdown_seen`）显示天数+考试日期。
  - 考试日期：默认自动取「最近的 12 月 20 日」（初试在 12 月下旬，过期自动滚到次年）；设置页可手动改（`DB.settings.examDate`，留空=自动）。
- **A1 每日新卡上限**：`buildSession` 每天最多引入 `DB.settings.dailyNew`（默认 10，设置页可调 1~99）张新卡，超出顺延次日（`DB.log.newToday` 记账）；完成页提示剩余新卡明日开放。实测政治 195 张新卡队列仅 10 张。
- **B6 助记**：数据可选 `MNEM` 字段（`🗝️ 助记：` 行），学习卡/浏览展开/导图悬浮窗渲染（学习卡跟随显示答案）。政治 46 条、数三 9 条、统计 5 条。
- **B3 时政**：politics.js 新增 `shizheng` 分类「时政·当代世界经济与政治」15 张卡（sz01–sz15：全球化/多极化/百年大变局/全球治理/大国关系/五项原则/一带一路/三大倡议/中国式现代化世界意义/二十届三中全会/2024 中央经济工作会议/2025 政府工作报告等），政治累计 195 卡 / 8 分类。
- 导出/导入保留 `settings.examDate`；sw.js 缓存列表加新图标（ms3-v6）。
- 验证：`node --check` 全 0；数据完整性（195 卡无重复、META/REL/DEPTH/PITFALL/MNEM 引用全落地）；headless Chrome + CDP：Athena 页脚、倒计时徽章 122 天、弹窗出现且同日不重复、新卡上限 10、设置改 15 生效、时政分类、助记渲染（政治/数三）、导图、零报错。
- 版本号：`app.js` 顶部 `VERSION='1.2.1'`。

## 版本 v1.2.0（政治模块，已完成）
- 新增第 4 个学科 `data/politics.js`（id=politics，name=思想政治理论，short=政治，icon=🏛️，`kind: 'qa'`）。
- 内容：180 张背诵知识点卡 + 7 个分类：
  - `马原·哲学` 52 张（mz01–mz52：唯物论/辩证法/认识论/唯物史观）
  - `马原·政经` 18 张（mg01–mg18）、`马原·科社` 5 张（ms01–ms05）
  - `毛中特` 38 张（mt01–mt38：毛思想/邓三科/习思想含新质生产力、中国式现代化）
  - `史纲` 36 张（hs01–hs36）、`思修·法治` 23 张（mo01–mo23）
  - `分析题套路` 8 张（tl01–tl08：马原/辩证法/认识论/唯物史观/毛中特/史纲/思修套路 + 万能三段式）
- 无例题无真题无时政（按要求）；PITFALL 20 条、REL 38 组、DEPTH/META 全覆盖（题型标「选择题/分析题/选择+分析/分析题方法」）。
- UI 适配（app.js `subjKind()`，kind='formula' 默认不动）：
  - 搜索框「搜索知识点（名称/内容）…」、空结果「没有匹配的知识点。」
  - 自测文案「给出内容，选择它的名称…」+「这个知识点叫什么？」
  - 学习/浏览/导图悬浮窗「📌 考查方式：」替代「常考题型」
  - 政治导出文件名「政治知识点记忆-备份.json」
- 主题：`body[data-subject="politics"]` 红色渐变（style.css）；index.html 加 `<script src="data/politics.js">`；sw.js 缓存列表 + 版本 bump ms3-v5；manifest 名称泛化为「考研知识点记忆」。
- 验证：`node --check` 全 0；数据完整性校验通过（180 卡、无重复 id、META/REL/DEPTH/PITFALL 引用全落地）；headless Chrome + CDP 实测：4 学科切换、政治学习/浏览(180)/导图(7 分类+60 胶囊)/自测/统计(7 分类条)全正常、数三旧文案不变、无报错。
- 版本号：`app.js` 顶部 `VERSION='1.2.0'`。

## 真题 PDF 阅读能力（重要）
- `真题/` 文件夹有 4 个 PDF（JYSG真题册 241 页、光华微观统计 36 页、合集统计 28 页、解析册 708 页）。
- 用 **Python + PyMuPDF(pymupdf)** 可完美提取中文（`pypdf` 对其中 3 个会乱码，PyMuPDF 不会）。已装 `pypdf`、`pymupdf`、`cryptography`。
- 已提取为 UTF-8 文本到 `真题/提取文本/*.txt`（JYSG 431 微观+统计在 5–29 页，432 统计在 30 页后；光华 2013–2017 混合微观+统计）。
- 提取脚本一次性命令：`py -c "import pymupdf,sys,io,os; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8',errors='replace'); [open(os.path.join(r'真题\提取文本',os.path.splitext(f)[0]+'.txt'),'w',encoding='utf-8').write(f'# {f}\n\n'+'\n\n'.join(f'===== 第 {i+1} 页 =====\n'+pymupdf.open(os.path.join(r'真题',f))[i].get_text().strip() for i in range(pymupdf.open(os.path.join(r'真题',f)).page_count))) for f in os.listdir('真题') if f.endswith('.pdf')]"`（可后续复用）。

## 任务清单与状态
- ✅ #1 连续学习
- ✅ #3 图标缓存
- ✅ #2 思维导图（三轮迭代：铺满/移动端拖动、知识点全名胶囊、字体、分类按平均掌握度染色、连线从气泡外缘出发；第三轮加曲线连线、悬浮详情窗、平滑接触、标题 LaTeX 渲染）
- 🔄 #5 例题重写 + #6 真题取材（进行中，范式已确立，见下）
- ⏳ #4 广度拓展（北大最高要求，待 #5 一轮完成后：对照 `考研数学三高等数学思维导图（幕布版）.md` 与北大考点分批新增知识点）

## #5/#6 例题重写范式（已确立）
- 把「直接套公式」的翻版式例题，改写为「真题来源的综合大题」。
- 顺序：**数三 → 统计 → 微观**；优先替换「直接套公式」类。
- 已完成样板（3 处）：
  - 数三·罗尔定理 `mvt01` → 2000 数三真题（连续 + 定积分为零 → 至少两个零点）
  - 数三·牛-莱公式 `int23` → 变限积分 + 求导 + 洛必达综合极限
  - 微观·预算线 `cu05` → 光华真题「货币+粮票双约束」消费集
- 例题数据位置：各数据文件内 `const EXAMPLE = { ... }`（math3.js 约 751 行起，econ.js 约 985 行起，stats.js 约 553 行起）。

## 关键修复记录（重要，勿回退）
### 思维导图气泡丢失 bug（已修，2024 轮）
- 现象：导图只剩文字+乱连线、气泡消失、点击无交互、无法滚动。
- 根因：`app.js` 里 `renderMap()` 调用 `mapPill(..., '18'/'15'/'13')` 时 `fontSize` 传的是**字符串**，而 `textWidth()` 中 `w += fontSize` 对中文分支触发**字符串拼接**（"18"→"1818"→…），气泡宽度爆炸（千万级），被推到画面外。
- 修复：`textWidth()` 内 `fontSize = +fontSize || 0`；`mapPill()` 内 `fontSize = parseFloat(fontSize) || 13`。
- 验证（headless Chrome + CDP）：气泡宽高恢复正常（根 126×36、卡片 91×26），无巨宽 rect，垂直滚动可用，点击知识点可弹出详情面板。

## 下一步
- 待办：2020 年数三高数题多数未可靠重构；数三概率统计题未标注；掌握度趋势需积累历史数据（已埋点，几天后自动显示）。

## 版本 v1.1.1（bug 修复）
- 导图悬浮窗补「知识点本身内容」：新增 提示(front) / 答案(back) 渲染（`buildMapPanel` 加 `.map-card-content`）。
- 常见陷阱修复：卡片陷阱框取消 `hidden`（常显），卡片与悬浮窗的陷阱文本改用 `texEl`（`renderTex`）渲染，公式正常显示。
- 回归：headless 实测 modalFront/modalBack=true、陷阱 KaTeX 渲染、版本 v1.1.1、无报错。

## 版本 v1.1.0（已完成）
- 导图缩放：`mapScale` + 工具栏 −/＋/⟲ 按钮 + Ctrl+滚轮缩放（0.4~2.5 倍）。
- 学习统计视图（新增「统计」导航）：学习日历热力图（近 16 周，`DB.log.daily`）、平均掌握度趋势折线（`DB.log.mastery` 每日快照，`snapshotMastery` 埋点）、各分类掌握度进度条。
- 模板卡真题化：数三 tp01–tp07、微观 te01–te06、统计 ts01–ts06 各配 1 道真题（原自编例题保留，EXAMPLE 改为数组双题）。
- 回归：headless 实测 版本 v1.1.0 / 热力图 112 格 / 分类条 14 / 缩放 scale(1.2) / 无报错 全部通过。

## 版本与内容增强（已完成）
- 版本号：`app.js` 顶部 `VERSION='1.0.0'`（语义化），页面底部显示「版本 v1.0.0」。
- 笔记隐藏：学习卡未显示答案时「📝 我的笔记」隐藏，点「显示答案」才出现（浏览卡笔记常显）。
- 待复习角标：「学习」导航按钮显示待复习数（红点）。
- 记忆算法：`interleave`（同知识点相邻错开）+ 重要性加权（★高优先、高星遗忘后 5 分钟重现，其余 10 分钟）。
- 沉浸模式：⛶ 按钮 / Esc / ✕ 退出，隐藏顶栏与统计条。
- 易错点：新增 `PITFALL` 对象（数三 29、微观 25、统计 19 条），渲染「⚠️常见陷阱」。
- 一题多解：`EXAMPLE` 支持 `a2`（巧解），渲染「💡巧解」（数三 13、微观 11、统计 10 条）。
- 解题模板卡：数三 tp01–tp07、微观 te01–te06、统计 ts01–ts06（共 19 张方法论卡）。
- 真题全覆盖映射：数三 +3 卡(ms01-03)+10 数组、微观 +6 卡(my01-06)+8替换+15数组、统计 +9 卡(sy01-09)。
- 回归：headless 实测 版本号/笔记隐藏/陷阱渲染/导图/无报错 全部通过。

## 视觉模型引擎（已接入，质量远超 OCR）
- 工具 `modlens_read_image` 已可用（用户部署了视觉引擎）。实测把数学公式转写为**准确 LaTeX**（矩阵/上下标/根号/分段函数均正确），远优于 PaddleOCR。
- 视觉重扫数三 2023/2024/2025 真题：新增 12 条 src 标注（inf08、ser05、la07、la09、int03、int16、tay03、multi01、la16、mvt04、la08、int19），答案多来自官方解析页。math3 累计 src **68 条**。
- 转写文本存 `真题/提取文本/数三/数三真题_YYYY_vision.txt`（2023/2024/2025）。
- 后续可复用：渲染 PDF 页→PNG（PyMuPDF 180DPI）→ `modlens_read_image` 转写 LaTeX。

## OCR 能力（已打通）
- OCR 工具：`D:\deepseek harness\ocr\Umi-OCR_Paddle_v2.1.5`，引擎 `UmiOCR-data\plugins\win7_x64_PaddleOCR-json\PaddleOCR-json.exe`（PaddleOCR 中文 ch_PP-OCRv3）。
- 用法：`PaddleOCR-json.exe -image_path=<ASCII路径.png>`（工作目录须为插件目录，否则找不到 models）；输出 JSON，`data[].text` 为识别文本。
- 流程：PyMuPDF 渲染 PDF 页→PNG（200DPI）→ PaddleOCR-json 识别 → 解析 JSON。
- 已 OCR 数三扫描件到 `真题/提取文本/数三/*_OCR.txt`（2020/2022–2025 真题+解析，79 页）。注意：**数学公式 OCR 乱码较重，中文叙述/题号/题型清晰**。
- 数三 OCR 补漏：新增 7 条 src 标注（dbi03=2025、lim16=2024、tay04=2024、int21=2023、multi02=2023、ser12=2023、la13=2022），math3 累计 src 56 条。

## PDF 真题拆解标注（最新一轮，已完成）
- 数三 `math3.js`：从 `真题/提取文本/数三/`（2010–2022 真题 + 2010–2017/2019/2021/2026 解析）拆解约 16 道高数/线代真题标注 src（累计 src 49 条）。
- 微观 `econ.js`：15 道光华 431 真题标注（累计 src 35 条）；补 5 张新卡 `nx01`~`nx05`（纵向差异化/质量竞争/差异化 Bertrand/min(x1,x2^α) 规模报酬/征税 Stackelberg）；质量对比替换 fl07、保持 cu05/un09/mk06/gm04/gm09。
- 统计 `stats.js`：18 道复旦/光华真题标注（累计 src 43 条）；补 4 张新卡 `sx01`~`sx04`（相关性检验/Beta 分布/充分统计量与 C-R/先验后验）；质量对比替换 test11、保持 est05/06、test13。
- 全部 `node --check` 退出码 0。

## 广度拓展（已完成）
- 数三 `math3.js` 新增 20 张卡 `ext01`~`ext20`（导数定义、洛必达、极限性质、零点/介值、单调极值、凹凸拐点、最值、边际弹性、换元积分、华里士、积分中值、瑕积分、可微判定、隐函数偏导、比较判别、绝对收敛、幂级数展开、年金现值、差分方程、微分方程经济应用），附 META/EXAMPLE（幕布版自测）/REL，未动 CATS/ORDER/return。

## 全科目回归（已完成）
- `node --check` 4 文件全 0；headless Chrome 全科目：无报错、笔记框存在、三学科导图首分类符合章节顺序、气泡/连线/弹窗正常、src 字段落地（如 cu05=2013 北大光华 431）。

## 真题标注 + 关联 + 章节顺序（已完成）
- 真题 src 标注 + REL 完善（3 个子代理）：
  - 数三 `math3.js`：13 道真题 src + 20 条 REL
  - 微观 `econ.js`：15 道真题 src + 19 条 REL
  - 统计 `stats.js`：12 道真题 src + 35 条 REL
- 章节顺序 `ORDER` 数组已补进三个数据文件的 return 语句（数三按幕布版 6 章、微观按消费者→生产→市场→一般均衡→市场失灵、统计按概率→…→回归）。

## 最近一轮新增（UI/交互/算法）
- 修复移动端「公式横向滚动被误判为翻页」：`touchstart` 记录是否在可横向滚动元素内（`inScrollable`），是则不触发翻页。
- 卡片笔记：`defaultCard` 加 `notes` 字段；学习卡与浏览卡新增「📝 我的笔记」textarea，输入防抖 400ms 存 DB。
- 章节顺序：`catOrder()` 优先读 `subj.ORDER` 数组，否则回退 `Object.keys(CATS)`（ORDER 数组待子代理完成后补进 data 文件）。
- 回忆算法兜底：新增 `surfaceDue()`，每次渲染学习视图时把「已到遗忘临界点（due<=now）」的待复习卡移到队首，确保推送到学习主页。
- 真题来源：`EXAMPLE` 支持可选 `src` 字段，学习/浏览/导图悬浮窗显示「📝 真题 + 📚 来源：YYYY年…」。
- 学科上边栏主题：`setSubject` 设 `body[data-subject]` 与 `header[data-subject-icon]`，CSS 按学科切换渐变 + 淡化学科图标。
- 导图气泡改纯 HTML 定位胶囊（连接线仍 SVG），修复 KaTeX 根号（`\sqrt`）在 foreignObject 中不显示被开方数的问题。

## 本轮（当前对话）进度
- 导图第三轮改造（app.js + style.css）：
  - 曲线连线：`mapEdge` 由 `<line>` 改为三次贝塞尔 `<path>`（水平切线出/入），避免穿过气泡。
  - 悬浮详情窗：点知识点气泡弹出居中模态窗（`.map-modal`），点遮罩或 × 关闭（新增 `mapclose` action）。
  - 平滑接触：连线从椭圆外缘出发 + 水平切线，端点圆滑。
  - 标题 LaTeX：气泡用 `<foreignObject>` + KaTeX 渲染（`mapPill` 改用 measureTitle 量宽）；浏览/自测/悬浮窗/关联 chip 标题改用 `texEl()`（走 `renderTex`）。
- 例题改写 + 标题 LaTeX 化：交由 3 个子代理并行完成：
  - 微观 `econ.js`：20 例题改写 + 15 标题（完成）。
  - 统计 `stats.js`：19 例题改写 + 9 标题（完成）。
  - 数三 `math3.js`：22 例题改写 + 95 标题 LaTeX 化（完成）。
- 前一轮已改数三例题 7 处：`lim01`、`dr09`、`int28`、`int29`、`multi03`、`ser10`、`la04`（详情见数据文件）。
- 真题参考源：[2002 数三真题及解析](https://www.renrendoc.com/paper/187455955.html)、[2023 数三真题及详解](https://www.sxydy.com/post/shijuan/011fd8fd7566e1207a896ac5a0e9594a.html)。
- 验证：`node --check` 全通过；headless Chrome + CDP 实测导图 path/foreignObject/模态窗/KaTeX 正常。

## 版本 v1.1.3（本轮）
- 为每一科目新增解题套路卡（方法论卡）：
  - 数三 `data/math3.js`：+3 张 `tp08`~`tp10`（二重积分计算套路 / 线性方程组求解套路 / 泰勒公式应用套路）。
  - 微观 `data/econ.js`：+3 张 `te07`~`te09`（斯勒茨基替代/收入效应分解 / 不确定性与保险决策 / 要素需求与工资地租决定）。
  - 统计 `data/stats.js`：+3 张 `ts07`~`ts09`（常用分布识别与计算 / 二维变量边缘·条件·独立 / 大数定律与中心极限定理应用）。
  - 每张卡均补 META / EXAMPLE（例题+解析）/ REL / DEPTH；`node --check` 4 文件全 0，数据完整性校验通过（无重复 id、REL/EXAMPLE/DEPTH 引用全部落地、模板卡元数据齐全）。
- 热力图可点开（`app.js` + `style.css`）：
  - 新增每日明细埋点 `DB.log.detail`：`markReviewed(id)` 记录当天每张卡的复习次数。
  - 统计视图热力图：有记录的格子可点击（`.clickable`），点击后下方展示「📅 日期 · 共复习 N 张 + 单卡明细列表（按次数排序，点击跳浏览）」，支持关闭/切换日期。
  - 导出/导入同步保留 `log.daily`/`log.mastery`/`log.detail`（之前导入只留 checkins，会丢统计）。
- 识图能力自检（迁移后复测）：
  - ✅ `modlens_read_image`（视觉引擎）可用：正确识别 2023 数三真题封面页（中文+版面），并把第二页数学题转写为 LaTeX（如 `D)$y_1^2+y_2^2-y_3^2$`）。
  - ⚠️ 整页数学转写被 `max_tokens` 截断（`finish_reason=length`）：需 `modlens config set openai.extraBody '{"max_tokens":4096}'`。
  - ❌ 当前模型 `deepseek-v4-pro` 自身无原生图像输入（`read_image` 报 does not declare image input），识图走 modlens 视觉桥。
- 版本号：`app.js` 顶部 `VERSION='1.1.3'`。
