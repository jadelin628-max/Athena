/*
 * C 语言 · 知识点记忆数据库
 * 依据：K&R《C 程序设计语言》+ C89/C99 标准通用框架（系统编程入门到进阶）
 * 结构与其他学科一致：id / cat / title / front / back + META / REL / PITFALL / MNEM
 * 说明：无例题学科；kind: 'qa' 驱动 UI 适配。
 */
window.SUBJECTS = window.SUBJECTS || {};
window.SUBJECTS.clang = (function () {
  'use strict';
  const R = String.raw;

  const CATS = {
arch: '架构与规则',
    basic: '基础与类型',
    flow: '控制流',
    fn: '函数与作用域',
    ptr: '指针与内存',
    arr: '数组与字符串',
    struct: '结构体与联合',
    build: '预处理与编译',
    lib: '标准库'
  };

  const F = (id, cat, title, front, back) => ({ id, cat, title, front, back });

  const DATA = [
    // ==================== 架构与规则 ====================
    F('cz01', 'arch', '内存布局：栈、堆、静态区与代码段',
      R`C 程序的内存是怎么分区的？`,
      R`四大区：① **代码段**——机器指令（只读）；② **静态/全局区**——全局变量与 static 变量（程序全程存活）；③ **栈**——局部变量与函数调用信息（自动分配回收，空间有限约几 MB）；④ **堆**——malloc 申请的区域（手动 free，空间大）。
规则：局部变量在栈上（函数返回即失效）；跨函数存活的要么 static 要么堆上。
指针错误十有八九是「栈上的地址被记住太久了」或「堆忘了还」。`),
    F('cz02', 'arch', '未定义行为（UB）概览',
      R`什么是未定义行为？为什么危险？`,
      R`**UB = 标准不规定行为**的代码：解引用空指针、数组越界、有符号溢出、除零、使用未初始化变量……
危险在于：编译器**可以假设 UB 不发生**来优化——结果不是「报个错」而是「任何行为都合法」：可能正常、可能崩、可能被优化得面目全非。
心法：C 的可靠性靠**不写 UB**——把 -Wall -Wextra 全开、用 sanitizer 工具检测，别靠测试兜底。`),
    F('cz03', 'arch', '类型限定符：short/long/signed/unsigned',
      R`基本类型的修饰符怎么组合？`,
      R`组合维度：**长度**（short < int < long < long long）× **符号**（signed 有符号——默认 / unsigned 无符号）。
要点：char 本身是否有符号**因平台而异**；unsigned 参与运算会把 signed 一方「拉下水」（比较陷阱）；溢出行为也不同——unsigned 回绕、signed 溢出是 UB。
size_t 是 unsigned 的别名——sizeof 返回它，循环比较时注意类型匹配。`),
    F('cz04', 'arch', '数组与指针的退化规则',
      R`数组传参后为什么 sizeof 变了？`,
      R`规则：数组在**表达式中**（除 sizeof 与取地址外）**退化为指向首元素的指针**——所以函数传参收到的只是指针，sizeof 拿到的是指针大小（8）不是数组大小。
推论：数组大小必须**额外传递**（void f(int arr[], size_t n) 的 n 就是干这个的）。
字符串字面量同理退化为指针——「数组名不是指针，但用起来像」是 C 最著名的规则。`),
    F('cz05', 'arch', 'extern 与多文件变量共享',
      R`多个源文件怎么共享一个全局变量？`,
      R`规则：**定义只有一处**（int counter = 0; 放 .c 文件），**声明可以多处**（extern int counter; 放头文件）——extern 的意思是「这名字在别处定义」。
包含 extern 声明的头文件被多个源文件 include，链接时都指向同一份定义。
反例：把定义写进头文件 → 多个翻译单元各有一份 → **重复定义链接错误**。函数天然是 extern 的——头文件里放函数声明同理。`),
    // ==================== 基础与类型 ====================
    F('ba01', 'basic', 'C 语言的定位与特征',
      R`C 语言是什么？它为什么长盛不衰？`,
      R`① **编译型**：源码经编译、汇编、链接成为机器码，效率极高；② **贴近硬件：** 直接操作内存与硬件寄存器（"高级汇编"）；③ **可移植性**：标准抽象了硬件差异——从单片机到超算无处不在（操作系统/嵌入式/数据库/解释器本身都是 C 写的）。
代价：没有内存安全网——指针与内存管理全靠程序员自律。`),
    F('ba02', 'basic', 'main 函数与程序结构',
      R`C 程序的入口是什么？写出最简程序与两种 main 签名。`,
      R`程序从 main 函数开始执行（每个程序有且仅有一个）：
int main(void) { return 0; }
或带命令行参数：int main(int argc, char 的二级指针 argv)——argc 是参数个数、argv 是参数字符串数组。
返回值 0 表示成功（可用 shell 的问号变量查看）。语句以分号结尾、块用花括号包裹、注释有单行与块两种。`),
    F('ba03', 'basic', '基本数据类型与 sizeof',
      R`C 的基本类型有哪些？大小由什么决定？sizeof 是什么？`,
      R`基本类型：char（字符，恒为 1 字节）、int、short、long、long long（整数族）、float、double（浮点族）、以及 unsigned 无符号变体。
**C 标准只规定最小范围与相对大小，不规定确切大小**——int 通常是 4 字节但由编译器与平台决定（"int 的自然长度 = 机器字长"）。
sizeof 是**编译期运算符**（非函数），返回类型或对象占用的字节数：sizeof(int)、sizeof 表达式。`),
    F('ba04', 'basic', '声明与定义的区别',
      R`什么是声明？什么是定义？为什么需要头文件里的声明？`,
      R`**定义**：分配存储并（可能）初始化——int a = 5; 每个符号全局只能定义一次；**声明**：告知编译器"这个名字存在、类型如此"——extern int a; 可出现多次。
使用其他文件（翻译单元）中的函数/变量时：头文件里放**声明（原型）**，源文件里放**定义**，链接器负责拼合。
易混：函数原型分号结尾是声明，带函数体就是定义。`),
    F('ba05', 'basic', '常量：const 与预处理宏',
      R`定义常量的两种方式是什么？各自的特点？`,
      R`① **const 关键字**：const double PI = 3.14159; —— 有类型、有作用域、可调试（编译器层面的只读变量）；② **宏定义**（预处理）：#define PI 3.14159 —— 预处理阶段**文本替换**、无类型无作用域、不占内存。
差异要点：宏在编译前展开（错误信息难读、可能多次求值副作用）；const 更安全，现代 C 优先 const，宏留给条件编译与"真常量表达式"。`),
    F('ba06', 'basic', '运算符要点与优先级陷阱',
      R`C 运算符中哪些最容易踩坑？`,
      R`① 赋值 = 与相等比较 == 形近（if (x = 5) 恒真——把 5 赋给 x）；② 逻辑短路：与、或运算符遇短路即停（用于空指针保护）；③ 自增自减的前置与后置差异（i 先用后加 / 先加后用）；④ 位运算：按位与或异或取反移位——嵌入式标志位操作的核心；⑤ 三目运算符 表达式一 ? 值二 : 值三。
优先级口诀：逻辑非 > 算术 > 关系 > 逻辑与或 > 赋值——**拿不准就加括号**。`),
    F('ba07', 'basic', '类型转换：隐式提升与强制转换',
      R`隐式转换的规则是什么？强制转换怎么写？风险在哪？`,
      R`隐式（自动）提升：小类型向大类型靠——int 与 double 运算时 int 提升为 double；char 在运算中先提升为 int。赋值时反向收窄会**截断**（double 给 int 丢小数）。
强制转换：（目标类型）表达式——如 (int)3.9 得 3（截断非四舍五入）。
风险：有符号与无符号混算（负数变成巨大正数——无符号比较的经典 bug）；截断大数丢失高位。`),
    F('ba08', 'basic', 'printf 与 scanf：格式符',
      R`怎么输出与读入？格式符有哪些？`,
      R`**printf**（"格式串", 参数们）：**%d** 整数、**%f** 浮点（%.2f 保留两位）、**%c** 单字符、**%s** 字符串、**%p** 指针；换行靠转义序列（反斜杠加字母 n）。
**scanf**（"%d", &x)：**必须传地址**（取地址符 &x）——忘写 & 是新手第一大崩溃源（运行时不是编译时！）。
格式符与参数类型不匹配是未定义行为——编译器只给 warning，别忽略。`),
    F('ba09', 'basic', '关键字总览：32 个保留字',
      R`C89 有哪些关键字？怎么分组记？`,
      R`C89 共 **32 个关键字**，按功能分组：
**类型**：int/char/float/double/void/short/long/signed/unsigned；**存储**：auto/static/extern/register；**控制流**：if/else/for/while/do/switch/case/default/break/continue/goto/return；**复合**：struct/union/enum/typedef；**其他**：const/volatile/sizeof。
记忆法：int/char/float/double/void 五大类型打头，其余按「控制/存储/复合」三堆——误用关键字命名编译器直接报错。`),
    F('ba10', 'basic', '逻辑运算与短路求值',
      R`逻辑运算符与「短路」是什么？`,
      R`**与（两个&）**、**或（两个竖线）**、**非（!）**——结果只有 0 和 1（真/假）。
**短路求值**：与的左边为假就**不算右边**；或的左边为真就**不算右边**——利用这一点可以安全地写「先判空再解引用」：if (p != NULL 且 p 的成员有效)。
惯例：C 里**非零即真**（-1 也是真）——判真假常直接写 if (p) 而不是 if (p != 0)。`),
    F('ba11', 'basic', '位运算入门',
      R`与或异或移位怎么用？`,
      R`**按位与 &**（清零/取位）、**按位或 |**（置位）、**异或 ^**（不同为 1——两次异或还原）、**取反 ~**、**左移右移 << >>**（左移一位 ≈ 乘 2，右移一位 ≈ 除 2）。
经典用法：flags & 0x04 判断某位是否为 1；flags |= 0x04 把某位置 1；x >> 1 快速除 2。
位运算是嵌入式与系统编程的日常——读别人代码时按「位图」理解，别当成算术。`),
    // ==================== 控制流 ====================
    F('fl01', 'flow', '条件分支：if 与 switch',
      R`if 与 switch 各自的适用场景？switch 的注意事项？`,
      R`if 适合区间判断与复杂条件；switch 适合**离散整型/字符值**的多路分发：
switch (x) { case 1: 语句; break; case 2: 语句; break; default: 语句; }
⚠️ **case 穿透（fall-through）**：漏写 break 会继续执行下一个 case——这是 C 著名的坑（也被有意用于多 case 共享逻辑）。case 必须是编译期常量。`),
    F('fl02', 'flow', '循环三件套：while、for、do-while',
      R`三种循环的差异与适用？`,
      R`while：先判断后执行（可能零次）；for：初始化、条件、步进一行写全——计数循环首选；**do-while：先执行后判断**（至少执行一次——菜单循环常用）。
三段皆可省略（for (;;) 即无限循环）。循环变量在循环外可见（C 无块级 for 变量，C99 起允许在 for 内声明）。`),
    F('fl03', 'flow', 'break、continue 与 goto',
      R`break 与 continue 的区别？goto 何时可用？`,
      R`**break**：跳出**本层**循环或 switch；**continue**：跳过本轮剩余语句、进入下一轮判断——多层嵌套时 break 只退一层（需要标志变量或 goto）。
goto：现代 C 中唯一的"正当用途"是**错误处理**——多层嵌套的统一清理出口（内核代码大量使用）；其余场景禁用。`),
    F('fl04', 'flow', 'C99 的 for 内声明与布尔类型',
      R`C99 带来了哪些便利语法？`,
      R`① for 内声明循环变量：for (int i = 0; ...)——变量作用域限于循环；② 布尔类型 stdbool.h：bool、true、false（此前用 int 与 0/1 模拟）；③ 变长数组 VLA（栈上可变长度，有争议）；④ 行注释 // 被标准化；⑤ 指定初始化器：结构体按成员名初始化。`),
    // ==================== 函数与作用域 ====================
    F('fn01', 'fn', '函数定义、原型与调用',
      R`函数原型声明为什么必须？放在哪里？`,
      R`C 是**单遍编译**——调用函数前编译器必须见过其签名，否则按默认假设生成代码（C89 下返回值与参数全被假设成 int，埋雷）。
原型：返回类型 函数名(参数类型列表); —— 放头文件中，所有调用者 include 它。
定义 = 原型 + 函数体。参数名在原型中可省略（int add(int, int);）。`),
    F('fn02', 'fn', '传值调用：C 只有传值',
      R`C 的参数传递机制是什么？如何实现"修改调用者的变量"？`,
      R`C **只有传值调用**——实参的值被复制给形参，函数内修改形参不影响调用者。
要修改调用者变量：传**它的地址**（指针），函数内通过解引用访问——swap 函数必须收两个指针。
数组作为参数例外：数组名退化为指针（传的是首地址），所以函数内能修改数组内容——但 sizeof 在函数内得到的是指针大小而非数组大小。`),
    F('fn03', 'fn', '递归',
      R`递归的要点与代价是什么？`,
      R`函数直接或间接调用自身：必须有**终止条件**、问题规模逐层收敛；每次调用在栈上分配新帧（局部变量与返回地址）——深度受栈大小限制（默认约一至八 MB，溢出即崩溃）。
经典：阶乘、斐波那契（朴素递归指数级重复计算——改记忆化或迭代）、汉诺塔、二分。
C 的价值场景：树遍历、分治算法。`),
    F('fn04', 'fn', '作用域与存储持续性',
      R`C 的四种作用域与存储类说明符是什么？`,
      R`作用域：**文件作用域**（全局，声明在所有函数外）、**块作用域**（函数/块内）、函数原型作用域、函数作用域（goto 标签）。
存储类：**auto**（默认局部，栈上）、**static**（局部变量延长生命周期到整个程序运行期且只初始化一次；全局变量限制在本文件可见）、**extern**（引用其他文件的全局）、**register**（建议放寄存器，现代编译器自行优化）。
static 的两义性（局部生命周期 / 内部链接）是高频考点。`),
    // ==================== 指针与内存 ====================
    F('pt01', 'ptr', '指针的概念：地址与解引用',
      R`什么是指针？如何取地址与解引用？`,
      R`指针是**存储内存地址**的变量：int 指针 p = 取地址运算符作用于 a（写为：与符号 + 变量名）；访问所指对象用**解引用运算符**（星号 + 指针名）——星号 p 即 a 本身。
声明：int 指针写法为 int 加星号 p——星号属于变量而非类型（int 加星号 a, b 中 b 是 int！经典声明陷阱）。
指针的大小恒定（32 位机 4 字节、64 位机 8 字节），与其指向的类型无关——类型决定的是**解引用时读多少字节与如何解释**。`),
    F('pt02', 'ptr', '指针与数组的关系',
      R`数组名在表达式中退化为什么？指针如何遍历数组？`,
      R`数组名在**大多数表达式中退化为指向首元素的指针**（例外：sizeof 与取地址）。因此：访问第 i 个元素等价于首地址加 i 再解引用——指针运算的单位是**元素大小**而非字节。
函数传参时数组退化为指针（见「函数与作用域」的传值调用卡）——所以 C 函数无法得知数组长度，必须额外传长度参数或约定结束标记。`),
    F('pt03', 'ptr', '指针运算',
      R`指针可以做什么运算？什么运算无意义？`,
      R`合法：指针 ± 整数（按元素大小步进）、同类型指针相减（得到元素个数差，类型是特殊的有符号整型）、指针关系比较（同数组内）。
无意义/非法：指针相加、不同类型指针相减、对 void 泛型指针做运算（需先转换）。
要点：指针运算越界后仅比较合法、解引用是**未定义行为**。`),
    F('pt04', 'ptr', '野指针、NULL 与悬空指针',
      R`区分 NULL、野指针与悬空指针。`,
      R`**NULL**：空指针常量——"不指向任何对象"的明确标记（现代 C 用 stddef.h 的 NULL）；解引用空指针即崩溃（段错误）。
**野指针**：未初始化的指针——值是随机垃圾，解引用行为不可预测（最危险的 bug）。
**悬空指针**：指向已释放/已失效内存的指针（free 之后、返回局部变量地址）——free 后立即置空是好习惯。
防御三件套：声明即初始化（不确定就置空）、使用前判空、free 后置空。`),
    F('pt05', 'ptr', '动态内存：malloc 与 free',
      R`动态内存四件套是什么？使用流程与规则？`,
      R`stdlib.h 四件套：**malloc**（分配未初始化内存）、**calloc**（分配并清零，两参数：个数 × 大小）、**realloc**（调整大小，可能搬移）、**free**（释放）。
流程：指针 p = (int 指针)malloc(数量 × sizeof(int)); 判空；使用；free(p); 置空。
规则：① malloc 与 free **成对**；② 不可重复释放（未定义行为）；③ free 后不可再访问；④ 内存泄漏 = 只 malloc 不 free（长期程序致命）。`),
    F('pt06', 'ptr', '内存泄漏与悬空：动态内存的两大病',
      R`什么是内存泄漏？什么是重复释放？如何排查？`,
      R`**内存泄漏**：malloc 的内存丢失了所有引用（指针被覆盖/函数返回未 free）——长期运行进程内存持续增长；**重复释放（double free）**：同一块内存 free 两次——堆元数据被破坏，未定义行为。
排查：Valgrind（Linux 内存检测神器）、AddressSanitizer（编译加编译选项，实时检测越界与泄漏）。
预防：谁分配谁释放的所有权约定、封装、RAII 思想（C++ 的答案）。`),
    F('pt07', 'ptr', '多级指针与指针数组',
      R`什么场景需要指针的指针？指针数组与数组指针有何区别？`,
      R`**指针的指针**（两级间接）：常见于① 函数内修改调用者的指针本身；② 指针数组（如 main 的参数表——字符串数组的数组名即两级指针）。
**指针数组**（元素是指针）与**数组指针**（指向整个数组的指针）声明写法不同——优先级辨析：前者是"数组 of 指针"，后者加括号才是"指针 to 数组"——声明阅读从标识符出发按优先级（括号 > 数组 > 指针）向外读。`),
    // ==================== 数组与字符串 ====================
    F('ar01', 'arr', '数组的声明、初始化与越界',
      R`数组如何初始化？越界的后果是什么？`,
      R`声明：int a[5] = {1, 2, 3}; —— 未给出的元素补零；全零写 int a[5] = {0};（或 = { };）大小可由初始化列表推断。
下标从 0 开始到长度减一；**C 不检查越界**——越界读写是未定义行为（可能悄悄破坏相邻变量、可能崩溃，取决于内存布局）——"C 信任程序员"的代价。
数组大小：sizeof(a) / sizeof(a[0])（仅在同一作用域内有效）。`),
    F('ar02', 'arr', '多维数组与数组指针',
      R`二维数组如何声明、传参、按行访问？`,
      R`int 矩阵[3][4] = {...}——内存中**按行连续存放**；访问 [行][列]。
传参必须告知列数：函数形参写 int m[][4] 或数组指针形式——编译器据此计算行内偏移。
可理解为"数组的数组"：矩阵[i] 是第 i 行（一个一维数组，退化为指针）。`),
    F('ar03', 'arr', 'C 字符串：字符数组与结尾零字符',
      R`C 语言如何表示字符串？结尾零字符为什么关键？`,
      R`C 没有**字符串类型**——字符串是**以空字符（数值零）结尾的字符数组**：char s[] = "hello"; 实际 6 字节（5 字符 + 结尾零）。
结尾零是所有字符串函数的终止依据——没有它，strlen/strcpy 会一直读到越界（缓冲区溢出攻击的经典根源）。
声明选择：char s[] = "hi"（可修改的数组副本）；const char 指针 s = "hi"（指向常量字面量——不可修改内容）。`),
    F('ar04', 'arr', '字符串处理函数族',
      R`string.h 的常用函数有哪些？注意什么？`,
      R`strlen（长度，不含结尾零）、strcpy（复制）、strncpy（定长复制——不足补零、超出不补结尾零，坑）、strcat（追加）、strcmp（比较：零为相等、负为小于、正为大于）、strchr/strstr（查找）。
安全版本：带 n 的变体（strncpy/strncat/strncmp）限制长度。
⚠️ strcpy/strcat 不检查目标空间——缓冲区溢出的头号来源，现代代码用 snprintf 或明确长度计算。`),
    F('ar05', 'arr', '缓冲区溢出：C 最著名的安全病',
      R`缓冲区溢出是怎么发生的？如何预防？`,
      R`机理：向固定大小缓冲区写入超出其容量的数据——覆盖相邻内存（可能覆盖返回地址 → 被利用为代码注入）。
经典：gets() 完全无法限制长度（已被标准废除）、scanf 的读入格式不加宽度、strcpy 长字符串。
预防：① 用 fgets 替代 gets（限制读入长度）；② scanf 加宽度限定；③ strncpy/snprintf；④ 编译开启栈保护与地址随机化；⑤ 代码审计所有"向数组写"的调用。`),
    // ==================== 结构体与联合 ====================
    F('st01', 'struct', '结构体：定义、声明与成员访问',
      R`如何定义与使用结构体？点运算符与箭头运算符的区别？`,
      R`struct Point { int x; int y; }; —— 定义类型（不占内存）；声明变量 struct Point p = {1, 2};
成员访问：变量用**点运算符**（p.x）；**指针**用**箭头运算符**（指针名 → 成员，等价于星号括号点——解引用后再取成员）。
结构体赋值是**逐成员复制**（浅拷贝——含指针成员时注意共享问题）。`),
    F('st02', 'struct', 'typedef：类型别名',
      R`typedef 是什么？结构体为什么常配合 typedef？`,
      R`typedef 为已有类型起别名：typedef unsigned long ulong; —— 之后可直接 ulong x;
结构体配合的原因：C 的 struct 变量声明必须带 struct 关键字（struct Point p）——typedef struct { ... } Point; 之后可省写 struct。
易混：typedef 是**别名不是新类型**（与 #define 不同，有作用域与类型检查）。`),
    F('st03', 'struct', '联合（union）：共享内存',
      R`联合与结构体的本质区别是什么？典型用途？`,
      R`union：所有成员**共享同一块内存**——大小 = 最大成员；同一时刻只有一个成员有效（写入新成员覆盖旧成员）。
struct 成员各占各的内存（大小 = 各成员之和，含对齐填充）。
用途：① 节省内存（互斥使用的变体）；② **类型双关**：同一内存按不同视角解读（如把 4 字节读成 int 或 4 个 char）——嵌入式寄存器与协议解析常用。`),
    F('st04', 'struct', '枚举（enum）',
      R`枚举是什么？默认值规则与用途？`,
      R`enum Color { RED, GREEN, BLUE }; —— 枚举常量默认从 **0 递增**（可显式指定：RED = 1, GREEN = 2，后续顺延）。
本质是 int 常量的命名集合——提升可读性、便于 switch 分发。
注意：C 的枚举变量并不严格限制取值（可以赋任意 int）——类型安全性弱于 C++/Java。`),
    F('st05', 'struct', '结构体内存对齐',
      R`为什么 sizeof(结构体) 常大于成员之和？对齐规则是什么？`,
      R`编译器在成员间插入**填充字节**，使每个成员的地址落在其"自然对齐边界"上（int 对齐到 4 的倍数）——CPU 访问对齐数据更快、某些平台不对齐直接崩溃。
规则：① 成员偏移是其对齐值的倍数；② 总大小是最大成员对齐值的倍数。
例：char + int 的结构体 = 1 + 3 填充 + 4 = 8 字节。
优化：成员按大小降序排列可减少填充；网络协议用打包属性禁用对齐。`),
    // ==================== 预处理与编译 ====================
    F('bu01', 'build', '编译的四个阶段',
      R`从源码到可执行文件经过哪四个阶段？`,
      R`① **预处理**：展开 #include/#define、处理条件编译——输出纯 C 源码（可用编译选项查看）；② **编译**：语法分析生成汇编；③ **汇编**：汇编代码变机器码（目标文件 .o）；④ **链接**：合并各目标文件与库，解析符号引用 → 可执行文件。
错误定位：预处理错误看宏、编译错误看语法类型、**链接错误（undefined reference）** 看声明与定义是否配对。`),
    F('bu02', 'build', '头文件与包含保护',
      R`头文件里该放什么？include 保护如何写？`,
      R`头文件放：函数**原型**、类型定义（结构体/枚举/typedef）、宏定义、extern 声明——**不放函数定义与全局变量定义**（多文件包含会重复定义，链接报错）。
**包含保护**：#ifndef UNIQUE_H / #define UNIQUE_H / ... / #endif——防止同一头文件被重复包含导致重复定义（或编译器支持的 #pragma once）。
约定：头文件名与保护宏同名。`),
    F('bu03', 'build', '宏定义：对象宏与函数宏',
      R`宏的两种形式与经典陷阱是什么？`,
      R`对象宏：#define MAX_SIZE 100 —— 文本替换。函数宏：#define SQUARE(x) ((x) 乘 (x)) —— 带参数的替换。
经典陷阱：① **参数未加括号**：SQUARE(a + b) 展开成 a + b 加星号 a + b——优先级全错；② **副作用重复求值**：SQUARE(i++) 使 i 加两次。所以宏体中每个参数与整体都要括号。
现代替代：const 常量替代对象宏、内联函数替代函数宏（有类型检查）。`),
    F('bu04', 'build', '条件编译',
      R`条件编译的常用形式与用途？`,
      R`#ifdef / #ifndef / #if / #else / #elif / #endif——预处理阶段按条件**保留或剔除代码**。
用途：① **包含保护**（#ifndef）；② 跨平台代码（按操作系统分支）；③ 调试开关（#ifdef DEBUG 输出日志）；④ 版本特性分支。
要点：条件编译的代码**不参与语法检查**（被剔除的分支写错了也不报错——上线前要各分支都编译一遍）。`),
    F('bu05', 'build', 'gcc 常用编译选项',
      R`gcc 的常用选项有哪些？开发时应该开什么？`,
      R`-o 输出名、-c 只编译不链接、-I 头文件目录、-L 与 -l 链接库目录与库名、-std=c99 指定标准。
**开发强烈建议**：-Wall -Wextra（打开全部警告——警告即潜在 bug）-g（调试信息，供 gdb 使用）-O2（优化，发布用）。
警告不是噪音：未初始化、类型截断、不可达代码——开警告是最低成本的 bug 预防。`),
    F('bu06', 'build', '链接：静态库与动态库',
      R`静态库与动态库的区别是什么？链接错误如何排查？`,
      R`**静态库（.a）**：链接时把库代码**复制进**可执行文件——体积大、自包含、库更新需重编；**动态库（.so/.dll）**：运行时加载——体积小、多程序共享、可独立升级（但环境缺库就跑不起来）。
链接错误：undefined reference = 声明了没定义（忘实现/忘链库/库名顺序）；multiple definition = 重复定义。
库搜索顺序与 -l 的顺序有关（被依赖的库放后面）。`),
    // ==================== 标准库 ====================
    F('li01', 'lib', 'stdio：printf 与 scanf 的格式化',
      R`printf 的格式说明符与 scanf 的注意事项？`,
      R`printf 常用：%d 整数、%f double（float 自动提升）、%c 字符、%s 字符串、%p 指针、%u 无符号、%x 十六进制、%% 百分号；宽度与精度：宽度.精度。
scanf：参数必须是**地址**（普通变量加取址符；数组名已是指针勿再加）——scanf 的读入格式不加宽度是溢出隐患；返回成功匹配的项数（判输入合法性）。
现代替代：fgets 读入 + sscanf 解析，安全得多。`),
    F('li02', 'lib', 'stdlib：动态内存与工具函数',
      R`stdlib.h 提供哪些核心功能？`,
      R`① 动态内存：malloc/calloc/realloc/free（见指针卡）；② 字符串转换：atoi/atof（无错误报告——推荐 strtol/strtod，带尾指针与错误检测）；③ 随机数：rand 与 srand（伪随机——rand 取模有偏差，用限制范围写法）；④ 程序控制：exit（终止，触发 atexit 注册的清理）、abort（立即异常终止）。
exit 与 return 的区别：exit 从任何深度终止、执行清理函数；return 只退出当前函数。`),
    F('li03', 'lib', 'string.h 与 math.h 速览',
      R`string.h 与 math.h 的常用函数？`,
      R`string.h：见字符串卡（strlen/strcpy/strcmp/strcat/strchr/memcpy/memmove/memset——mem 系列按字节操作、不关心结尾零，memset 清零与初始化常用；memmove 可处理重叠区域、memcpy 不行）。
math.h：pow、sqrt、fabs（浮点绝对值，abs 是整数版）、floor/ceil、sin/cos/tan；编译需链接数学库（gcc 加 -lm）。
三角函数用**弧度**——角度要先转换。`),
    F('li04', 'lib', 'gdb 调试入门',
      R`gdb 的基本调试流程与常用命令？`,
      R`编译加 -g 选项后：gdb 程序名 启动。核心命令：**break 行号/函数名**（断点）、run（运行）、next（逐过程）、step（逐语句，进入函数）、print 表达式（查看变量）、continue（继续到下个断点）、backtrace（查看调用栈——崩溃定位神器）。
段错误调试：backtrace 直接显示崩溃时的完整调用链。
替代/补充：printf 调试法（简单直接）、AddressSanitizer（内存类问题）。`),
  ];

  const META = {
    cz01: [4, '内存布局'],
    cz02: [4, '未定义行为'],
    cz03: [4, '类型限定符'],
    cz04: [4, '数组退化'],
    cz05: [3, 'extern'],

    ba08: [4, 'printf scanf'],
    ba09: [4, '关键字'],
    ba10: [4, '短路求值'],
    ba11: [3, '位运算'],

    ba01: [4, '语言特征'], ba02: [4, '程序结构'], ba03: [5, '类型与 sizeof'], ba04: [4, '声明与定义'], ba05: [4, 'const 与宏'], ba06: [4, '运算符陷阱'], ba07: [4, '类型转换'],
    fl01: [5, 'switch 穿透'], fl02: [3, '三种循环'], fl03: [3, 'break 与 goto'], fl04: [2, 'C99 特性'],
    fn01: [5, '函数原型'], fn02: [5, '传值调用'], fn03: [3, '递归'], fn04: [4, 'static 两义性'],
    pt01: [5, '指针基础'], pt02: [5, '指针与数组'], pt03: [3, '指针运算'], pt04: [5, '野指针悬空'], pt05: [5, '动态内存'], pt06: [4, '泄漏排查'], pt07: [3, '多级指针'],
    ar01: [4, '数组与越界'], ar02: [3, '多维数组'], ar03: [5, '结尾零字符'], ar04: [4, '字符串函数'], ar05: [5, '缓冲区溢出'],
    st01: [4, '结构体'], st02: [3, 'typedef'], st03: [4, '联合'], st04: [3, '枚举'], st05: [4, '内存对齐'],
    bu01: [4, '编译四阶段'], bu02: [5, '头文件与保护'], bu03: [4, '宏陷阱'], bu04: [3, '条件编译'], bu05: [3, 'gcc 选项'], bu06: [3, '静态与动态库'],
    li01: [4, '格式化 IO'], li02: [3, 'stdlib'], li03: [3, 'string 与 math'], li04: [3, 'gdb 调试']
  };

  const PITFALL = {
    ba03: R`int 的大小由平台决定——可移植代码用 stdint.h 的定宽类型。`,
    fl01: R`switch 漏写 break 就是穿透——最常见的 C 逻辑 bug 之一。`,
    pt04: R`声明指针即初始化（不确定就置空）——野指针是"随机崩溃"的总根源。`,
    ar03: R`结尾零字符必须留空间：长度为 N 的字符串需要 N+1 字节的数组。`,
    bu03: R`宏是文本替换：参数与整体都要加括号，副作用参数会被重复求值。`
  };

  // —— 帮助栏目文章（检索用，不参与学习调度；内容由原起步章迁移）——
  const HELP = [
    { id: 'bz01', title: 'C 语言适合做什么', body: R`**编译型静态语言**：代码先翻译成机器码再运行——极快、极贴近硬件。操作系统（Linux/Windows 内核）、嵌入式设备、数据库几乎都是 C 写的。
它是现代语言的祖师爷：C++、Java、Python 的语法都受它影响。学 C 的最大价值是**理解计算机真正在做什么**（内存、指针、字节）。
今天仍值得学：嵌入式与系统领域是主力语言；作为第一门语言能打下最扎实的底层直觉。` },
    { id: 'bz02', title: '安装编译器：gcc 与验证', body: R`C 源码必须经**编译器**翻译才能运行：Windows 装 **MinGW-w64**（或用 WSL），Mac 自带 clang（终端敲 cc 即可）。
验证：终端敲 gcc --version 出现版本号即成功。
编辑器用 VS Code 加 C/C++ 扩展——编译与报错提示都有。` },
    { id: 'bz03', title: '第一个程序：从源码到运行', body: R`① 新建 hello.c，写最经典的程序（include 头文件、main 函数、printf 输出、return 0）；② 终端敲 **gcc hello.c -o hello**（编译成可执行文件 hello）；③ 敲 **./hello**（Windows 是 hello.exe）运行。
四步走：写源码 → 编译 → 得到可执行文件 → 运行。
「改了代码必须重新编译」——与 Python 直接跑是本质区别，这是 C 的第一直觉。` },
    { id: 'bz04', title: '编译是什么：与解释型的分野', body: R`编译器把你写的源码**整体翻译成机器码**（CPU 能直接执行的指令）——翻译一次，以后每次运行都直接跑机器码，所以 C 极快。
对比 Python：解释器**边读边执行**源码，灵活但慢。
推论：**编译错误**（编译器拒绝翻译，如少了分号）与**运行时错误**（能编译，跑的时候崩了，如除零）是两类问题，处理方式不同。` },
    { id: 'bz05', title: '怎么读编译报错', body: R`报错格式：**文件:行号: error/warning: 说明**——行号直接定位问题。
关键习惯：**先修第一个错误**——后面的报错常常只是连锁反应（比如少了个花括号，后面全乱），第一个修完往往连锁消失。
warning（警告）不是 error，但初学阶段**把警告当错误修**——它们几乎都预示真 bug。` },
    { id: 'bz06', title: '注释与代码风格', body: R`注释两种：**//** 单行、**/* …… */** 多行（不可嵌套）。注释解释「为什么」，不逐行复述「做了什么」。
C 对缩进无强制（编译器不管），但风格必须统一：四空格缩进、花括号位置自选一种保持一致。
命名：变量小写加下划线（user_name）、常量大写（MAX_SIZE）——社区惯例。` },
    { id: 'bz07', title: '工具链总览：make 与 gdb', body: R`**编译器**：gcc（Linux/MinGW）、clang（Mac）——必装。
**make**：项目大了后自动执行「一串编译命令」的工具——先知道名字，多文件项目时再学。
**gdb / lldb**：调试器，可以单步执行、看变量——新手先会用 printf 打印调试，进阶再上调试器。
编辑器：VS Code 加 C/C++ 扩展足够入门全程。` },
    { id: 'bz08', title: 'C 学习路径建议', body: R`① 本卡组起步章 + 基础章打底；② 控制流与函数写熟后（能写百行小程序），进入 **指针章——C 的灵魂，也是最大关卡**，卡多过几遍、配合画内存图；③ 数组与字符串紧随指针（两者本质相连）；④ 预处理编译章在读多文件项目前过一遍。
节奏参考：每天 10 张卡 + 每周一个小程序（计算器、字符串反转）。
心态：C 的报错与段错误是常态——每次崩溃都是理解内存模型的机会。` }
  ];

  const BEGINNER = ['basic', 'flow'];
  return { BEGINNER: BEGINNER, id: 'clang', name: 'C 语言', short: 'C 语言', icon: '🅲', kind: 'qa', group: 'skill', CATS: CATS, DATA: DATA, META: META, HELP: HELP, REL: {}, PITFALL: PITFALL, MNEM: {}, ORDER: ['arch', 'basic', 'flow', 'fn', 'ptr', 'arr', 'struct', 'build', 'lib'] };
})();
