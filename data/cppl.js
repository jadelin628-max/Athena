/*
 * C++ · 知识点记忆数据库
 * 依据：C++ Primer + 现代 C++（C++11 起核心特性）框架（面向对象与泛型编程进阶）
 * 结构与其他学科一致：id / cat / title / front / back + META / PITFALL
 * 说明：无例题学科；kind: 'qa' 驱动 UI 适配。
 */
window.SUBJECTS = window.SUBJECTS || {};
window.SUBJECTS.cppl = (function () {
  'use strict';
  const R = String.raw;

  const CATS = {
arch: '架构与规则',
    basic: '基础与增强',
    oop: '面向对象',
    spec: '拷贝与移动',
    tmpl: '模板与泛型',
    stl: 'STL 标准库',
    mem: '内存与智能指针',
    modern: '现代 C++'
  };

  const F = (id, cat, title, front, back) => ({ id, cat, title, front, back });

  const DATA = [
    // ==================== 架构与规则 ====================
    F('ar01', 'arch', '翻译单元与 ODR 规则',
      R`什么是翻译单元？ODR 是什么？`,
      R`**翻译单元** = 一个 .cpp 文件 + 它 include 的所有头文件（预处理展开后）——编译器实际的工作单位。
**ODR（一次定义规则）**：任何实体在**整个程序中只能有一个定义**，但声明可以多个；inline 函数与模板是例外（各单元可各有一份，须完全相同）。
违反 ODR 的典型：在头文件里定义非 inline 的普通函数/全局变量——多个 cpp include 后链接冲突。`),
    F('ar02', 'arch', '前置声明与头文件最小化',
      R`什么时候用前置声明代替 include？`,
      R`**前置声明**：class Foo; ——只告诉编译器「有这么个类」，不引入完整定义。
可用场景：只需**指针/引用**（大小已知）——成员用 Foo* 、参数引用；**必须 include** 的场景：需要大小（值成员）、调用成员函数、继承它。
收益：头文件 include 越少，编译越快、耦合越低——「include 什么，暴露什么」。`),
    F('ar03', 'arch', '内存分区与对象存储期',
      R`C++ 对象住在内存哪里？`,
      R`四种存储期：① **自动**（局部对象——栈，作用域结束析构）；② **静态**（全局/static——程序全程）；③ **动态**（new 出来的——堆，delete 释放，现代代码交给智能指针）；④ **线程存储期**（thread_local——每线程一份）。
成员变量的位置跟着宿主对象走（对象在栈，成员就在栈）。
RAII 的全部魔法就是把「堆资源」绑到「栈对象」的析构上——mem 章的根基。`),
    F('ar04', 'arch', '值类别：左值与右值',
      R`左值、右值是什么？为什么移动语义关心它？`,
      R`**左值**：有名字、可取地址、表达式结束后还活着（变量、*p）；**右值（纯右值）**：临时的、即将消亡的（字面量、函数返回值、运算结果）。
意义：移动语义**只对右值安全**——从临时对象「偷」资源没有后果；spec 章的右值引用（两个&）就是给右值绑定的工具。
判断口诀：能对它取地址的是左值；"5"、a+b、"字符串字面量除外"这类临时是右值。`),
    F('ar05', 'arch', 'STL 组件地图',
      R`STL 由哪几块组成？`,
      R`六大组件：① **容器**——vector/list/map/set/unordered_map（存数据）；② **迭代器**——容器与算法之间的「泛化指针」；③ **算法**——sort/find/count/transform 等（操作数据，经迭代器）；④ **函数对象与 lambda**——算法的策略参数；⑤ **适配器**——stack/queue（改造容器接口）；⑥ **仿函数设施**——function/bind。
设计精髓：算法不直接碰容器——通过迭代器解耦，一套算法对所有容器生效（tmpl 章的泛型思想落地）。`),
    // ==================== 基础与增强 ====================
    F('ba01', 'basic', 'C++ 与 C 的关系',
      R`C++ 相对 C 增强了什么？两者如何选择？`,
      R`C++ = C 的超集基础（几乎完全兼容）+ 四大增强：① **面向对象**（类/继承/多态）；② **泛型编程**（模板与 STL）；③ **RAII 资源管理**（构造析构自动配对）；④ 更强的类型检查（引用、const 语义、new/delete）。
选择：系统底层、嵌入式、内核 → C；应用层、大型系统、需要抽象与库 → C++。
"零开销抽象"哲学：高级特性不付出运行时代价。`),
    F('ba02', 'basic', 'iostream 与命名空间',
      R`C++ 的输入输出与命名空间如何使用？`,
      R`输入输出流：std::cout 与插入运算符（双小于号）输出、std::cin 与抽取运算符输入——自动类型识别、可扩展（为自定义类型重载）、无需格式符（对比 printf）。
**命名空间**解决名字冲突：namespace 内定义、using 声明引入单个名字、using namespace 整体引入（头文件中禁用后者）。
std 是标准库的命名空间——所有标准符号都在其中。`),
    F('ba03', 'basic', '引用：C++ 的安全别名',
      R`引用与指针的本质区别是什么？`,
      R`引用 = 变量的**别名**：必须初始化、初始化后**不可改绑**、使用时无需解引用语法（像原变量一样用）、**没有空引用**。
对比指针：指针是独立对象（可改指向、可空、可算术运算）；引用更安全——参数传递首选引用（const 引用读、普通引用写）。
返回引用的函数：不可返回局部变量的引用（悬空）。`),
    F('ba04', 'basic', 'const 的完整语义',
      R`C++ 中 const 修饰指针的几种组合？const 成员函数是什么？`,
      R`① 指向常量的指针（const 在类型左侧）：值不可改、指针可改指向；② 常量指针（const 在星号后）：指针不可改指向、值可改；③ 两者皆 const。
**const 成员函数**：参数列表后加 const——承诺不修改成员变量（const 对象只能调 const 成员函数）。
constexpr：编译期常量/可在编译期求值的函数——比 const 更强的"编译期"承诺。`),
    F('ba05', 'basic', 'auto 与 decltype：类型推导',
      R`auto 如何推导类型？decltype 与之有何不同？`,
      R`**auto**：让编译器从初始化表达式推导变量类型——迭代器、长类型名、lambda 的救星；推导时**忽略引用与顶层 const**（要引用写 auto 与与符号）。
**decltype**：推导表达式的类型但不求值——保留引用与 const（用于声明与表达式同类型的变量，常见于模板返回类型）。
要点：auto 牺牲显式性换可读性——推导结果不确定时（如代理表达式）慎用。`),
    F('ba06', 'basic', '范围 for 与初始化列表',
      R`现代 C++ 的遍历写法是什么？统一初始化如何使用？`,
      R`范围 for：for (auto 元素 : 容器) —— 遍历所有元素（要修改元素用 auto 加与符号）。
**统一初始化**：花括号初始化——int x{5};、容器 vector v{1, 2, 3}、类成员、聚合体通吃；且**禁止窄化转换**（double 给 int 编译报错）——比圆括号初始化更安全。`),
    F('ba07', 'basic', 'cin 输入与流状态',
      R`怎么读入用户输入？`,
      R`**cin 右移读入**：cin >> x——自动按类型解析，跳过空白；连续读：cin >> a >> b。
失败状态：输入类型不符（如往 int 里输字母）→ cin 进入失败态，后续读取全部跳过——检查与恢复：if (!cin) 与 cin.clear() 加忽略错误行。
读一整行（含空格）：getline(cin, str)——与 cin >> 混用时注意吃掉残留换行。`),
    F('ba08', 'basic', '函数重载与默认参数',
      R`同名函数为什么可以并存？`,
      R`**重载**：同名函数按**参数类型/个数**区分——print(int)、print(double)、print(string) 共存；编译器按实参挑最匹配的。与返回值无关（只看参数）。
**默认参数**：void f(int x, int y = 10)——调用 f(5) 时 y 自动取 10；默认值只能**从右往左**连续给。
坑：重载加默认参数可能产生二义调用（f(5) 分不清 f(int) 还是 f(int, int=10) 的单参版本）——二选一即可。`),
    F('ba09', 'basic', 'bool 与比较运算',
      R`布尔类型与比较的细节？`,
      R`**bool**：true/false 两值；任何数值可转 bool（**零为假，非零为真**）；bool 占一字节。
比较运算（== != < > <= >=）结果就是 bool——可链在条件里直接用。
C++ 特色：真值上下文里指针也可作 bool（空指针为假）——if (ptr) 是「指针非空」的惯用缩写。
注意与整数的互转：cout 输出 bool 默认打 1/0，加 boolalpha 打 true/false。`),
    F('ba10', 'basic', 'static_cast 与显式转换',
      R`C++ 推荐的类型转换写法？`,
      R`**static_cast<目标>(值)**——现代 C++ 的显式转换：static_cast<int>(3.9) 得 3（截断）、static_cast<double>(a)/b（避免整数除法）。
为什么不用 C 风格 (int)x：static_cast 意图明确、**可检索**、禁止危险的转换（如无关指针互转）——错用会被编译器拦下。
其余三种（dynamic_cast/const_cast/reinterpret_cast）各有专项，见到再学——日常 static_cast 覆盖九成。`),
    // ==================== 面向对象 ====================
    F('oo01', 'oop', '类与访问控制',
      R`class 与 struct 在 C++ 中的区别？三种访问控制的含义？`,
      R`C++ 中 class 与 struct 几乎相同——唯一默认差异：**struct 成员默认 public、class 默认 private**。
访问控制：**private**（仅类内与友元可访问——封装的边界）、**protected**（类内、派生类可访问）、**public**（任意访问）。
封装原则：数据成员 private、通过公共接口访问——不变量由类维护，外部无法破坏。`),
    F('oo02', 'oop', '构造函数与成员初始化列表',
      R`构造函数如何初始化成员？为什么优先用初始化列表？`,
      R`构造函数与类同名、无返回类型、可重载；**成员初始化列表**（冒号后逐成员初始化）：$$类名(参数) : 成员一(值一), 成员二(值二) \\{ ... \\}$$
优先用初始化列表的原因：列表是**直接初始化**（一次构造）；函数体内赋值是"默认构造 + 赋值"两步——浪费且 **const、引用、无默认构造的成员只能在列表中初始化**。
C++11 起还可用类内初始化（成员声明处给默认值）。`),
    F('oo03', 'oop', '析构函数与 RAII',
      R`析构函数何时调用？RAII 是什么思想？`,
      R`析构函数（波浪号 + 类名）在对象**生命周期结束**时自动调用：栈对象出作用域、堆对象被 delete、成员随宿主析构。
**RAII**：资源获取即初始化——资源（内存/文件/锁/连接）的获取放在构造函数、释放放在析构函数——依赖 C++ 的确定性析构，**异常与提前返回也不会泄漏**。
RAII 是 C++ 相对其他语言最大的工程优势——智能指针、lock_guard、容器全是它的实例。`),
    F('oo04', 'oop', 'this 指针与链式调用',
      R`this 是什么？何时必须显式使用？`,
      R`this 是指向**当前对象自身**的常量指针，每个非静态成员函数隐式持有。
必须显式的场景：① 成员函数参数与成员变量同名（this-成员 访问成员）；② 返回当前对象实现**链式调用**（return *this——流式接口）；③ 需要把自身传给其他函数。
静态成员函数没有 this（不属于任何实例）。`),
    F('oo05', 'oop', '继承与 protected',
      R`C++ 的继承语法与访问规则是什么？`,
      R`class 派生类 : public 基类 { ... }; —— 派生类获得基类全部成员。
继承方式（public 最常用）：**public 继承**保持基类访问级别（"是一个"关系）；protected/private 继承改变访问级别（实现细节，少用）。
**protected**：派生类可访问、外部不可——基类留给派生类的"内部接口"。
构造顺序：基类 → 成员（按声明序）→ 派生类构造函数体；析构相反。`),
    F('oo06', 'oop', '虚函数与动态多态',
      R`虚函数如何实现运行时多态？虚表机制是什么？`,
      R`基类函数加 **virtual** → 派生类重写（override 标注）→ 通过**基类指针或引用**调用时，实际执行**派生类的版本**（动态绑定）。
机制：每个含虚函数的类有**虚函数表（vtable）**，对象头部有虚表指针——调用时查表间接跳转（一次间接寻址的代价）。
要点：**通过值调用会发生切片**（slicing）——多态必须用指针或引用；基类析构函数必须 virtual（否则 delete 基类指针不调派生类析构 → 泄漏）。`),
    F('oo07', 'oop', '纯虚函数与抽象类',
      R`什么是纯虚函数与抽象类？接口类如何设计？`,
      R`纯虚函数：virtual 返回类型 函数名(参数) = 0; —— 只有声明没有实现（或提供默认实现供派生类调用）。
含纯虚函数的类是**抽象类**：不能实例化、只能作为基类被继承——派生类必须实现全部纯虚函数才能实例化。
设计：抽象类 = **接口**（定义"能做什么"）；实现类 = 具体行为——多态 + 工厂函数是 C++ 解耦的核心手法。`),
    F('oo08', 'oop', '运算符重载',
      R`运算符重载的规则与两种实现方式？`,
      R`为自定义类型赋予运算符含义：① 成员函数形式（左操作数是本类）：类型 operator+ (const 类型与符号 other) const; ② 非成员形式（左操作数非本类，如流输出）：友元或普通函数。
规则：不可发明新运算符、不可改变优先级与元数、某些运算符（逻辑与或、取址等）不可重载。
惯例：比较运算符返回 bool、加法返回值、加等就地修改；输出流运算符必须是非成员（左操作数是流）。`),
    // ==================== 拷贝与移动 ====================
    F('sp01', 'spec', '拷贝构造与拷贝赋值',
      R`拷贝构造函数何时被调用？默认行为是什么？`,
      R`拷贝构造：用同类型对象初始化新对象时调用（传值传参、按值返回、用对象初始化对象）。
**默认拷贝是逐成员复制**——含裸指针成员时复制的是**指针本身**（两个对象指向同一块内存 → 双重释放）——因此管理资源的类必须自定义拷贝行为。
签名：类名(const 类名与符号 other); —— const 引用（传值会无限递归）。`),
    F('sp02', 'spec', 'Rule of Three / Five / Zero',
      R`三法则、五法则、零法则分别是什么？`,
      R`**三法则**：需要自定义析构 / 拷贝构造 / 拷贝赋值之一（管理资源）→ 通常三个都需要。
**五法则**（移动语义加入后）：再加移动构造与移动赋值。
**零法则**（现代首选）：把资源管理交给智能指针与容器成员——类本身**不写任何五个特殊成员**，编译器生成的版本自动正确。
考试常考：三者出现的场景判断。`),
    F('sp03', 'spec', '移动语义与右值引用',
      R`什么是右值引用与移动语义？解决了什么问题？`,
      R`**右值引用**（双与符号类型名）绑定**临时对象（右值）**——即将销毁的对象，其资源可以"偷"过来。
**移动构造**：接管源对象的内部指针（源置空）而非深拷贝——临时大对象的"搬运"从深拷贝降为指针交换。
触发：std::move 显式把左值转为右值引用、返回临时对象、容器扩容/重排时自动移动。
意义：C++11 后容器操作的巨大性能提升来源；配合 noexcept 保证 vector 扩容用移动而非拷贝。`),
    F('sp04', 'spec', 'std::move 的本质与误用',
      R`std::move 做了什么？误用的后果？`,
      R`std::move **不做任何移动**——只是类型转换（把表达式转为右值引用），真正的搬运由移动构造/赋值完成。
误用：① 对 const 对象 move——退化为拷贝（const 无法绑定右值引用）；② move 后继续使用源对象（已处于"有效但未指定"状态）；③ 对右值引用参数再 move（参数本身是左值）。
纪律：move 仅用于"确定不再使用源对象"处；移动后的对象只能赋新值或销毁。`),
    // ==================== 模板与泛型 ====================
    F('tm01', 'tmpl', '函数模板',
      R`函数模板如何定义与实例化？`,
      R`template <typename T> T 最大值(const T 与符号 a, const T 与符号 b) { return a > b ? a : b; } —— 调用时编译器**按实参类型实例化**出具体版本（隐式实例化）。
显式指定：函数名<类型>(参数)——类型无法推导或多类型时用。
要点：模板是**编译期代码生成**——实例化几种类型就生成几份代码；模板定义通常放头文件（实例化时需见定义）。`),
    F('tm02', 'tmpl', '类模板与模板参数',
      R`类模板如何定义？模板参数可以是什么？`,
      R`template <typename T> class 容器 { T 数据; ... }; —— 使用时指定：容器<int> v;。
模板参数不止类型：**非类型模板参数**（整型/指针：template <typename T, size_t N> 定长数组）——编译期常量。
成员函数在需要时才实例化——未用到的成员不生成代码。
模板 + 运算符要求 = "鸭子类型"的编译期版本（概念 Concepts 在 C++20 标准化）。`),
    F('tm03', 'tmpl', 'STL 的泛型设计思想',
      R`STL 为什么能同时服务所有容器？`,
      R`STL 六大组件：**容器**（存数据）、**迭代器**（泛化的指针，容器与算法的粘合剂）、**算法**（sort/find/transform——不依赖具体容器）、仿函数、适配器、分配器。
核心思想：**算法通过迭代器操作容器**——算法只要求迭代器满足"能力分类"（输入/输出/前向/双向/随机访问），任何提供相应迭代器的容器都能用同一份算法。
这是"泛型编程"范式的奠基之作。`),
    // ==================== STL 标准库 ====================
    F('sl01', 'stl', 'vector：动态数组',
      R`vector 的特性与扩容机制是什么？`,
      R`连续内存动态数组：末尾追加均摊常数时间（push_back）、随机访问常数时间、中间插入删除线性。
**扩容机制**：容量不足时分配**更大的新块**（通常 1.5–2 倍增长因子）、搬运元素（有移动语义则移动）、释放旧块——均摊 O(1) 但单次 O(n)。
惯用：reserve 预留已知规模避免反复扩容；shrink_to_fit 收缩；clear 不缩容量。`),
    F('sl02', 'stl', 'string：字符串类',
      R`std::string 相对 C 字符串的优势？`,
      R`自动管理内存（动态扩容）、长度查询（size）、拼接（加号与追加）、比较（运算符重载）、查找（find 系列返回下标或npos）、与 C 字符串互转（c_str / data）。
子串：substr(起点, 长度)；查找：find 返回首次出现下标、找不到返回 npos（判断用 与 npos 比较）。
小字符串优化（SSO）：短字符串存在对象内部、无堆分配——string 传参用 const 引用避免拷贝。`),
    F('sl03', 'stl', 'map 与 set：有序关联容器',
      R`map 与 set 的底层结构与复杂度？`,
      R`**map**：键值对、按键排序；**set**：只有键。底层**红黑树**（自平衡二叉搜索树）——插入/查找/删除均为 O(log n)、**遍历有序**。
用法：m[键] 不存在时自动插入默认值（判存在用 find 或 count）；insert 不覆盖已有键；C++17 的 insert_or_assign / try_emplace 更明确。
有序容器的键必须支持比较（自定义键提供比较器）。`),
    F('sl04', 'stl', 'unordered_map：哈希容器',
      R`unordered_map 与 map 如何选择？`,
      R`**unordered_map**：哈希表实现——平均 O(1) 查找、**无序遍历**、最坏 O(n)（哈希冲突退化为链）；需要键可哈希（标准类型自带）。
选择：只做查找、不要求顺序、追求速度 → unordered（默认选它）；要求有序遍历/范围查询/顺序稳定性 → map。
哈希质量差（自定义键的哈希函数弱）会退化——CFA？不，工程上用标准库的哈希或测试分布。`),
    F('sl05', 'stl', 'STL 算法与迭代器',
      R`STL 算法如何配合迭代器使用？常用的有哪些？`,
      R`算法以**迭代器范围**为参数：sort(v.begin(), v.end())、find、count、transform（变换）、accumulate（累加）、copy、remove_if 与 erase 惯用法。
**迭代器分类**决定可用算法：随机访问（vector 的迭代器可 sort）、双向（list 可 reverse）、前向（哈希容器）。
现代写法：范围 for 覆盖大多数遍历需求；算法用于表达"意图"（remove_if 比手写循环更声明式）。`),
    // ==================== 内存与智能指针 ====================
    F('me01', 'mem', 'new/delete 与 malloc/free 的区别',
      R`new/delete 与 C 的内存函数有何本质区别？`,
      R`new：① 分配内存；② **调用构造函数**；③ 类型安全（无需强转）；④ 失败抛异常（而非返回空）。delete：调析构函数再释放——new/delete、new 数组与 delete 数组必须**严格配对**（混用未定义行为）。
malloc/free：纯内存分配释放、不调用构造析构——C++ 中仅用于与 C 交互或 placement new 场景。
配对纪律与 C 相同：不可重复释放、释放后置空。`),
    F('me02', 'mem', 'unique_ptr：独占所有权',
      R`unique_ptr 的特性与使用场景？`,
      R`**独占所有权**的智能指针：不可复制、可移动（所有权转移）——**零开销**（大小等于裸指针）。
创建：auto p = std::make_unique<类型>(构造参数)——异常安全且简洁；访问：与指针相同的运算符；释放：出作用域自动 delete。
所有权转移：auto p2 = std::move(p)——之后 p 为空。
默认选择：**不知道用哪个智能指针时，用 unique_ptr**；需要传出所有权时再转 shared。`),
    F('me03', 'mem', 'shared_ptr 与循环引用',
      R`shared_ptr 的引用计数机制？循环引用如何破解？`,
      R`**共享所有权**：内部维护**引用计数**——拷贝加一、析构减一、归零释放；开销：控制块（计数+弱计数）原子操作。
**循环引用**：A 持有 B 的 shared、B 又持有 A 的 shared → 计数永不归零 → **泄漏**。
破解：把"回指"的一侧改为 **weak_ptr**（弱引用不增计数）——使用时 lock() 升级为 shared。
原则：默认 unique；确实需要共享时 shared；父子/回指关系用 weak。`),
    F('me04', 'mem', 'weak_ptr 与悬空的破解',
      R`weak_ptr 的用途与使用方式？`,
      R`weak_ptr：由 shared_ptr 构造、**不增加引用计数**的观察者——用于① 打破循环引用；② 缓存（对象可能已被销毁）。
使用：不能直接解引用——先 **lock()** 尝试升级为 shared_ptr（对象活着返回有效 shared、已死返回空）；expired() 判断是否已销毁。
组合口诀：**独占用 unique、共享用 shared、观察用 weak、循环必用 weak**。`),
    // ==================== 现代 C++ ====================
    F('mo01', 'modern', 'Lambda 表达式',
      R`lambda 的语法与捕获方式是什么？`,
      R`[捕获列表](参数) -> 返回类型 { 函数体 }——内联匿名函数。
**捕获列表**：[] 不捕获（只用全局与参数）；[=] 按值捕获全部；[与符号] 按引用捕获全部；[x, 与符号 y] 指定捕获——捕获即把外部变量"带进"lambda。
⚠️ 按引用捕获的局部变量在 lambda 存活超过作用域时悬空——长生命周期回调用按值捕获或显式列出。
用途：算法谓词、回调、局部小逻辑。`),
    F('mo02', 'modern', '范围 for 与结构化绑定',
      R`C++11/17 的遍历与解绑语法是什么？`,
      R`范围 for：for (auto 元素 : 容器)——遍历（修改加与符号）；底层依赖 begin/end。
**结构化绑定**（C++17）：auto [键, 值] : map——一次解包一对；auto [x, y, z] : 三元组——具名解构元组/结构体。
可读性飞跃：告别 first 与 second 的魔法下标。`),
    F('mo03', 'modern', '可空值与可选类型',
      R`std::optional 解决什么问题？`,
      R`optional<类型>：**可能没有值**的类型——比"用特殊值表示无效"（如 -1、空串）语义明确得多。
用法：nullopt 表示空；has_value() 判断；value() 取值（空则抛异常）；value_or(默认值) 安全取值。
典型场景：查找可能失败（字典查询）、解析可能失败（字符串转数字）——"无结果"是合法状态而非错误。`),
    F('mo04', 'modern', 'C++ 的线程与同步概览',
      R`标准线程库的核心组件有哪些？`,
      R`C++11 起标准线程库：**std::thread**（线程）、**std::mutex** 与 lock_guard/unique_lock（互斥锁，RAII 封装）、**std::atomic**（原子变量——无锁计数）、**condition_variable**（条件等待通知）、**std::future 与 promise**（异步结果）。
数据竞争的本质：多线程无同步地访问同一内存位置且至少一个是写——data race 是未定义行为。
工具：ThreadSanitizer 检测竞态。`),
  ];

  const META = {
    ar01: [4, '翻译单元ODR'],
    ar02: [3, '前置声明'],
    ar03: [4, '存储期'],
    ar04: [4, '值类别'],
    ar05: [4, 'STL地图'],

    ba07: [4, 'cin'],
    ba08: [4, '重载默认参数'],
    ba09: [3, 'bool 比较'],
    ba10: [4, 'static_cast'],

    ba01: [3, '语言定位'], ba02: [3, 'iostream 与命名空间'], ba03: [5, '引用'], ba04: [4, 'const 语义'], ba05: [3, 'auto 推导'], ba06: [3, '范围 for'],
    oo01: [4, '封装'], oo02: [5, '初始化列表'], oo03: [5, 'RAII'], oo04: [3, 'this 指针'], oo05: [4, '继承'], oo06: [5, '虚函数与切片'], oo07: [4, '抽象类'], oo08: [3, '运算符重载'],
    sp01: [5, '拷贝构造'], sp02: [5, 'Rule of Zero'], sp03: [5, '移动语义'], sp04: [4, 'std::move 误用'],
    tm01: [4, '函数模板'], tm02: [4, '类模板'], tm03: [5, 'STL 设计思想'],
    sl01: [5, 'vector 扩容'], sl02: [3, 'string'], sl03: [4, 'map 有序容器'], sl04: [4, '哈希容器'], sl05: [4, '算法与迭代器'],
    me01: [4, 'new 与 delete'], me02: [5, 'unique_ptr'], me03: [5, 'shared_ptr 循环引用'], me04: [4, 'weak_ptr'],
    mo01: [4, 'lambda 捕获'], mo02: [3, '结构化绑定'], mo03: [3, 'optional'], mo04: [3, '线程库']
  };

  const PITFALL = {
    oo06: R`两条铁律：多态必须通过指针或引用调用（值调用切片）；基类析构函数必须 virtual。`,
    sp01: R`类里有裸指针成员 = 必须写五个特殊成员函数——否则默认浅拷贝双重释放。`,
    sp04: R`std::move 只做类型转换；move 过的对象只能赋新值或销毁，继续使用是逻辑错误。`,
    me03: R`shared_ptr 循环引用用 weak_ptr 破解——互相持有 shared 的两个对象永不释放。`,
    mo01: R`按引用捕获的局部变量在 lambda 存活超作用域后悬空——异步回调用按值捕获。`
  };

  // —— 帮助栏目文章（检索用，不参与学习调度；内容由原起步章迁移）——
  const HELP = [
    { id: 'bs01', title: 'C++ 适合做什么', body: R`在 C 的基础上加上**面向对象、标准库、泛型编程**——既要 C 级性能，又要高级抽象。
典型场景：游戏引擎（Unreal）、高频交易、图形渲染、浏览器内核——性能敏感的大型软件。
学习路线之争：可先学 C 再学 C++，也可**直接学现代 C++**（C++11 起）——本卡组按现代 C++ 为主线。` },
    { id: 'bs02', title: '安装编译器与环境', body: R`编译器三选一：**g++**（MinGW-w64/WSL）、clang++（Mac 自带）、MSVC（Visual Studio）。
验证：终端敲 g++ --version 出现版本号即成功。编辑器 VS Code 加 C++ 扩展，或直接上 CLion。
版本意识：编译时加 **-std=c++17**（或更高）启用现代特性——老标准会编译不过新写法。` },
    { id: 'bs03', title: '第一个程序：iostream 与 cout', body: R`标准骨架：include 输入输出头文件 → main 函数 → 用 **std::cout** 输出（左移符号连着写）→ return 0。
编译运行两步：**g++ main.cpp -o main**，然后 **./main**。
与 C 的 printf 相比，cout 自动识别类型——这是 C++ 「类型感知」风格的第一个体验。` },
    { id: 'bs04', title: '头文件与编译模型', body: R`**#include 就是复制粘贴**：预处理阶段把头文件内容原样拷进当前文件——理解了这点，很多编译错误都好懂。
惯例：**头文件（.h/.hpp）放声明，源文件（.cpp）放实现**；自己写的头文件用双引号 include，系统的用尖括号。
多文件项目的编译与**链接**是 C++ 的独门功课——先见怪不怪，build 章细讲。` },
    { id: 'bs05', title: '怎么读 C++ 报错', body: R`C++ 报错（尤其涉及模板/STL）动辄几十行——**别慌，策略有三**：
① 看编译器给出的**第一个** error（后面的多是连锁）；② 在长报错里搜索**你自己文件的名字与行号**——那一行才是你的问题；③ 模板报错看**最后几行**的「required from」链。
开启 **-Wall** 警告：初学阶段把警告当错误修。` },
    { id: 'bs06', title: '命名空间与 std', body: R`**命名空间**是名字的分区：标准库所有名字都住在 std 里（std::cout、std::string）——避免你自己的 cout 之类撞名。
**using namespace std;**（用 std 前缀）写小练习方便，但工程上不推荐放头文件——会污染所有包含者的命名空间。
初学建议：练习时图省事可用，从写正式代码起就老老实实写 std:: 前缀——肌肉记忆从第一天养成。` },
    { id: 'bs07', title: '工具链总览：CMake 与调试', body: R`**编译选项**：-std=c++17（标准版本）、-Wall -Wextra（警告全开）、-g（带调试信息）——记这三个起步够用。
**CMake**：多文件项目的构建系统事实标准——先知道名字，项目超过两三个文件时再学。
**gdb/lldb**：调试器；**valgrind/ sanitizer**：内存错误检测工具（mem 章会用到）——先混个脸熟。` },
    { id: 'bs08', title: 'C++ 学习路径建议', body: R`① 起步章 + 基础章打底（重点：引用、const、auto）；② 面向对象章是 C++ 的骨架——类、构造析构、虚函数逐一过；③ **STL 优先于模板元编程**：vector/string/map 用起来，90% 的日常需求就够了；④ 拷贝移动与智能指针（mem 章）是现代 C++ 的核心，值得反复。
节奏参考：每天 10 张卡 + 每周一个小项目（通讯录、简单容器实现）。
心态：C++ 广而深——按本卡组 ORDER 顺序走，别在模板章死磕而跳过 STL 实用主义。` }
  ];

  const BEGINNER = ['basic', 'oop'];
  return { BEGINNER: BEGINNER, id: 'cppl', name: 'C++', short: 'C++', icon: '➕', kind: 'qa', group: 'skill', CATS: CATS, DATA: DATA, META: META, HELP: HELP, REL: {}, PITFALL: PITFALL, MNEM: {}, ORDER: ['arch', 'basic', 'oop', 'spec', 'tmpl', 'stl', 'mem', 'modern'] };
})();
