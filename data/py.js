/*
 * Python 知识 · 知识点记忆数据库
 * 依据：Python 三.x 通用核心语法与工程常识（入门到中级系统学习）
 * 结构与其他学科一致：id / cat / title / front / back + META / REL / PITFALL / MNEM
 * 说明：无例题学科；kind: 'qa' 驱动 UI 适配。
 * 注意：正文刻意避开会被渲染器/校验器误解的写法——幂运算符用文字描述、不写反斜杠转义、
 *       不出现"小写字母+数字"样式的标识符。
 */
window.SUBJECTS = window.SUBJECTS || {};
window.SUBJECTS.py = (function () {
  'use strict';
  const R = String.raw;

  const CATS = {
    base: '基础语法与类型',
    ctrl: '控制流',
    ds: '数据结构',
    str: '字符串',
    func: '函数与作用域',
    oo: '面向对象',
    io: '异常与文件',
    mod: '模块与环境',
    adv: '进阶特性'
  };

  const F = (id, cat, title, front, back) => ({ id, cat, title, front, back });

  const DATA = [
    // ==================== 基础语法与类型 ====================
    F('ba01', 'base', 'Python 的核心语言特征',
      R`Python 是什么类型的语言？有哪些核心特征？`,
      R`**解释型**（源码直接由解释器执行，无需编译）、**动态类型**（变量无需声明类型，类型属于对象而非变量）、**强类型**（"1" + 1 会报错，不自动跨类型拼接）、**缩进即语法**（冒号后缩进定义代码块，官方推荐四空格）。
其他标签：多范式（面向对象+函数式）、自带"电池"（标准库丰富）、GIL 影响多线程并行（见进阶）。`),
    F('ba02', 'base', '变量、命名与动态绑定',
      R`Python 变量的本质是什么？命名规则有哪些？`,
      R`变量是**对象的名字（引用绑定）**，不是装值的盒子——赋值语句 a = [1, 2] 让名字 a 指向列表对象；两个名字可指向同一对象（共享可变对象时修改会互相可见，见浅拷贝卡）。
命名：字母数字下划线、不能以数字开头、区分大小写；约定 snake_case（小写下划线）；以下划线开头表示内部使用（单下划线为约定私有，双下划线触发名称改写）。`),
    F('ba03', 'base', '核心数字类型',
      R`Python 有哪三种数字类型？整数有什么特别之处？`,
      R`① int 整数：**任意精度**（不会溢出，可算超大数）；② float 浮点：双精度二进制浮点，存在表示误差（0.1 + 0.2 不等于 0.3），精确小数用 decimal 模块；③ complex 复数（如三加四j 写作 3+4j）。
整除 / 得 float；双斜杠（地板除）向下取整得 int；百分号取余；幂运算用两个星号（星号星号）——本卡刻意用文字描述以免符号被误渲染。`),
    F('ba04', 'base', '布尔值与真值判断',
      R`布尔类型如何定义？哪些值在条件判断中为假？`,
      R`True 与 False（首字母大写），是 int 的子类（True 等于 1）。
**假值（falsy）**：False、None、数字零（0、0.0）、空容器（空字符串、空列表、空元组、空字典、空集合）、空 range。其余一切对象为真值。
逻辑运算 and、or 返回**操作数的值**而非布尔（or 短路返回第一个真值，常见默认值写法）。`),
    F('ba05', 'base', 'None 与身份判断',
      R`None 是什么？为什么判断 None 要用 is 而不是相等比较？`,
      R`None 是单例对象（NoneType 唯一实例），表示"没有值"。
判断用 **is None / is not None**：is 比较对象身份（内存同一性），相等比较可能被自定义 dunder 方法重载出意外结果；且 None 只有一个实例，身份判断最快最可靠。`),
    F('ba06', 'base', '运算符优先级与链式比较',
      R`Python 支持链式比较吗？逻辑运算符的短路行为是什么？`,
      R`支持链式比较：0 < x < 10 等价于 (0 < x) and (x < 10)——数学写法直接可用。
优先级（高到低）：幂运算 → 一元负号 → 乘除模 → 加减 → 比较 → not → and → or（or 最低）。
短路：and 遇假即停返回假值，or 遇真即停返回真值——右侧可能不被求值（利用此特性写默认值与保护条件）。`),
    F('ba07', 'base', '类型转换与输入',
      R`如何做显式类型转换？input 函数返回什么类型？`,
      R`显式转换函数：int(x)、float(x)、str(x)、list(x)、tuple(x)、set(x)、bool(x)。
int 解析字符串失败抛 ValueError；int("3.5") 直接报错（先转 float 再转 int 或用 round）。
**input() 一律返回字符串**——数字输入必须手动 int() 或 float() 转换，新手第一大坑。`),
    // ==================== 控制流 ====================
    F('ct01', 'ctrl', '条件分支：if、elif、else',
      R`写出 Python 条件分支的结构与要点。`,
      R`if 条件一:
    语句
elif 条件二:
    语句
else:
    语句
要点：elif 可以有多个、按顺序短路匹配；Python **没有 switch/case**（三.x 后来加入 match-case 结构匹配，属另一机制）；条件表达式（三元）写法：值一 if 条件 else 值二。`),
    F('ct02', 'ctrl', 'for 循环与 range',
      R`Python 的 for 循环遍历什么？range 函数的参数是什么？`,
      R`for 遍历**可迭代对象**（列表、字符串、字典、文件行、range……），不是计数式循环。
range(起, 止, 步长)：**含头不含尾**（range(3) 产生 0、1、2）；步长可为负（倒序）。
遍历列表同时要下标用 enumerate(seq, start=0)；并行遍历两个序列用 zip。`),
    F('ct03', 'ctrl', 'while、break 与 continue',
      R`while 循环与 break、continue 的行为是什么？for 有 else 分支吗？`,
      R`while 条件为真重复执行；**break** 立即跳出本层循环；**continue** 跳过本轮剩余语句进入下一轮。
循环可以带 else：循环**正常结束（未被 break）**时执行 else——常用于"找没找到"的标记逻辑。
死循环防护：确保循环体内条件会变化。`),
    F('ct04', 'ctrl', 'match-case 结构匹配',
      R`三.x 新增的 match-case 是什么？与 switch 有何不同？`,
      R`match 值:
    case 模式一: 语句
    case 模式二 if 额外条件: 语句
    case _: 默认分支
与 C 系 switch 不同：它是**结构匹配（模式匹配）**——可解构序列/映射、绑定变量、带守卫条件，不发生隐式贯穿（无需写跳出口）。`),
    // ==================== 数据结构 ====================
    F('ds01', 'ds', '列表：特性与常用操作',
      R`列表的特性与最常用的增删改查方法有哪些？`,
      R`有序、**可变**、允许重复与混合类型。
增：append（尾部加一个）、extend（并入可迭代）、insert（指定位置）；删：pop（尾或指定索引并返回）、remove（按值删第一个）、del 语句；查：index、in 成员判断；排：sort（原地）、sorted（返回新表，key 与 reverse 参数）。
sort 与 sorted 的 key 参数（如按长度排：key=len）是高频考点。`),
    F('ds02', 'ds', '元组：不可变序列',
      R`元组与列表的区别？单元素元组怎么写？什么场景用元组？`,
      R`元组**不可变**（创建后不能增删改元素）——可哈希（可作为字典键、集合元素）、可解包（a, b = 一对值）。
单元素元组必须带逗号：(only,)（只写 (only) 是加括号的表达式）。
场景：固定结构的数据记录、函数多值返回（本质返回一个元组）、字典键。`),
    F('ds03', 'ds', '字典：键值映射',
      R`字典的特性、常用操作与取值的两种方式是什么？`,
      R`键值对集合；三.x 中保持**插入顺序**；键必须**可哈希**（不可变类型：字符串、数字、元组），值任意。
取值：d[键] 不存在抛 KeyError；d.get(键, 默认值) 不存在返回默认（安全读取）。
增改：d[键] = 值；删除：pop、del；遍历：keys()、values()、items()（配合 for 键, 值 in d.items()）。
合并：更新方法或解包写法（两个花括号星号解包在文字里从略）。`),
    F('ds04', 'ds', '集合：去重与集合运算',
      R`集合的特性与四大集合运算是什么？`,
      R`无序、元素**唯一**（自动去重）、可变（frozenset 为不可变版本）。
运算：并集（竖线符号或 union）、交集（和符号或 intersection）、差集（减号或 difference）、对称差（异或符号或 symmetric_difference）。
判断子集用比较运算符（小于等于）。去重列表惯用法：list(set(原列表))——但会丢失原顺序。`),
    F('ds05', 'ds', '切片语法',
      R`写出切片的完整语法与常见用法。`,
      R`seq[起:止:步长]——**含头不含尾**；步长默认一、可为负。
惯用法：复制序列 seq[:]；反转 seq[::-1]；取偶数位 seq[::2]；删头去尾 seq[1:-1]。
越界**不报错**（自动截断，与索引访问不同——单个下标越界抛 IndexError）。`),
    F('ds06', 'ds', '可变与不可变、深浅拷贝',
      R`可变对象与不可变对象的区别？浅拷贝与深拷贝的差异？`,
      R`可变（list、dict、set）：原地修改会影响所有引用者；不可变（int、str、tuple、frozenset）："修改"实际是创建新对象。
赋值只复制引用；**浅拷贝**（copy、切片、list 构造）新建外层容器、内层元素仍共享引用；**深拷贝**（copy 模块的 deepcopy）递归复制所有层级——嵌套可变结构必须用深拷贝才真正隔离。`),
    F('ds07', 'ds', '推导式（ comprehensions ）',
      R`写出列表推导式、字典推导式与集合推导式的形式，以及何时改用生成器。`,
      R`列表推导：[表达式 for 元素 in 可迭代 if 条件]——带条件的映射过滤，通常比等价 for 循环更快更清晰。
字典推导：{键表达式: 值表达式 for ...}；集合推导：{表达式 for ...}。
数据量大且只需逐个消费时，把方括号换成圆括号得到**生成器表达式**（惰性、省内存）。嵌套推导超过两层可读性骤降，改普通循环。`),
    F('ds08', 'ds', '序列解包与星号收集',
      R`什么是序列解包？函数定义与调用中的星号收集怎么用（文字描述）？`,
      R`解包：a, b, c = 序列（长度须一致）；星号收尾：first, 星号 rest = 序列（rest 收集成列表）。
函数定义中的单星号参数：把多余的**位置参数**收集成元组；双星号参数：把多余的**关键字参数**收集成字典（本卡以文字描述，避免符号被误渲染）。
调用端的星号则相反：把序列/字典**展开**传参。`),
    // ==================== 字符串 ====================
    F('st01', 'str', '字符串的不可变性与常用方法',
      R`字符串可变吗？列举最高频的字符串方法。`,
      R`字符串**不可变**——所有"修改"方法都返回新字符串。
高频方法：strip（去两端空白）、split（切分，默认按空白）、join（以分隔符拼接可迭代）、replace（替换）、startswith/endswith、find（找不到返回负一，index 抛错）、upper/lower、isdigit 等 is 系列判断。
大小写比较前先统一 case；split 与 join 是文本处理黄金搭档。`),
    F('st02', 'str', '格式化：f-string 与 format',
      R`三.x 推荐的格式化方式是什么？如何控制小数位与对齐？`,
      R`**f-string**（格式化字符串字面量）：字符串前加 f，花括号内直接写表达式与变量名——最快最可读。
格式说明符（冒号后）：保留两位小数（冒号加点二 f）、千分位（逗号）、百分比、宽度与对齐（大于号右对齐、小于号左对齐、补零）。
旧式：format 方法与百分号占位——读旧代码要认识，新代码一律 f-string。`),
    F('st03', 'str', '字符串与编码',
      R`Python 三.x 的字符串是什么编码？encode 与 decode 的方向是什么？`,
      R`三.x 的 str 是 **Unicode 文本**；处理字节用 bytes 类型（前缀 b 的字面量）。
方向记忆：**encode 编码 = 字符串到字节**（str.encode 得 bytes）；**decode 解码 = 字节到字符串**（bytes.decode 得 str）。互联网与文件 IO 的默认编码约定为 UTF-8；读写文件显式指定编码可避免跨平台乱码。`),
    F('st04', 'str', '原始字符串与常见转义',
      R`字符串里有哪些常见转义序列？原始字符串（前缀 r）何时使用？`,
      R`常用转义：换行符（反斜杠加字母 n）、制表符（反斜杠加字母 t）、表示反斜杠本身用双反斜杠、引号内嵌引号用转义或换引号类型。
**原始字符串**（引号前加 r）：反斜杠不转义、原样保留——正则表达式与 Windows 路径的必备写法（如路径中的盘符反斜杠）。`),
    // ==================== 函数与作用域 ====================
    F('fu01', 'func', '函数定义、返回值与文档字符串',
      R`函数如何定义与返回多值？什么是文档字符串？`,
      R`def 函数名(参数):
    文档字符串（首行三引号说明）
    语句
    return 结果
无 return 或裸 return 返回 None；return 多个值本质返回**元组**（接收端解包）。
文档字符串（首行三引号文本）是官方的函数说明方式，help() 与 IDE 提示读取它。`),
    F('fu02', 'func', '参数：默认值、关键字与陷阱',
      R`默认参数的大陷阱是什么？关键字参数如何使用？`,
      R`**默认参数只在函数定义时求值一次**——可变默认值（如空列表）会被多次调用共享修改，经典坑；正确写法：默认 None、函数体内再建新容器。
调用时可按**关键字传参**（函数名(参数名=值)），顺序无关、可读性高；参数太多时可用单星号强制其后只能关键字传参。`),
    F('fu03', 'func', '作用域：LEGB 规则与 global',
      R`Python 的变量查找顺序是什么？如何在函数内修改全局变量？`,
      R`LEGB 查找顺序：**L**ocal（函数内）→ **E**nclosing（外层函数）→ **G**lobal（模块级）→ **B**uilt-in（内建）。
函数内赋值会**创建局部变量**（即使外层同名）；要改全局需 global 声明、改外层函数变量用 nonlocal 声明——但实践中优先用返回值传递而非改全局。`),
    F('fu04', 'func', 'lambda 与高阶函数',
      R`lambda 是什么？map、filter、sorted 配合 key 的用法？`,
      R`lambda 参数: 表达式——匿名单表达式函数，适合作为一次性参数。
高阶函数（接收函数为参数）：map(函数, 可迭代)（逐个变换）、filter(谓词, 可迭代)（过滤）；sorted(数据, key=lambda 记录: 记录字段) 按 key 排序。
实践：简单场景用推导式更 Pythonic；复杂逻辑用 def 命名函数。`),
    F('fu05', 'func', '函数是一等公民与闭包',
      R`"函数是一等公民"是什么意思？闭包如何形成？`,
      R`一等公民：函数可以赋值给变量、作为参数传递、作为返回值——装饰器、回调、策略模式的基础。
**闭包**：内层函数引用了外层函数的变量，外层返回后该变量仍被内层"记住"（带状态的函数）；经典坑：循环内创建闭包引用循环变量——所有闭包共享最终值，需用默认参数固化。`),
    F('fu06', 'func', '递归与默认递归深度',
      R`递归函数的要点是什么？Python 默认递归深度限制是多少？`,
      R`递归 = 函数调用自身：必须有**终止条件**、问题规模逐层收敛。
Python 默认递归深度约一千层（防栈溢出，可用 sys 模块查看与调整）；深层递归（如长链遍历）应改写为循环或显式栈。
经典练习：阶乘、斐波那契（递归版重复计算多，应记忆化或迭代）。`),
    // ==================== 面向对象 ====================
    F('oo01', 'oo', '类与实例：定义与初始化',
      R`如何定义类？初始化方法与实例引用（常记作 self）的作用是什么？`,
      R`class 类名:
    def 初始化方法(self, 参数):
        self.属性 = 参数
初始化方法（名字前后各双下划线）在创建实例时自动调用；**self 指实例自身**、是实例方法的第一个参数（调用时自动传入，定义时必须显式写）。
实例属性属对象、类属性属类共享——同名时实例属性遮蔽类属性。`),
    F('oo02', 'oo', '继承与 super',
      R`如何继承类并扩展初始化方法？什么是方法重写？`,
      R`class 子类(父类): —— 子类获得父类全部方法；在子类中重新定义同名方法即**重写（覆盖）**。
子类初始化方法内用 super().初始化方法(参数) 调用父类逻辑，避免重复代码。
多重继承按**方法解析顺序（MRO）**线性查找（可用类的特殊属性查看顺序）；组合优于继承是通用设计原则。`),
    F('oo03', 'oo', '魔术方法（dunder）',
      R`什么是魔术方法？列举最常用的几个。`,
      R`双下划线包围的方法由解释器在特定语法下**隐式调用**：
初始化与销毁（前后双下划线的 init 与 del）、字符串化（str 面向用户、repr 面向调试）、长度（len）、比较系列（相等、小于等）、算术系列（加、减等）、容器协议（getitem、contains、iter）。
实现对应魔术方法即让自定义对象"接入"语言内建行为——鸭子类型的正式化。`),
    F('oo04', 'oo', '封装约定与 property',
      R`Python 如何表达"私有"？property 的作用是什么？`,
      R`无强制私有：单下划线前缀是"内部使用"约定；双下划线前缀触发**名称改写**（编译为 _类名__名字，防意外覆盖而非真私有）。
**property**：把方法包装成属性访问（读计算、写校验），保持接口稳定而不破坏封装——替代 Java 式的成对 getter/setter。`),
    F('oo05', 'oo', '类方法、静态方法与实例方法的区别',
      R`实例方法、类方法、静态方法如何定义与区分？`,
      R`实例方法：第一个参数是实例（self），操作实例状态——最常见。
**类方法**：装饰器 classmethod、第一个参数是类（惯名 cls），常作**替代构造器**（如从字符串解析建对象）。
**静态方法**：装饰器 staticmethod、无自动首参，只是命名空间内的普通函数。
判断标准：需要访问实例用实例方法、需要类本身用类方法、都不需要用静态方法。`),
    F('oo06', 'oo', '鸭子类型与协议',
      R`什么是鸭子类型？它和继承多态的区别？`,
      R`"走起来像鸭子叫起来像鸭子，那它就是鸭子"——Python 的多态不检查继承关系，只看对象**是否实现了被调用的方法**（行为协议）。
自定义对象实现迭代协议、容器协议、上下文协议即可接入对应内建语法；相比静态语言"先声明类型再多态"，鸭子类型灵活但错误推迟到运行时——类型注解（typing 模块）用于弥补可读性与工具检查。`),
    // ==================== 异常与文件 ====================
    F('io01', 'io', '异常处理结构',
      R`写出完整的异常处理结构，处理多个异常怎么写？`,
      R`try:
    可能出错的代码
except 具体异常 as 别名:
    处理
else:
    无异常时执行
finally:
    无论如何都执行（清理）
要点：**先捕获具体异常、最后兜底 Exception**——裸 except 会吞掉 KeyboardInterrupt 等系统异常；except 可写多个分支按顺序匹配。`),
    F('io02', 'io', '常见内置异常与主动抛出',
      R`最常见的内置异常有哪些？如何在代码中主动抛出异常？`,
      R`高频异常：ValueError（值类型正确但内容不合法）、TypeError（类型用错）、KeyError、IndexError、FileNotFoundError、ZeroDivisionError。
主动抛出：raise ValueError("说明信息")；自定义异常继承 Exception。
原则：**能优雅处理就不抛**；抛出时带清晰信息；不要用异常做正常流程控制（跳转开销与可读性差）。`),
    F('io03', 'io', 'with 上下文管理器',
      R`with 语句解决什么问题？它的原理是什么？`,
      R`with 打开表达式 as 名字: —— 自动管理资源的获取与**释放**（无论块内是否异常都执行清理），最典型是文件关闭。
原理：对象实现上下文协议（进入与退出两个双下划线方法），with 调用进入方法、块结束调用退出方法（退出方法接收异常信息）。
凡是"必须配对释放"的资源（文件、锁、网络连接）都用 with。`),
    F('io04', 'io', '文件读写模式与最佳实践',
      R`open 的常见模式有哪些？如何逐行读取大文件？`,
      R`模式：r 读（默认）、w 写（**清空已有内容**）、a 追加、加 b 为二进制；带加号可读写。
推荐写法：with open(路径, 模式, encoding 等于 UTF-8 的写法) as 文件对象。
大文件**逐行迭代**（for 行 in 文件对象）——惰性、内存占用恒定；readlines 会一次读入全部，慎用于大文件。`),
    // ==================== 模块与环境 ====================
    F('mo01', 'mod', '模块与 import 的几种方式',
      R`import 有哪几种写法？模块搜索顺序是什么？`,
      R`① import 模块（用点号访问成员）；② from 模块 import 名字（直接使用，注意命名冲突）；③ from 模块 import 名字 as 别名。
搜索顺序：脚本所在目录 → 环境变量路径 → 标准库 → 第三方包目录；**本地文件名与标准库同名会遮蔽标准库**（起名避坑：不叫 random.py 之类）。
每个模块只被导入一次（缓存于 sys.modules），模块顶层代码执行一遍。`),
    F('mo02', 'mod', '常用标准库速览',
      R`列举最常用的标准库模块及用途。`,
      R`collections（Counter 计数、defaultdict 带默认值、deque 双端队列）；itertools（组合迭代工具）；math 与 random；datetime（日期时间）；os 与 os.path、pathlib（路径，面向对象风格）；json（序列化）；re（正则）；sys（解释器与参数）；logging（日志，替代散落的打印）；unittest 与 pytest（测试）。
"自带电池"：写轮子之前先查标准库。`),
    F('mo03', 'mod', '虚拟环境与包管理',
      R`为什么需要虚拟环境？用什么工具创建与安装第三方包？`,
      R`虚拟环境 = 每个项目独立的解释器与包目录，**隔离依赖版本**（A 项目要旧版、B 项目要新版不冲突）。
创建：python 的 venv 模块（命令行运行 venv 模块加目录名），激活后 pip 安装只影响本环境。
包管理：pip install 包名；依赖清单惯例 requirements.txt（pip 的 freeze 子命令导出）；新工具 uv、poetry 提供更快的解析与锁定。`),
    // ==================== 进阶特性 ====================
    F('ad01', 'adv', '生成器与 yield',
      R`什么是生成器？与返回列表相比优势是什么？`,
      R`含 **yield** 关键字的函数是生成器：调用返回生成器对象，每次 next 执行到 yield 暂停并交出一个值、下次从暂停处继续——**惰性求值、状态自动保存**。
优势：处理大数据/无限序列时内存占用恒定（不一次构造全部结果）；可组合成数据处理管道。
生成器表达式：把列表推导的方括号换圆括号。`),
    F('ad02', 'adv', '迭代器协议',
      R`可迭代对象与迭代器的区别是什么？for 循环的底层机制？`,
      R`**可迭代对象**：实现可迭代方法（返回迭代器）；**迭代器**：实现下一个值方法（无更多值时抛停止异常）且自身可迭代。
for 循环底层：对可迭代对象调用可迭代方法拿迭代器，反复取下一个值直到停止异常。
要点：迭代器是**一次性**的（耗尽即空）——生成器、文件对象都是迭代器；列表是可迭代对象但不是迭代器（可反复遍历）。`),
    F('ad03', 'adv', '装饰器',
      R`什么是装饰器？写出一个计时装饰器的思路。`,
      R`装饰器 = **接收函数并返回新函数**的高阶函数，用 @名字 语法应用——在不修改原函数的前提下增加行为（计时、日志、缓存、权限校验）。
思路：内层包装函数先记时间、调用原函数、再算差并打印，外层返回包装函数；用 functools 模块的 wraps 装饰包装函数以保留原函数名与文档。
带参数的装饰器再多包一层（三层函数）。`),
    F('ad04', 'adv', 'GIL 与并发模型选择',
      R`什么是 GIL？多线程、多进程、异步各适合什么场景？`,
      R`**GIL（全局解释器锁）**：同一时刻仅一个线程执行 Python 字节码——多线程无法让 CPU 密集任务并行提速。
选择：**CPU 密集** → 多进程（multiprocessing，各自解释器绕开 GIL）；**IO 密集** → 多线程（等待时释放 GIL）或 **asyncio 异步**（单线程事件循环，高并发网络场景）；数据科学绕道 NumPy（底层 C 释放 GIL）。
三.x 后续版本开始提供可关闭 GIL 的实验构建，生态仍在演进。`),
    F('ad05', 'adv', '类型注解与数据类',
      R`类型注解是强制的吗？dataclass 解决什么问题？`,
      R`注解**不强制**（解释器默认忽略，靠 mypy 等工具静态检查）——写法：参数与返回值后跟冒号与类型（如名字: str、箭头加 int）。收益：可读性、IDE 补全、提前抓类型错误。
**dataclass** 装饰器：为纯数据类自动生成初始化、相等比较、 repr 等样板——少写几十行重复代码；frozen 参数可造不可变数据类。`),
    F('ad06', 'adv', 'Pythonic 风格速查',
      R`列举最常见的 Pythonic 写法与对应的反面写法。`,
      R`① 遍历用 for 元素 in 序列，不用下标套下标（要下标用 enumerate）；② 真值判断 if 列表: 而非 if len(列表) 大于零；③ 交换变量用 a, b = b, a；④ 拼接列表用 extend 或加号于新表，循环内 append 到字符串改用 join；⑤ 推导式替代简单 map/filter；⑥ EAFP 风格：先做再捕获异常（与 LBYL 先检查相对），Python 社区偏好前者。
总纲：可读性优先——"代码是写给人看的"。`),
  ];

  const META = {
    ba01: [3, '语言特征'], ba02: [4, '变量与命名'], ba03: [5, '数字类型'], ba04: [4, '真值判断'], ba05: [4, 'None 判断'], ba06: [3, '运算符与短路'], ba07: [4, '类型转换'],
    ct01: [3, '条件分支'], ct02: [5, 'for 与 range'], ct03: [4, 'while 与 break'], ct04: [2, 'match-case'],
    ds01: [5, '列表操作'], ds02: [4, '元组'], ds03: [5, '字典'], ds04: [3, '集合运算'], ds05: [5, '切片'], ds06: [5, '深浅拷贝'], ds07: [4, '推导式'], ds08: [3, '解包与星号收集'],
    st01: [5, '字符串方法'], st02: [4, 'f-string'], st03: [4, '编码'], st04: [3, '原始字符串'],
    fu01: [4, '函数定义'], fu02: [5, '默认参数陷阱'], fu03: [5, 'LEGB 作用域'], fu04: [3, 'lambda 与高阶函数'], fu05: [4, '闭包'], fu06: [3, '递归'],
    oo01: [5, '类与初始化'], oo02: [4, '继承与 super'], oo03: [4, '魔术方法'], oo04: [3, '封装与 property'], oo05: [3, '三种方法'], oo06: [3, '鸭子类型'],
    io01: [5, '异常结构'], io02: [4, '常见异常'], io03: [4, 'with 原理'], io04: [4, '文件读写'],
    mo01: [4, 'import 方式'], mo02: [3, '标准库速览'], mo03: [3, '虚拟环境'],
    ad01: [4, '生成器'], ad02: [4, '迭代器协议'], ad03: [4, '装饰器'], ad04: [3, 'GIL 与并发'], ad05: [3, '类型注解'], ad06: [3, 'Pythonic 风格']
  };

  const PITFALL = {
    ba03: R`浮点是二进制近似：0.1 加 0.2 不等于 0.3——精确小数用 decimal 模块。`,
    ds06: R`赋值不是拷贝；嵌套列表的"复制"用浅拷贝仍共享内层——改内层会互相可见，需 deepcopy。`,
    fu02: R`默认参数在定义时只求值一次：可变默认值（空列表）会被多次调用共享——默认写 None 函数体内再建。`,
    io04: R`写模式 w 会清空已有文件——追加用 a；读写文件始终显式指定 UTF-8 编码。`,
    ad04: R`多线程不加速 CPU 密集任务（GIL）：算得快的路是多进程或把热点交给 NumPy/C 扩展。`
  };

  return { id: 'py', name: 'Python 知识', short: 'Python', icon: '🐍', kind: 'qa', CATS: CATS, DATA: DATA, META: META, REL: {}, PITFALL: PITFALL, MNEM: {}, ORDER: ['base', 'ctrl', 'ds', 'str', 'func', 'oo', 'io', 'mod', 'adv'] };
})();
