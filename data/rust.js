/*
 * Rust · 知识点记忆数据库
 * 依据：The Rust Programming Language（The Book）+ Rust by Example 核心框架
 * 结构与其他学科一致：id / cat / title / front / back + META / PITFALL
 * 说明：无例题学科；kind: 'qa' 驱动 UI 适配。
 */
window.SUBJECTS = window.SUBJECTS || {};
window.SUBJECTS.rust = (function () {
  'use strict';
  const R = String.raw;

  const CATS = {
arch: '架构与规则',
    base: '基础与所有权',
    types: '类型系统',
    flow: '控制流与模式匹配',
    data: '结构体与枚举',
    err: '错误处理',
    coll: '集合与迭代器',
    eco: '工程与生态'
  };

  const F = (id, cat, title, front, back) => ({ id, cat, title, front, back });

  const DATA = [
    // ==================== 架构与规则 ====================
    F('ar01', 'arch', '宏系统概览：声明宏与过程宏',
      R`println! 结尾的感叹号是什么？`,
      R`感叹号标记**宏**（编译期代码生成）——不是函数：println!（输出）、vec!（建向量）、assert!（断言）都是**声明宏**（模式匹配生成代码）。
**过程宏**更进一步：直接操作语法树（derive 宏——#[derive(Debug)] 自动实现 trait 的原理）。
为什么用宏：参数个数可变、编译期展开零运行时开销——println! 的格式串是**编译期检查**的（占位符与参数不匹配直接编译错误——比 printf 安全一代）。`),
    F('ar02', 'arch', '内存布局与 Box：栈与堆的视角',
      R`Rust 的值住在哪里？`,
      R`默认在**栈**（大小编译期已知的类型——i32、固定数组）；需要堆时用 **Box**（智能指针——指针在栈、数据在堆）。
Rust 的纪律：谁拥有堆数据，谁负责释放——Box 离开作用域自动释放堆内存（无需手动 free，也无 GC）。
递归类型必须 Box（编译期算不出大小）；大对象 Box 避免栈拷贝——所有权模型与内存布局咬合的入门视角。`),
    F('ar03', 'arch', 'Copy 与 Move：赋值的字节语义',
      R`赋值时发生了什么？什么时候会「移动」？`,
      R`规则：实现了 **Copy trait** 的类型（整数/浮点/bool/char/由 Copy 组成的元组数组）——赋值与传参**按位拷贝**，原值仍可用。
没有 Copy 的类型（String/Vec/自定义结构体）——赋值即 **Move**：所有权转移，原变量编译器禁止再使用。
想保留原值又不拷贝堆数据：**clone()**（显式深拷贝——性能代价写在明面上）。
这是所有权系统的第一层：Rust 把「浅拷贝引用失效」这类 C++ 经典坑变成了编译错误。`),
    F('ar04', 'arch', '标准库地图',
      R`Rust 标准库的常用模块有哪些？`,
      R`**std::collections**——Vec/HashMap/HashSet/BTreeMap（coll 章主场）；**std::io**——stdin/stdout/文件读写；**std::fs 与 std::path**——文件系统操作；**std::string 与 std::fmt**——String 与格式化；**std::iter**——迭代器工具；**std::sync 与 std::thread**——并发（进阶）。
字符串双类型：String（拥有）与 str（切片）——标准库设计的代表案例。
习惯：标准库文档（doc.rust-lang.org/std）按模块树浏览——Rust 的 std 覆盖面决定你不用装多少第三方包。`),
    F('ar05', 'arch', 'Debug 与 Release：编译 profile',
      R`cargo run 和 cargo run --release 差在哪？`,
      R`两套编译配置：**dev（默认）**——不优化、溢出检查开启、编译快、运行慢；**release（--release）**——全优化、溢出回绕（不检查）、编译慢、快十倍以上。
推论一：初学永远用默认配置——溢出/越界等错误立刻 panic 暴露。
推论二：**测性能必须用 release**——Debug 下测出的耗时没有意义；Release 的优化可能改变浮点细节与溢出行为——行为差异要心里有数。`),
    // ==================== 基础与所有权 ====================
    F('ba01', 'base', 'Rust 的定位与核心承诺',
      R`Rust 是什么？它的核心卖点是什么？`,
      R`**系统级编程语言**：无垃圾回收器却**内存安全**——通过编译期的所有权系统在**编译时**拒绝内存错误（悬空、双重释放、数据竞争），运行时零额外开销（**零成本抽象**）。
其他特征：强静态类型 + 类型推导、表达式导向的语法、模式匹配、无空值（用 Option 表达"可能没有"）。
代价：学习曲线陡峭（所有权与借用规则）——"与借用检查器搏斗"是必经之路。`),
    F('ba02', 'base', 'Cargo 与项目管理',
      R`cargo 是什么？常用命令有哪些？`,
      R`Cargo 是 Rust 的**构建工具与包管理器**合一：new 创建项目、build 编译（debug 或 release 模式）、run 编译并运行、test 跑测试、check 快速语法检查（比 build 快）、add 添加依赖。
配置在 Cargo.toml（依赖来自 crates.io 中央仓库）——依赖解析与版本锁定（Cargo.lock）自动化。
对比：cargo 之于 Rust ≈ npm 之于 Node，但带完整构建系统。`),
    F('ba03', 'base', '变量绑定与可变性',
      R`Rust 的变量默认可变吗？shadowing 是什么？`,
      R`**默认不可变**：let x = 5; 之后再赋值编译报错——需要可变必须显式声明（let mut x）。"默认不可变"是 Rust 安全哲学的第一课。
**Shadowing（遮蔽）**：同作用域用 let 重新声明同名变量——新变量**遮蔽**旧变量（可改变类型），与 mut 的"原地修改"不同。
用途：值的转换链（原始解析版 → 遮蔽为解析后的类型）保持名字简洁。`),
    F('ba04', 'base', '基本类型与类型推导',
      R`Rust 的基本类型有哪些？类型推导何时需要标注？`,
      R`标量：i8 到 i128 与 isize（有符号整数）、u 系列（无符号）、f32/f64（浮点）、bool、char（Unicode 四字节）。
复合：元组（固定长度异构）、数组（固定长度同构、栈上）。
**类型推导**：多数情况无需标注——但推导需要上下文（如解析函数的默认类型有歧义时）必须显式标注（冒号加类型）。
整数溢出：debug 模式 panic、release 模式环绕（要用显式检查方法处理）。`),
    F('ba05', 'base', '函数定义与表达式返回',
      R`Rust 函数的语法特点？什么是"表达式作为返回值"？`,
      R`函数：fn 函数名(参数: 类型) -> 返回类型 { ... }——**参数必须标注类型**（无推导）、返回类型用箭头；不写箭头即无返回（相当于单元类型）。
**表达式导向**：函数体最后一个**不带分号的表达式**即为返回值（分号会丢弃值返回单元类型）——if、match、块都是表达式。
例：加法函数体直接写 a 加 b（不带分号）即返回和。`),
    F('ba06', 'base', '所有权三规则',
      R`Rust 的所有权三规则是什么？什么是移动语义？`,
      R`① 每个值有**唯一的所有者**（变量）；② 同一时刻只能有一个所有者；③ 所有者离开作用域时值被**丢弃（drop）**。
**移动语义**：把堆上数据赋给新变量 = **所有权转移**（旧变量失效）——不是浅拷贝（无双重释放）也不是深拷贝（零成本）。
需要深拷贝时显式调用 clone 方法；栈上的简单类型实现 Copy trait、赋值自动复制（不走移动）。`),
    F('ba07', 'base', '借用：不可变与可变引用',
      R`借用规则是什么？"同一时刻"的精确含义？`,
      R`借用 = 不获取所有权的临时访问：**不可变引用**（与符号，只读、可同时存在多个）与**可变引用**（mut 加与符号，可写、**同一作用域内只能有一个**）。
核心规则：不可变引用与可变引用**不能同时存在**——编译期杜绝数据竞争。
"同一时刻"按使用区间（NLL 非词法作用域）判断：最后一个使用点之后引用即结束。
例外豁免：内部可变性类型（RefCell）把借用检查推迟到运行时。`),
    F('ba08', 'base', '切片类型',
      R`什么是切片？字符串切片的写法？`,
      R`切片 = 对集合**一段连续序列的引用**（指针 + 长度）——不拥有数据。
字符串切片：与符号加方括号范围（如 s 的前五字节）——类型是字符串切片类型；字符串字面量本身就是切片类型。
函数参数的惯用法：收字符串切片类型（而非 String 引用）——同时兼容 String 与字面量（ Deref 强制转换）。`),
    F('ba09', 'base', 'println! 与格式化',
      R`输出的格式化语法？`,
      R`**println!**（宏——名字带感叹号）：占位用**花括号**——println!("你好, {}", name)；连续占位或指名：{0}、{name}（捕获变量名，Rust 特色）。
调试格式：**{:?}**（Debug 输出——打印结构体/数组就靠它）、{:#?}（美化缩进版）；小数：{:.2}（两位）。
输出变量直接写进花括号而不带参数列表——这是 println!("x = {x}") 的简写，现代 Rust 惯例。`),
    F('ba10', 'base', '运算符与整数溢出',
      R`运算符与别的语言最大的差异？`,
      R`算术/比较/逻辑与 C 系一致（逻辑与或是两个符号、短路求值）。**差异在溢出**：
Debug 构建下整数溢出**直接 panic**（崩溃报错），Release 下回绕——初学期用 Debug 构建让溢出立刻暴露。
取模/整除：/ 是整除（整数间）、% 取余；**没有隐式数值转换**——i32 和 i64 直接运算编译不过，必须显式 as 或 TryFrom（见类型转换卡）。
还有一个特殊运算符：两个问号——err 章的重头戏，先混个脸熟。`),
    F('ba11', 'base', '关键字总览：保留字一览',
      R`Rust 有哪些关键字？`,
      R`**定义**：fn/let/mut/struct/enum/impl/trait/type/const/static；**控制流**：if/else/match/loop/while/for/in/break/continue/return；**模块**：mod/use/pub/crate/self/Super（超，大写——Self 类型）；**所有权**：move/ref/mut；**异步**：async/await；**unsafe**：unsafe/as/dyn/where。
loop 是 Rust 特色关键字（无限循环且可作表达式返回值）。
误用保留字命名编译器直接报错——报错信息还会告诉你这是保留字。`),
    F('ba12', 'base', '类型转换：as 与显式转换',
      R`Rust 怎么做类型转换？`,
      R`**as** 显式转换（基本数值类型间）：x as i64、3.9 as i32（**截断**为 3，不四舍五入）——语义与 C 的强制转换相同。
设计哲学：**没有任何隐式数值转换**——i32 混 i64、整数混浮点都要显式写 as，让每次可能的精度损失都经过你的手。
可能失败的转换（字符串解析数字、超范围收窄）不用 as，用返回 Result 的 parse/TryFrom——错误处理章的配合拳。`),
    F('ba13', 'base', '条件与循环：一切皆表达式',
      R`Rust 的控制流写法与特色？`,
      R`**if 不加括号、花括号强制**；特色：**if 是表达式**——let x = if 条件 { 5 } else { 10 };（有分号返回空——两分支类型必须一致）。
**loop**：无限循环，break 可以**带值返回**——loop { break 计算结果; } 直接赋给变量。
**while** 与 **for**（for 遍历迭代器：for i in 0..5——左闭右开区间；0..=5 含尾）。
match 才是 Rust 控制流的主角——flow 章专讲。`),
    // ==================== 类型系统 ====================
    F('ty01', 'types', '类型转换：as 与 From/Into',
      R`Rust 的类型转换有哪些方式？`,
      R`① **as 运算符**：原始数值类型间的显式转换（截断/环绕语义——可能丢失数据，编译器允许）；② **From trait**：实现 From 后自动获得 Into（from 转入 / into 转出）——安全的标准转换路径；③ 字符串解析：parse 方法返回 Result（失败是正常路径）。
原则：可能丢失数据的转换用 as（显式冒险）、无损转换用 From/Into（编译器保证）、字符串解析用 parse（运行时校验）。`),
    F('ty02', 'types', 'Option：没有空值的世界',
      R`Rust 如何表达"可能没有值"？与空指针的区别？`,
      R`**Option 枚举**：Some(值) 或 None——把"可能缺失"编码进**类型系统**，编译器强制处理 None 分支——空指针异常在 Rust 中**不存在**。
解包：match 分支处理、unwrap_or(默认值)、unwrap_or_else(计算默认)、? 运算符向上传播。
unwrap 直接取值（None 时 panic）——原型与测试可用、生产代码需有理由。`),
    // ==================== 控制流与模式匹配 ====================
    F('fl01', 'flow', 'if 表达式与循环',
      R`Rust 的 if 与循环有什么独特之处？`,
      R`**if 是表达式**：let 值 = if 条件 { 一 } else { 二 }——分支必须有相同类型（作为返回值用）。
循环：loop（无限循环、**可通过 break 返回值**——重试逻辑的优雅写法）、while、**for 遍历迭代器**（for 元素 in 集合——没有 C 式三段 for，遍历一律走迭代器）。
范围：起点..终点（不含）与起点..=终点（含）。`),
    F('fl02', 'flow', 'match：穷尽性模式匹配',
      R`match 表达式的核心特性是什么？`,
      R`match 值 { 模式一 => 表达式, 模式二 => 表达式, ... }——
① **穷尽性**：必须覆盖所有可能（用兜底分支 _ 补全）——编译器强制；
② 模式可以**解构**（枚举变体、元组、结构体、区间、多重模式）；
③ 是**表达式**（分支的值即 match 的值）。
Rust 的 match = 加强版 switch：安全（无穿透）+ 强大（解构绑定）+ 强制（穷尽）。`),
    F('fl03', 'flow', 'if let 与 while let：简化匹配',
      R`if let 何时替代 match？`,
      R`只关心**一种模式**、其余忽略时，match 会写一堆兜底分支——**if let 模式 = 表达式** 只处理匹配情况（可加 else）。
**while let**：只要模式匹配就持续循环（从迭代器逐个弹出直到空）。
选择：多分支解构用 match、单模式用 if let——简洁与穷尽性的折中。`),
    // ==================== 结构体与枚举 ====================
    F('da01', 'data', '结构体的三种形式与 impl',
      R`结构体有哪三种形式？方法如何定义？`,
      R`① **命名字段**（struct 点（英文句点）前缀的花括号字段）；② **元组结构体**（字段无名按位置——新类型模式）；③ **单元结构体**（无字段——标记类型）。
方法：**impl 块**中定义——第一个参数是自身引用（&self 读、&mut self 写、self 转移所有权）的方法即**关联函数/方法**；无 self 的称为关联函数（如构造器，惯例名 new）。
调用：实例点方法名；类型双冒号关联函数名。`),
    F('da02', 'data', '枚举与代数数据类型',
      R`Rust 枚举为什么被称为"代数数据类型"？`,
      R`枚举的变体可以**携带不同类型与数量的数据**：enum 消息 { 退出, 移动 { x: i32, y: i32 }, 文本(String), 变色(红绿蓝) }——"或"的组合（结构体是"与"的组合）——合称代数数据类型（ADT）。
配合 match 解构：每个变体独立处理、携带的数据自动绑定为变量。
标准库核心枚举：Option（Some/None）与 Result（Ok/Err）——语言级建模"缺失"与"失败"。`),
    F('da03', 'data', '泛型与 trait 约束',
      R`Rust 的泛型如何声明与约束？`,
      R`泛型函数：fn 函数名<T: 某trait>(参数: T) —— **trait 约束**限定 T 必须实现某能力（否则编译报错——调用方的问题）。
impl 块也可泛化（impl<T> 结构体<T>）。
与 C++ 模板的区别：Rust 泛型**必须声明约束**、编译期单态化生成具体代码（零成本）但错误信息更友好（约束显式）。
标准 trait：Copy/Clone（复制）、Debug（调试打印）、PartialEq（比较）、Display（展示）。`),
    // ==================== 错误处理 ====================
    F('er01', 'err', 'Result：可恢复错误的建模',
      R`Result 枚举如何建模错误？`,
      R`enum Result<T, E> { Ok(T), Err(E) }——成功带值、失败带错误信息的**类型化**错误处理（对比异常：错误出现在函数签名里、编译器强制处理）。
处理：match 全分支、unwrap/expect（Ok 取值、Err panic）、组合器（map 转换 Ok 值、and_then 链式、unwrap_or 默认）。
原则：库代码返回 Result 把决策交给调用方；只有"不可能失败的前提"或示例代码才 unwrap。`),
    F('er02', 'err', '? 运算符：错误传播的语法糖',
      R`? 运算符做什么？与传统传播写法的对比？`,
      R`表达式后加 **?**：Ok 则取出值继续、Err 则**立即从当前函数返回该错误**（要求函数返回类型兼容）。
对比：手写 match Err 分支 return——一行问号替代五行样板。
配合：错误类型实现 From trait 时 ? 自动转换错误类型（底层错误 → 业务错误的桥）。
适用：链式传播错误的函数（文件读取解析写库一条链）。`),
    F('er03', 'err', 'panic!：不可恢复错误的边界',
      R`何时应该 panic？panic 与 Result 的分工？`,
      R`**panic** = 不可恢复：程序无法继续的正确性前提被破坏（数组越界、断言失败、外部环境崩坏）——栈展开或立即终止。
分工：**库代码返回 Result**（把处理权交给调用方）、**调用方决定**继续恢复或 panic；示例/原型/测试可 unwrap。
红线：panic 的信息是给开发者的、不是给用户的——用户面错误必须 Result 化并友好呈现。`),
    F('er04', 'err', '自定义错误类型',
      R`如何设计自定义错误？thiserror 的角色？`,
      R`自定义错误 = 实现 **Error trait**（Debug + Display 必需）的枚举——每个变体一种失败模式（解析失败/找不到/权限不足……各携带自己的上下文数据）。
实现 From trait 使 **? 自动转换**底层错误。
工程：thiserror 派生宏自动生成 Display 与 From 样板；应用层用 anyhow（动态任意错误容器）——库用具体类型、应用用 anyhow 的分层惯例。`),
    // ==================== 集合与迭代器 ====================
    F('cl01', 'coll', 'Vec：动态数组',
      R`Vec 的创建、访问与常用操作？`,
      R`创建：Vec::new() 或 vec 宏（方括号列举）；追加：push（尾部）；访问：下标（越界 panic）或 get（返回 Option——越界返回 None）。
遍历：for 元素 in 与符号 vec（借用）或 into_iter（转移所有权）；删除：remove（按下标）、pop（尾部弹出）。
与数组的关系：数组固定长度栈上、Vec 堆上可增长——函数参数惯例收切片类型（兼容两者）。`),
    F('cl02', 'coll', 'HashMap：键值映射',
      R`HashMap 的创建、访问与更新惯用法？`,
      R`创建：HashMap::new()；插入：insert(键, 值)——**同键覆盖**；访问：get 返回 Option（存在性安全）、方括号下标（不存在 panic）。
惯用法：entry API——**不存在则插入默认、存在则修改**（一行实现计数器：entry(键).or_insert(0) 然后自增）。
遍历：无固定顺序（RandomState 随机化——防哈希碰撞攻击）；遍历用 for (键, 值) in 映射。`),
    F('cl03', 'coll', '迭代器：惰性与适配器',
      R`迭代器的惰性求值与适配器链是什么？`,
      R`迭代器是**惰性**的：创建时不执行任何计算、消费方法（for/collect/sum）才驱动。
**适配器**（返回新迭代器）：map 变换、filter 过滤、take 限量、skip 跳过、zip 配对、chain 连接——链式组合成处理管道。
**消费者**（触发执行）：collect 收集、sum 求和、max、any/all、fold 折叠。
零成本：适配器链编译后与手写循环同等高效——抽象不付运行时税。`),
    F('cl04', 'coll', '迭代器与闭包的配合',
      R`迭代器适配器如何接收闭包？`,
      R`适配器接收**闭包**作为变换逻辑：map(|元素| 元素运算)、filter(|元素| 条件)、for_each(|元素| 副作用)。
闭包捕获环境变量——map 一个值域加偏移量、filter 一个阈值——逻辑与数据组合成自包含的管道。
类型推导：闭包参数类型由迭代器元素类型推导——无需标注（与泛型约束联动）。`),
    // ==================== 工程与生态 ====================
    F('ec01', 'eco', '模块系统：crate、module 与 pub',
      R`Rust 的模块层级是什么？可见性如何控制？`,
      R`层级：**crate**（编译单元：二进制或库）→ **module**（逻辑分区：mod 模块名 { } 或 mod 文件名; 引入文件）→ 条目。
可见性：默认**私有**（仅父模块可见）——**pub** 公开；pub use 把内部路径**重导出**到顶层（用户不必知道内部结构）。
引用路径：crate 根、super（父模块）、self（当前）、双冒号路径——模块树即命名空间树。`),
    F('ec02', 'eco', '测试：内置测试框架',
      R`Rust 的单元测试如何编写与运行？`,
      R`函数上方加测试属性标记（井号加中括号的 test）即成测试函数；**断言宏**：assert!（布尔）、assert_eq!/assert_ne!（相等/不等，失败打印两侧值）。
**断言 panic**：属性加 should_panic——验证错误路径确实 panic。
组织：单元测试与代码同文件（mod tests 模块、标注 cfg(test) 仅测试编译时包含）；集成测试放 tests 目录（每文件独立 crate）。
运行：cargo test（可按名称过滤）。`),
    F('ec03', 'eco', '常用工具链：clippy 与 rustfmt',
      R`clippy 与 rustfmt 的作用是什么？`,
      R`**clippy**：官方 lint 工具——数百条规则揪出低效写法、惯用法偏离、潜在 bug（如用 clone 代替拷贝、可用迭代器的手写循环）；cargo clippy 运行。
**rustfmt**：官方格式化器——统一缩进、换行、导入排序（cargo fmt 一键）；团队协作零格式争论。
生态惯例：CI 里 clippy 加 fmt 检查作为门禁——风格与惯用法问题在合并前拦截。`),
    F('ec04', 'eco', 'Rust 的适用场景与生态位',
      R`Rust 适合什么场景？生态有哪些代表项目？`,
      R`甜蜜区：**性能敏感 + 内存安全**并存处——系统工具（重写 C 基础设施：命令行工具 ripgrep/fd）、WebAssembly 前端高性能模块、嵌入式、区块链、操作系统组件、高性能 Web 后端（actix/axum）、CLI 工具。
生态：Web（axum/actix-web）、异步运行时（tokio）、序列化（serde——JSON/YAML 框架的事实标准）、命令行（clap）。
定位：C/C++ 的现代替代 + 侵入应用层的"类型驱动开发"。`),
  ];

  const META = {
    ar01: [3, '宏系统'],
    ar02: [4, '内存布局Box'],
    ar03: [5, 'Copy与Move'],
    ar04: [3, '标准库地图'],
    ar05: [3, 'Debug与Release'],

    ba09: [4, 'println 格式化'],
    ba10: [4, '溢出'],
    ba11: [4, '关键字'],
    ba12: [4, 'as 转换'],
    ba13: [4, '表达式控制流'],

    ba01: [4, '语言定位'], ba02: [4, 'Cargo'], ba03: [4, '可变性与遮蔽'], ba04: [4, '基本类型'], ba05: [4, '表达式返回'], ba06: [5, '所有权三规则'], ba07: [5, '借用规则'], ba08: [3, '切片'],
    ty01: [4, '类型转换'], ty02: [5, 'Option'],
    fl01: [3, 'if 表达式'], fl02: [5, 'match 穷尽性'], fl03: [3, 'if let'],
    da01: [4, '结构体与方法'], da02: [5, '枚举与 ADT'], da03: [4, '泛型与 trait'],
    er01: [5, 'Result'], er02: [5, '? 运算符'], er03: [4, 'panic 边界'], er04: [3, '自定义错误'],
    cl01: [4, 'Vec'], cl02: [4, 'HashMap'], cl03: [5, '迭代器惰性'], cl04: [3, '迭代器与闭包'],
    ec01: [4, '模块系统'], ec02: [4, '内置测试'], ec03: [3, 'clippy 与 rustfmt'], ec04: [3, '生态位']
  };

  const PITFALL = {
    ba06: R`堆数据的赋值默认是移动不是拷贝——旧变量失效是编译器在保护你，clone 才是真复制。`,
    ba07: R`可变引用与不可变引用不能同时存在——数据竞争在编译期就被拒绝。`,
    ty01: R`as 是可能丢数据的显式冒险：溢出环绕不报错——关键数值用检查方法或 From。`,
    er02: R`? 要求函数返回类型与错误兼容——底层错误到业务错误的转换靠 From 实现。`,
    er03: R`库代码返回 Result 把决策交给调用方；unwrap 只该出现在测试与"逻辑上不可能失败"处。`
  };

  // —— 帮助栏目文章（检索用，不参与学习调度；内容由原起步章迁移）——
  const HELP = [
    { id: 'bs01', title: 'Rust 适合做什么', body: R`**系统级编译语言**，两大承诺：**内存安全**（编译期杜绝空指针/悬垂指针等）与**零成本抽象**（高级写法不损失性能）。
场景：系统工具、命令行程序、WebAssembly、性能敏感的后端；连续多年「最受喜爱语言」榜首。
诚实预告：**学习曲线陡**——所有权概念会卡住几乎所有人。但编译器报错是教科书级的，它会一步步教你写对——本卡组也会陪你过这关。` },
    { id: 'bs02', title: '安装 rustup：一行命令全套', body: R`官方安装器 **rustup** 一行命令装全套（rustc 编译器 + cargo 构建工具 + 标准库）；Windows 也一样顺畅（官网下载 rustup-init）。
验证：终端敲 **cargo --version** 与 rustc --version 出版本号即成功。
升级也一条命令：rustup update——Rust 版本节奏稳定（六周一版），工具链升级从没负担。` },
    { id: 'bs03', title: '第一个程序：cargo new', body: R`**cargo new hello**（创建项目目录，自带 Cargo.toml 与 src/main.rs）→ 进入目录敲 **cargo run**——编译加运行一步到位。
main.rs 里 main 函数是入口，println! 宏负责输出（带感叹号——宏的标志）。
注意：默认就建好了「项目」而非单个文件——Rust 从第一天就是工程化姿势，包管理不用学第二次。` },
    { id: 'bs04', title: 'cargo 是什么：三合一工具', body: R`**构建工具 + 包管理器 + 测试器**三合一——Rust 生态的中央枢纽：
cargo run（编译运行）、cargo build（只编译）、**cargo add 包名**（引入第三方库，自动写进 Cargo.toml）、cargo test（跑测试）。
**Cargo.toml** 是项目清单：名字、版本、依赖——看一眼就懂。
别处语言要拼装的工具链，Rust 出厂即全套——这是新手体验最好的编译语言之一。` },
    { id: 'bs05', title: '怎么读 rustc 报错', body: R`Rust 报错是**三段式教学**：① 错误是什么（带精确行号与高亮）；② **为什么**（解释背后的规则，如「值在这里被移动了」）；③ **怎么改**（直接给出修复建议代码，很多照抄即可）。
心法：**把编译器当老师不当拦路虎**——它拒绝你，是因为这段代码在运行时真的会出事。
报错看不懂时先读 suggestion 部分，再回头读规则解释。` },
    { id: 'bs06', title: '注释与文档注释', body: R`普通注释 **//**（行）与 /* */（块）——与其他语言相同。
Rust 特色：**文档注释 ///**（三个斜杠）——写在函数上方，cargo doc 命令自动生成 HTML 文档站，标准库文档就是这么来的。
惯例：公开函数写文档注释（含示例），内部实现写普通注释——「文档即代码的一部分」是 Rust 工程文化的入口。` },
    { id: 'bs07', title: '工具链总览：clippy 与 rust-analyzer', body: R`**rust-analyzer**：编辑器插件（VS Code 装 Rust 扩展自动带上）——类型提示、补全、行内报错，**第一优先级装它**。
**clippy**：官方代码检查器（cargo clippy），几百条「更地道的写法」建议——初学就开始跑。
**rustfmt**：自动格式化（cargo fmt）——风格争论终结者。
测试：cargo test 内置——测试框架都第三方都不用装。` },
    { id: 'bs08', title: 'Rust 学习路径建议', body: R`① 起步章 + 基础章打底；② **所有权是最大关卡**（base 章后段）——被借用检查器拒绝时回到这两张卡，配合画「值的所有权箭头图」；③ 类型与模式匹配按 ORDER 走；④ 错误处理章（Result/问号运算符）是 Rust 工程品味的核心；⑤ 集合与迭代器让代码现代起来。
节奏参考：每天 10 张卡 + 每周一个小工具（grep 简化版、todo CLI）。
心态：前两周被编译器拒绝是所有人的常态——坚持过所有权，后面一马平川。` }
  ];

  const BEGINNER = ['base', 'types'];
  return { BEGINNER: BEGINNER, id: 'rust', name: 'Rust', short: 'Rust', icon: '🦀', kind: 'qa', group: 'skill', CATS: CATS, DATA: DATA, META: META, HELP: HELP, REL: {}, PITFALL: PITFALL, MNEM: {}, ORDER: ['arch', 'base', 'types', 'flow', 'data', 'err', 'coll', 'eco'] };
})();
